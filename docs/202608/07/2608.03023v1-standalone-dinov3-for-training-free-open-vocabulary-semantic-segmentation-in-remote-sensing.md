---
title: Standalone DINOv3 for Training-Free Open-Vocabulary Semantic Segmentation in Remote Sensing
title_zh: 用于遥感无训练开放词汇语义分割的独立DINOv3
authors: "Changhao Zhao, Haoxiang Li, Yuke Li, Hai Liu, LingLin Zeng"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03023v1"
tags: ["query:rs-fm"]
score: 9.0
evidence: 利用DINOv3基础模型实现遥感免训练语义分割
tldr: 遥感语义分割受限于像素级标注成本，训练免开放词汇方法成为趋势。DINOv3新引入DINO.txt，使独立DINO骨干具备图文对比学习能力，可用于开放词汇分割。本文提出DinoSplat-OV，无需微调或额外预训练，通过文本感知拉普拉斯传播模块结合文本语义亲和与局部视觉相似性去噪，并通过高斯泼溅上采样模块在RGB引导下重建像素级特征，配合全局锚点滑窗支持大尺寸影像。在UDD5、DOTA和LoveDA上达到优于现有训练免方法的性能，填补了DINO系列在训练免开放词汇分割中的空白。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03023v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1798, \"height\": 703, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03023v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 869, \"height\": 215, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03023v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 843, \"height\": 193, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03023v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1833, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03023v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 836, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03023v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 836, \"height\": 801, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03023v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1453, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03023v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 846, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03023v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 860, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03023v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 566, \"height\": 342, \"label\": \"Table\"}]"
motivation: 遥感影像像素级标注昂贵，而现有训练免开放词汇方法在遥感领域性能不足；DINOv3的发布带来新机遇，但需适配遥感影像的密集、多尺度和大尺寸特性。
method: 提出DinoSplat-OV框架，含文本感知拉普拉斯传播模块（融合文本语义与局部视觉相似性去噪）和高斯泼溅上采样模块（RGB引导各向异性聚合与测试时优化），并采用全局锚点滑窗策略。
result: 在UDD5、DOTA和LoveDA数据集上，DinoSplat-OV达到与现有训练免方法相当或更优的性能，验证了其有效性。
conclusion: DinoSplat-OV填补了DINO系列模型在训练免开放词汇分割中的空白，为遥感领域提供了一条无需训练的新路径。
---

## 摘要
遥感语义分割受限于昂贵的像素级标注，这推动了免训练开放词汇方法的发展。近期，DINOv3的发布带来了DINO.txt，它赋予独立的DINO骨干网络图像-文本对比学习能力，从而为开放词汇分割提供了可能。我们提出了DinoSplat-OV，一个免训练框架，将DINOv3适配到遥感领域，无需微调或额外预训练。针对遥感影像的密集分布、多尺度特性和大幅面特点，我们设计了两个核心模块。其文本感知拉普拉斯传播模块通过结合文本语义亲和力与局部视觉相似度来对块级预测进行去噪，在保留边界的同时提升区域一致性。其高斯溅射上采样模块通过RGB引导的各向异性聚合和测试时优化来重构像素级特征。全局锚点滑动窗口策略进一步支持大幅面影像。在UDD5、DOTA和LoveDA上的实验表明，该方法相对于现有免训练方法具有竞争力或更优的性能，有效填补了DINO系列模型在免训练开放词汇分割方面的空白，并为该方向的进一步发展提供了一条可行新路径。

## Abstract
Remote sensing semantic segmentation is hindered by costly pixel-level annotations, motivating training-free open-vocabulary methods. Recently, the recent release of DINOv3 brings DINO.txt, which equips the standalone DINO backbone with image-text contrastive learning and thus opens up the possibility of open-vocabulary segmentation. We propose DinoSplat-OV, a training-free framework that adapts DINOv3 to remote sensing without fine-tuning or additional pretraining. Targeting the dense distribution, multi-scale nature, and large size of remote sensing imagery, we design two core modules. Its Text-aware Laplacian Propagation module de-noises patch-level predictions by combining textual semantic affinities with local visual similarity, improving regional consistency while preserving boundaries. Its Gaussian Splatting Upsampling module reconstructs pixel-level features through RGB-guided anisotropic aggregation and test-time optimization. A global-anchor sliding-window strategy further supports large-scale imagery. Experiments on UDD5, DOTA, and LoveDA demonstrate competitive or superior performance over existing training-free methods, effectively filling the gap of DINO-series models in training-free open-vocabulary segmentation and providing a viable new path for further advances in this direction.

---

## 论文详细总结（自动生成）

## 一、论文的核心问题与整体含义

- **研究背景**：遥感影像语义分割在精准农业、灾害响应和环境监测中至关重要，但像素级标注极其昂贵；同时遥感影像具有目标密集、尺度多变、幅面巨大等特点，使天然图像预训练的视觉基础模型难以直接应用。
- **核心问题**：现有免训练开放词汇分割方法大多依赖CLIP的可修改内部模块（如自注意力、FFN等）进行推理时优化，难以迁移到其他视觉基础模型。DINOv3新发布的配套文本编码器DINO.txt首次让DINO骨干具备独立进行开放词汇分割的能力，但其采用Locked-image Tuning（LiT）范式，视觉骨干完全冻结，无法像CLIP那样修改内部结构，直接应用于遥感影像时存在严重噪声和边界模糊。
- **论文目标**：提出一个完全免训练、免微调、免额外预训练的遥感开放词汇语义分割框架**DinoSplat-OV**，将DINOv3/DINO.txt适配到遥感场景，填补DINO系列模型在免训练开放词汇分割中的空白。

## 二、方法论

- **总体思路**：DinoSplat-OV是一个纯推理期的优化管线，不更新任何骨干网络参数，通过四个顺序模块完成从文本嵌入到像素级预测的端到端推理：
  1. **同义词聚合（Synonym Aggregation）**：对每个类别收集多个同义文本描述，将它们的文本嵌入加权平均得到更稳定的类级嵌入，缓解DINO.txt跨模态对齐较弱的问题。
     - 公式：`T_c = Σ w_{c,i} t_{c,i} / Σ w_{c,i}`。
  2. **文本感知拉普拉斯传播（TLP）**：在低分辨率patch特征的对数几率（logits）上执行各向异性扩散。利用文本语义相关矩阵S（余弦相似度+温度缩放、对称化）和图像局部边缘权重构造图拉普拉斯算子，通过求解稀疏线性系统（式7）实现“同质区域平滑去噪、语义边界保留”的效果。
     - 关键变量：置信度γ(u)、语义自一致性α(u)、扩散门控系数λ(u)、对称化边权ω̂_{u,v}（结合图像梯度项和语义项）。
     - 求解方法：共轭梯度法（CG），无需训练。
     - 实现细节：先降采样到粗网格（如72×72）求解，再用GSUP恢复分辨率。
  3. **高斯溅射上采样（GSUP）**：将低分辨率特征图的每个像素视为一个2D高斯溅射基元，通过RGB引导的各向异性高斯核（空间项用马氏距离，颜色项用RGB差值）对高分辨率目标位置进行加权聚合，重建像素级特征。
     - 参数（尺度s_x, s_y、旋转角θ、颜色带宽σ_r）通过测试时优化（TTO）学习：以低分辨率RGB为输入、原始高分辨率RGB为监督，最小化L1重建损失，仅需10步SGD迭代。
     - 无需任何预训练上采样器，跨域泛化性好，额外开销约<3GB GPU内存、每图1.2秒。
  4. **全局锚点滑动窗口（Global-Anchor Window Attention）**：针对遥感影像超大尺寸，采用滑动窗口推理，并用CLS token作为全局锚点，计算每个窗口的全局感知权重，结合高斯窗口权重平滑消除拼接伪影。
- **设计动机**：由于DINOv3视觉骨干冻结，不能像CLIP那样修改内部结构，因此所有适配都在推理期以输入/特征处理层完成。

## 三、实验设计

- **数据集**：
  - **DOTA**（转为语义分割版本）、**LoveDA**（城乡混合农业场景）、**UDD5**（无人机/类自然影像）、**ISPRS Vaihingen**（无蓝波段城市场景）。
  - 额外在ImageNet子集上评估上采样方法性能（表2）。
- **基准与对比方法**：
  - 对比主流的免训练开放词汇方法：MaskCLIP、DINO.txt（基线）、ClearCLIP、LPOSS、SegEarth-OV。
  - 上采样对比：双线性、UPLiFT、AnyUp、SatUp（预训练上采样器）。
- **评估指标**：mIoU（平均交并比）为主。

## 四、资源与算力

- **论文未明确说明**所使用的GPU型号、数量、总训练时长或推理硬件配置。
- 仅提到GSUP模块的额外开销：约**<3 GB GPU内存**和**每图约1.2秒**延迟（单次推理），以及TTO只需10次SGD迭代。
- 因此无法全面评估其计算资源需求，只能确认框架本身为免训练设计，训练成本为零，推理开销可控。

## 五、实验数量与充分性

- **实验组数**：
  - 主对比实验：4个遥感数据集 × 5种方法（表1）。
  - 上采样方法单独对比：在ImageNet子集上和5种方法对比（表2）。
  - 消融实验：在UDD5上逐步添加滑动窗口、同义词聚合、TLP、GSUP，共4个增量配置（表3）。
  - 骨干泛化实验：将TLP和GSUP分别应用于DINO.txt和ClearCLIP，验证通用性（表4）。
- **充分性与客观性评估**：
  - **优点**：覆盖多个不同场景的遥感数据集，包含消融和跨骨干泛化测试；所有对比方法统一使用滑动窗口策略，保证公平性；GSUP的对比中也考虑了有无预训练上采样器，体现公平原则。
  - **不足**：
    - 主结果表1中部分数值（如SegEarth-OV在DOTA上22.3）来自各自论文，可能存在实现差异。
    - 消融只在UDD5单一数据集上进行，缺少在LoveDA、DOTA等更大尺度数据集上的消融验证，泛化证据略显单薄。
    - 缺少与其他SOTA（如SegEarth-OV3、PEARL）的对比，时间覆盖不够新。

## 六、主要结论与发现

- DinoSplat-OV作为首个基于DINOv3/DINO.txt的免训练遥感开放词汇分割框架，在四个遥感数据集上达到与现有SOTA（SegEarth-OV）相当或更优的性能（平均mIoU 37.5 vs. 36.3，且在DOTA上显著领先28.6 vs. 22.3）。
- TLP和GSUP两个核心模块分别贡献了+8.3%和+10.5%的mIoU提升（相对基线32.4%），证明了文本引导的拉普拉斯传播和高斯溅射式上采样能够有效弥补冻结DINO骨干在密预测上的不足。
- GSUP无需预训练，仅通过测试时优化即可达到与预训练上采样器相近的重建质量，且在跨域泛化上具有理论优势。
- 该框架可迁移到其他骨干（如ClearCLIP），验证了方法的通用性。

## 七、优点

- **创新性**：首次利用DINOv3+DINO.txt实现独立、免训练的开放词汇分割，突破了CLIP独占的局面。
- **设计针对性强**：针对遥感影像的密集分布、多尺度、大幅面三大痛点，分别设计了TLP、GSUP和全局锚点滑窗，问题与解法对应清晰。
- **完全免训练**：所有模块均为推理期优化，无需微调骨干、无需预训练上采样器，部署灵活、跨域适应性强。
- **理论规范**：TLP使用对称化图拉普拉斯和共轭梯度法，保证收敛性和数值稳定；GSUP借鉴3DGS原理并适配2D特征重建，数学表述完整。
- **实验设计较全面**：包含主实验、消融、上采样对比、骨干泛化实验；并提供特征重建可视化，验证定性效果。

## 八、不足与局限

- **实验覆盖有限**：消融仅在UDD5上执行，未在其他遥感数据集上验证模块贡献；缺少与更多最新SOTA（如SegEarth-OV3）的对比。
- **效率与性能权衡**：GSUP每图需1.2秒额外延迟，在处理超大幅遥感影像时可能成为瓶颈；表2显示其性能略低于预训练AnyUp/SatUp（42.9 vs 43.8/43.2），并非绝对最优。
- **文本侧依赖**：同义词聚合虽然缓解了文本对齐弱的问题，但同义词数量与选择策略未做深入分析，可能影响泛化。
- **超参数敏感**：TLP中的温度τ_S、梯度常数k_I、γ_min、平滑强度τ等，以及GSUP中K、迭代步数等，均需人工设定，论文未提供灵敏度分析。
- **应用限制**：目前仅在中等分辨率遥感数据集上验证，对真正的大幅面航空/卫星影像（gigapixel级）的端到端推理仍需进一步测试。
- **论文信息缺失**：未报告计算资源细节、运行环境、代码可用性等，可复现性有待加强。

（完）
