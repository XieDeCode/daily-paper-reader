---
title: "UniJEPA: A Unified Joint-Embedding Predictive Architecture for Task-Agnostic Visual World Modeling"
title_zh: UniJEPA：用于任务无关视觉世界建模的统一联合嵌入预测架构
authors: "An Lanji, Dawei Liu, Jin Li, Haoran Xu, Mei Chen, Yu Tian"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07409v1"
tags: ["query:world-models"]
score: 9.0
evidence: 统一图像与视频预测的视觉世界模型
tldr: 现有JEPA方法在图像与视频世界建模上目标割裂，各自使用独立编码器、预测器和防坍塌正则器，难以统一。UniJEPA提出单一共享潜在空间联合学习光度预测与时序预测，仅用下一嵌入预测损失和高斯正则器即可无EMA端到端训练。模型同时支持不变结构学习与等变动态学习，经动作条件后训练可零样本规划，在图像、视频和控制基准上匹敌或超越任务专用JEPA。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.07409v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 727, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1685, \"height\": 948}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 802, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 830, \"height\": 497}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 826, \"height\": 497}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 833, \"height\": 497}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1696, \"height\": 934}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.07409v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 391}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1479, \"height\": 441}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1072, \"height\": 461}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1006, \"height\": 352}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1322, \"height\": 315}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1776, \"height\": 396}]"
motivation: 现有JEPA在图像和视频级目标上方法碎片化，缺乏统一框架，阻碍单一模型同时建模图像级与视频级世界。
method: 提出统一JEPA，用共享潜在空间联合光度与时序预测，以下一嵌入预测损失加高斯正则器实现防坍塌训练，无需EMA或预训练编码器。
result: 在图像、视频和控制基准上匹配或超越任务特化JEPA，规划速度比生成式世界模型快数十倍且精度相当。
conclusion: UniJEPA证明统一图像与视频世界建模可行，为任务无关视觉世界模型提供简洁高效的通用方案。
---

## 摘要
联合嵌入预测架构（JEPA）已成为在紧凑潜在空间中进行自监督世界模型学习的原则性框架，但现有方法较为分散：有些在潜在空间中预测单幅图像的掩码部分（I-JEPA），有些学习预测全局光度变换（图像世界模型），而视频级JEPA则预测未来时间状态，并在行动条件规划中进行后训练（V-JEPA~2、DINO-World、DINO-WM）。这些目标被视为不同的方案，配备各自的编码器、预测器和抗坍塌正则化器，阻碍了单一模型统一图像级和视频级世界建模。我们提出UniJEPA，一种统一的JEPA，在共享潜在空间中联合学习光度预测（图像级变换）和时间预测（视频级下一状态动态）。一个由下一嵌入预测损失和高斯正则化器组成的单一端到端目标，可生成可证明抗坍塌的编码器-预测器对，且无需EMA、停止梯度或预训练编码器即可从原始像素训练。我们表明，同一潜在空间支持可控抽象：光度预测学习不变结构，而时间预测学习等变动态。在离线轨迹上进行行动条件后训练后，UniJEPA通过将目标特征视为预测目标来实现零样本规划。在图像、视频和控制基准测试中，UniJEPA在仅需单一损失超参数的情况下，达到或超越任务特定JEPA，并在相当精度下，规划速度比生成式世界模型快数十倍。

## Abstract
Joint-Embedding Predictive Architectures (JEPAs) have emerged as a principled framework for self-supervised learning of world models in compact latent spaces, yet existing methods are fragmented: some predict masked parts of a single image in latent space (I-JEPA), others learn to predict global photometric transformations (Image World Models), while video-scale JEPAs predict future temporal states and are post-trained for action-conditioned planning (V-JEPA~2, DINO-World, DINO-WM). These objectives are treated as distinct recipes with separate encoders, predictors, and anti-collapse regularizers, hindering a single model from unifying image-level and video-level world modeling. We present UniJEPA, a unified JEPA that jointly learns photometric prediction (image-level transformations) and temporal prediction (video-level next-state dynamics) in one shared latent space. A single end-to-end objective, composed of a next-embedding prediction loss and a Gaussian regularizer, yields a provably anti-collapse encoder-predictor pair trainable from raw pixels without EMA, stop-gradient, or pre-trained encoders. We show that the same latent space supports controllable abstraction: photometric prediction learns invariant structure while temporal prediction learns equivariant dynamics. After action-conditioned post-training on offline trajectories, UniJEPA enables zero-shot planning by treating goal features as prediction targets. On image, video, and control benchmarks, UniJEPA matches or surpasses task-specific JEPAs while requiring a single loss hyperparameter, and plans up to tens of times faster than generative world models at comparable accuracy.

---

## 论文详细总结（自动生成）

# UniJEPA：用于任务无关视觉世界建模的统一联合嵌入预测架构——详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：联合嵌入预测架构（JEPA）被视为学习世界模型的有前景的自监督框架，其核心思想是让编码器将观测映射到紧凑的潜在空间，再由预测器预测未来或掩码内容的潜在表示，而不是重建原始像素。
- **核心问题**：现有 JEPA 家族严重碎片化，具体表现为：
  - **图像级 JEPA**（如 I-JEPA）在潜在空间中预测单幅图像的掩码块；
  - **图像世界模型 IWM** 将预测扩展到全局光度变换；
  - **视频级 JEPA**（如 V-JEPA 2、DINO-World/DINO-WM）预测未来时间状态，并需要单独的后训练以支持行动条件规划。
  - 这些方法各自使用独立的编码器、预测器和防坍塌正则化器，导致光度世界模型与时序世界模型处于**互不兼容的潜在空间**，无法统一或组合。
- **本文主张**：图像级和视频级预测是世界模型的两种视角，单一潜在空间应能同时支持两者。统一的好处在于更强的可迁移性、更高的样本效率，以及从鲁棒表示学习到具身规划的更广下游应用范围。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 2.1 核心思想
- 提出 **UniJEPA**，在**一个共享潜在空间**中联合优化光度预测（图像级变换）与时序预测（视频级下一状态动态）。
- 使用**单一端到端目标**：下一嵌入预测损失 + 高斯正则器，无需 EMA、停止梯度或预训练编码器即可训练，且具有**可证明的抗坍塌保证**。
- 通过调节两个损失的权重，可**控制表示的抽象程度**：光度预测学习不变结构，时序预测学习等变动态。

### 2.2 关键技术细节

- **统一目标**：
  - 光度预测损失（公式 1）：给定编码表示 \(z=f_\theta(x)\) 和光度变换参数 \(\tau\)，预测目标 \(z' = f_\theta(\tau(x))\)：
    \[
    L_{\text{photo}} = \mathbb{E}_{x,\tau} \left[ \| g_\psi(f_\theta(x), \tau) - f_\theta(\tau(x)) \|_2^2 \right]
    \]
  - 时序预测损失（公式 2）：给定当前状态 \(x_t\) 和动作 \(a_t\)，预测下一状态 \(x_{t+1}\) 的潜在表示：
    \[
    L_{\text{temp}} = \mathbb{E}_{(x_t,a_t,x_{t+1})} \left[ \| g_\psi(f_\theta(x_t), a_t) - f_\theta(x_{t+1}) \|_2^2 \right]
    \]
  - 两者共享同一预测器 \(g_\psi\)，可视为单一潜在预测任务的两个实例。

- **防坍塌正则化（公式 3）** ：
  - 通过对潜在向量在大量随机单位方向上的投影施加卡方（\(\chi^2_1\)）偏差惩罚，使潜在分布接近球面高斯：
    \[
    R(\theta) = \mathbb{E}_{x,u} \left[ \chi^2_1(u^\top z)^2 \right]
    \]
  - 总目标（公式 4）为：
    \[
    L_{\text{UniJEPA}} = L_{\text{photo}} + L_{\text{temp}} + \alpha R
    \]
    其中 \(\alpha > 0\) 是唯一标量超参数。

- **理论保证（Theorem 3.1）** ：若正则器被最小化到 \(R \le \epsilon\)，则编码器不能为常数，潜在分布协方差非退化且最小特征值 \(\lambda_{\min} \ge 1 - O(\epsilon)\)。这证明了单一正则器足以防止坍塌。

- **行动条件规划**：
  - 预训练后冻结编码器，在离线轨迹上对预测器进行后训练，得到行动条件模型 \(g_\psi(z,a)\)。
  - 规划通过视觉目标达成形式化（公式 5）：给定当前观测 \(z_0\) 和目标 \(z_g\)，最小化预测轨迹与目标特征的均方距离，采用基于采样的模型预测控制（MPC）。
  - 由于预测在潜在空间中进行，每次 rollout 只需一次预测器前向传播，计算成本远低于像素级生成模型。

### 2.3 算法流程（Algorithm 1 & 2）
1. **预训练**：采样光度变换样本 \((x,\tau)\) 和时序样本 \((x_t,a_t,x_{t+1})\)，按公式 (4) 更新编码器 \(\theta\) 和预测器 \(\psi\)。
2. **后训练**：冻结 \(\theta\)，仅用公式 (2) 在离线轨迹上微调 \(\psi\)。
3. **规划**：对当前状态和目标，求解公式 (5) 得到动作序列。

## 3. 实验设计：数据集 / 场景、基准（benchmark）与对比方法

### 3.1 数据集与基准
- **图像级**：ImageNet-1k 线性探测与微调。
- **视频理解**：Something-Something-v2（运动理解）、Epic-Kitchens-100（动作预期），以及 Perception Test 和 TempCompass（诊断性时序推理）。
- **规划/控制**：离线控制套件，包括迷宫导航、推操作、多粒子控制等目标达成任务，遵循离线世界模型协议。

### 3.2 对比方法
- **任务特定 JEPA**：I-JEPA、IWM、V-JEPA、V-JEPA 2、DINO-WM、LeWorldModel。
- **对比学习**：SimCLR、DINOv2。
- **生成式世界模型**：Sora（Brooks et al.）、Stable Video Diffusion（Blattmann et al.）等。

### 3.3 指标
- ImageNet top-1 准确率、动作预期 recall@5、规划成功率、规划速度相对倍数、损失超参数数量。

## 4. 资源与算力

- 论文明确提到：**15M 参数的 ViT-Small 变体在单张 GPU 上训练只需数小时**，与 LeWorldModel 一致。
- 大模型使用 ViT-Large，但文中未具体说明 GPU 型号、数量、训练总时长或能耗。
- 规划阶段因潜在空间预测，速度比像素级生成模型快 44 倍，但具体硬件细节未展开。

## 5. 实验数量与充分性评估

- **实验组数较多且覆盖面较广**：
  - 主结果表（Table 1）涵盖图像、视频、动作预期、规划成功率和速度等多个维度。
  - 两张消融表（Table 2、Table 3）分别对损失组件、正则器权重、编码器规模，以及规划配置（MPC 步数、候选 rollout 数量、后训练数据量）进行了系统分析。
  - Table 4 提供跨数据集/设置更细粒度的结果（如不同 ViT 规模、不同动作预期长度）。
  - 另有收敛性分析、成本-精度热图、定性结果和敏感性分析。
- **充分性评价**：
  - **优点**：实验设计较全面，既对比了任务专用 JEPA，也对比了对比学习和生成模型；消融实验直接验证了核心贡献（防坍塌定理、双损失必要性、规划后训练必要性）。
  - **不足**：论文未说明多次运行的方差/置信区间，也未提及统计显著性检验；部分结果（如 ViT-Large 的 ImageNet 微调、DINO-WM 的行缺失）在表中不完整，对比公平性略有欠缺；定性结果图未在文本中完整呈现。

## 6. 论文的主要结论与发现

- **统一可行**：单一共享潜在空间可以同时支持光度预测和时序预测，且端到端训练稳定，无需 EMA、停止梯度或预训练编码器。
- **性能超越或持平**：在 ImageNet 线性探测上达到 74.9%，超过 IWM（73.5%）；在 Something-Something-v2 上达到 78.1%，超过 V-JEPA 2（77.3%）；规划成功率 75.8%，优于 DINO-WM 和 LeWorldModel。
- **效率突出**：规划速度比像素级生成模型快 44 倍，且只需 1 个损失超参数。
- **可控抽象**：光度损失增强不变性（改善 ImageNet 和时序推理），时序损失增强等变性（决定规划能力），二者权重可调节表示抽象程度。
- **防坍塌定理有效**：去掉正则器会导致潜在表示坍缩到秩 3，验证了理论分析。

## 7. 优点

- **简洁统一的目标**：将原本割裂的图像级和视频级世界建模统一为一个损失，只需一个超参数，大大简化了训练流程。
- **可证明的抗坍塌保证**：首次在统一 JEPA 中提供理论证明，说明单一高斯正则器即可替代 EMA、stop-gradient 等启发式手段。
- **可控的抽象谱**：从理论和实验上展示了不变性-等变性光谱，为实际应用提供了灵活调节表示抽象度的旋钮。
- **高效的零样本规划**：在共享潜在空间中实现 MPC，速度提升数十倍，且不依赖专家演示或奖励信号。
- **端到端学习编码器**：不像 DINO-WM 依赖冻结的预训练视觉编码器，UniJEPA 从原始像素联合学习编码器与预测器，更具通用性。

## 8. 不足与局限

- **共享预测器的表达能力**：光度和时序任务共用单一预测器，对于长时程、多模态或复杂动作条件任务，可能需要更丰富的条件机制。
- **高斯正则器的潜在容量限制**：虽然能防坍塌，但在极大模型规模下可能限制表示的容量或灵活性。
- **缺乏语言与更通用智能体能力**：当前模型不消费语言指令，无法与视觉-语言-动作通用智能体（如 RT-2、OpenVLA）直接比较或集成。
- **实验对比的公平性存疑**：部分基线（如 V-JEPA-2 的 ImageNet 结果、LeWorldModel 在多个基准上的缺失）未完整报告；规划和视频实验的硬件、训练预算未对齐，可能引入不公平比较。
- **评估维度局限**：未报告多次重复的方差/置信区间，鲁棒性证据不足；规划任务均为较简单的 goal-reaching 类控制任务，未涉及真实机器人或长时程复杂操作。
- **潜在部署风险**：世界模型的动态预测不完美，在安全关键任务中需要额外验证，但论文对此仅作简要影响声明，没有给出具体的可靠性量化手段。

（完）
