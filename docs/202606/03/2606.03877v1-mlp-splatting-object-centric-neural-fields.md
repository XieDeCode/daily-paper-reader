---
title: "MLP Splatting: Object-Centric Neural Fields"
title_zh: MLP Splatting：以物体为中心的神经场
authors: "Shinjeong Kim, Yuzhou Cheng, Xin Kong, Paul H. J. Kelly, Andrew J. Davison"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03877v1"
tags: ["query:world-models"]
score: 9.0
evidence: 以对象为中心的MLP原语神经辐射场
tldr: 3D表示方法如3D高斯泼溅和NeRF虽能实现逼真新视角合成，但缺乏场景分解能力，需要额外分割或分组才能进行目标级操作。MLP-Splatting通过将每个基元建模为独立紧凑MLP，具有局部空间支持，预测辐射和透明度，并采用稀疏体素合成渲染。仅使用RGB监督即可获得对应物体或部件的基元，支持交互式目标级编辑，内存使用降低至1/15，渲染速度提升3倍。结合语义特征蒸馏，还能实现开放词汇场景交互和开放集实例分割，显著优于现有语义3DGS方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03877v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1250, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03877v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1242, \"height\": 439, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03877v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1267, \"height\": 1165, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03877v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1253, \"height\": 940, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03877v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1266, \"height\": 636, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03877v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1267, \"height\": 251, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03877v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1276, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03877v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1275, \"height\": 304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03877v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1027, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03877v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1150, \"height\": 183, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03877v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1151, \"height\": 222, \"label\": \"Table\"}]"
motivation: 现有3D表示难以分解场景为少数基元，需要额外分割或分组才能进行目标级操作。
method: 将每个基元建模为独立紧凑MLP，具有局部空间支持，预测辐射和透明度，并通过稀疏体素合成渲染。
result: 仅RGB监督即可获得对应物体或部件的基元，内存使用降低至1/15，渲染速度提升3倍。
conclusion: MLP-Splatting实现目标级场景分解与新视图合成，结合语义蒸馏可进行开放词汇交互。
---

## 摘要
3D表示对于场景渲染、理解和交互至关重要。最近的方法，如3D高斯泼溅和神经辐射场，在照片级真实感新视角合成方面取得了令人瞩目的成果，但缺乏将场景元素轻松分解为少量基元的能力，需要额外的分割或分组才能进行物体级别的操作。我们提出了MLP-Splatting，一种通过少量富有表现力的光场基元实现场景分解，同时提供照片级真实感新视角合成的方法。MLP-Splatting将每个基元建模为一个独立的紧凑型MLP，具有局部空间支持，可预测辐射度和不透明度。与低层级的高斯基元或单个全局辐射场相比，我们的神经基元在保持空间局部性的同时提供了更强的表达能力。渲染通过射线-基元交互的高效稀疏体积合成来执行。我们的基元仅使用RGB监督进行训练，产生的基元表示通常对应于物体或物体部分的局部场景区域，通过选择少量基元即可实现无需分割掩码的交互式物体级编辑。我们的方法辅以可选的语义特征蒸馏，实现了开放词汇场景交互和开放集实例分割。与最先进的方法相比，我们的实验表明，与语义3DGS方法相比，我们实现了显著更低的内存使用（1/15倍）和更快的渲染速度（3倍）。项目页面：https://shinjeongkim.com/mlp-splatting

## Abstract
3D representations are fundamental to scene rendering, understanding, and interaction. Recent approaches, such as 3D Gaussian Splatting and Neural Radiance Fields, achieve impressive photorealistic novel-view synthesis, but lack the ability to easily decompose scene elements into a few primitives, requiring additional segmentation or grouping for object-level manipulation. We present MLP-Splatting, a method that enables scene decomposition via a few expressive light-field primitives while providing photorealistic novel-view synthesis.   MLP-Splatting models each primitive as an independent compact MLP with localized spatial support that predicts radiance and opacity. In contrast to low-level Gaussian primitives or a single global radiance field, our neural primitives provide greater expressive capacity while remaining spatially localized. Rendering is performed through efficient sparse volumetric compositing over ray-primitive interactions.   Our primitives are supervised using RGB supervision alone, which yields primitives that represent local scene regions often corresponding to objects or object parts, enabling interactive object-level editing without segmentation masks by selecting a handful of primitives. Our method, augmented with optional semantic feature distillation, enables open-vocabulary scene interaction and open-set instant segmentation. Compared to state-of-the-art methods, we achieve substantially lower memory usage (1/15$\times$) and faster rendering (3$\times$), as we show in our experiments compared to semantic 3DGS methods. Project Page: https://shinjeongkim.com/mlp-splatting

---

## 论文详细总结（自动生成）

# 论文《MLP Splatting: Object-Centric Neural Fields》详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的3D场景表示方法（如3D高斯泼溅3DGS、神经辐射场NeRF）虽然能够实现高质量的逼真新视角合成，但缺乏内在的场景分解能力。它们的基本单元是低层级几何或辐射度量元素（高斯椭球、体素等），无法直接支持物体級别的操作（如编辑、删除、变换），需要额外的分割或分组步骤。
- **整体意义**：论文提出一种以物体为中心的神经场表示**MLP-Splatting**，将每个基元建模为独立的紧凑MLP（多层感知机），使得基元自然地对应场景中的物体或物体部分。这实现了：
  - 无需分割掩码即可进行交互式物体级编辑；
  - 结合语义蒸馏可实现开放词汇场景交互和开放集实例分割；
  - 大幅降低内存占用（1/15）并提升渲染速度（3×）。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：场景由多个独立的**MLP基元**组成，每个基元是一个紧凑的神经辐射场，在自己的局部空间内预测颜色和密度。所有基元通过稀疏体素合成渲染，形成完整的场景场。
- **关键技术细节**：
  - **基元表示**：每个基元 \(i\) 参数化为中心 \(\mu_i\in\mathbb{R}^3\)、尺度 \(s_i\in\mathbb{R}^3\)、旋转四元数 \(q_i\in\mathbb{S}^3\)，定义各向异性高斯支持区域。关联一个独立的MLP \(f_i(\gamma(x_i))\)，输入为6维向量 \(x_i=[d;\delta_i]\)（射线方向 + 从射线与基元软接触点到中心的残差），输出RGB颜色 \(c_i\) 和密度 \(\sigma_i\)。
  - **位置编码**：对输入应用多分辨率位置编码，增强高频表达能力。
  - **交互与排序**：对于每条射线，计算与每个基元的软接触点 \(t_i^\star\)（闭式解），按深度排序。
  - **渲染**：采用基于瓦片（tile）的CUDA渲染器：将图像划分为 \(8\times8\) 瓦片，对每个瓦片基于中心虚拟射线排序基元；在CUDA线程块中协作加载MLP权重、前向传播、执行alpha混合，支持提前终止。
  - **优化**：仅使用RGB监督（L1+SSIM损失）训练基元；可选特征蒸馏——为每个基元附加语义特征向量（通过方向条件4个槽），用与颜色相同的体积加权方式渲染特征图，并与2D预训练模型（如LSeg-CLIP、SAM）的输出对齐。
  - **初始化**：通过COLMAP点云的八叉树自适应划分初始化基元的位置和尺度。
  - **密度控制**：不显式设计密度控制机制，基元在优化中自然竞争（部分基元缩小消失）。
- **公式与算法流程**（文字说明）：
  1. 输入：多视角RGB图像（+可选2D语义特征图）。
  2. 初始化：基于COLMAP点云八叉树生成初始基元（位置、尺度、随机旋转、随机MLP参数）。
  3. 迭代训练：随机采样图像瓦片；对每个瓦片执行AABB-视锥剔除获取候选基元；用瓦片中心射线计算深度并进行块级基数排序；按顺序加载基元MLP权重到共享内存，所有像素协作前向传播得到颜色和密度；alpha混合得到渲染颜色 \(C\)（以及可选特征 \(F\)）；计算损失 \(L = L_{rgb} + \lambda L_f\)（仅RGB时\(\lambda=0\)）；反向传播更新基元参数。
  4. 推理：类似渲染过程，支持任意视角新视图合成。

## 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：Replica（室内合成场景）和ScanNet（真实室内场景），采样方式遵循Feature-3DGS。
- **Benchmark**：光度指标（PSNR、SSIM、LPIPS）和语义指标（mIoU、像素准确率）。
- **对比方法**：
  - NeRF-DFF：将CLIP-LSeg特征蒸馏到NeRF风格特征场。
  - Feature-3DGS：将相同教师特征蒸馏到3D高斯基元（带高效光栅化）。
  - 论文也展示了与原始3DGS在物体参数数量上的对比（表2）。
- **消融实验**：
  - 语义共训练对光度性能的影响（A.1）。
  - 特征槽数量（1/4/8）对内存、速度、mIoU的影响（A.2）。
  - 基元数量（通过八叉树参数 \(m\) 控制）的影响（A.3）。
  - 瓦片大小（逐像素排序 vs \(8\times8\) 瓦片）对精度和速度的影响（A.4）。
- **定性演示**：物体缩放、删除、颜色编辑、刚体变换（图3）；语言引导分割（图4）；基于SAM提示的实例分割（图5）。

## 4. 资源与算力

- **训练**：单张NVIDIA RTX 6000 Ada GPU；60,000次迭代。论文未明确给出单次训练时长，但由收敛速度可知训练时间合理。
- **推理评估**：NVIDIA RTX 4090 GPU，标准前向渲染壁钟时间（FPS）。
- **内存对比**：MLP-Splatting仅需32 MB（Replica），而Feature-3DGS需要629.7 MB。
- **训练超参数**：优化器Adam，学习率 \(10^{-2}\)（log-scales）、\(10^{-4}\)（位置）、\(10^{-3}\)（其他参数）；无显式密度控制。

## 5. 实验数量与充分性

- **实验数量**：主要包括两个数据集（Replica、ScanNet）的定量对比实验（表1、表3）、物体参数统计（表2）、四个消融实验（表4-6及附录A）、多组定性编辑和分割演示。整体实验覆盖了光度性能、语义性能、效率、可编辑性、理论分析。
- **充分性与公平性**：
  - 对比方法为领域内标准基线（NeRF-DFF、Feature-3DGS），使用了相同的数据划分和评估协议。
  - 消融实验覆盖了关键设计选择（共训练、特征槽数、基元数、瓦片大小），但未消融MLP架构深度/宽度（默认固定）。
  - 定性演示有说服力。
  - 理论分析（Equal-Quality Memory Law）为方法的效率优势提供了严格证明。
  - 不足之处：未与更多近期方法（如LangSplat、Gaussian Grouping）进行直接语义比较；仅在室内场景评估，缺乏室外/大规模场景泛化测试；未提供训练时间的具体数值。

## 6. 论文的主要结论与发现

- **主要结论**：
  1. MLP-Splatting能够以物体/部分物体为基本单元构建场景表示，在RGB监督下自然涌现物体级结构，无需分割掩码即可实现交互式编辑。
  2. 该方法在光度质量上达到或优于Feature-3DGS，同时内存减少约1/15，渲染速度提升约3倍。
  3. 结合特征蒸馏，语义分割性能与Feature-3DGS相当，但效率显著更优。
  4. 理论分析（Equal-Quality Memory Law）表明，对于分段光滑场景，MLP-Splatting的参数复杂度为 \(\Theta(\varepsilon^{-3/s})\)，优于高斯溅射的 \(\Omega(\varepsilon^{-2})\)，当光滑阶数 \(s\ge2\) 时优势明显。

## 7. 优点

- **创新性**：提出以独立MLP作为场景基本单元，兼具神经场的表达能力和显式基元的空间局部性，是一种新颖的物体为中心表示范式。
- **无需显式分割**：仅靠RGB监督即可产生物体级分解，降低编辑和交互门槛。
- **高效性**：极低的基元数量（通常小于3DGS的1%）带来显著的内存和速度优势；基于瓦片的CUDA渲染器设计合理。
- **语义扩展性**：可无缝集成2D基础模型（CLIP、SAM），支持开放词汇查询和实例分割。
- **理论支撑**：提出并证明Equal-Quality Memory Law，从逼近论角度解释效率优势。

## 8. 不足与局限

- **无密度控制**：不显式设计生长/剪枝机制，基元在复杂外观区域可能振荡，简单区域基元过少或死亡，影响重建质量或效率。
- **初始化依赖**：需要COLMAP点云作为初始化输入，对于无纹理或动态场景不鲁棒。
- **语义蒸馏代价**：依赖2D预训练模型提取特征，可能引入教师模型的偏差；且需要多视角一致性。
- **实验局限**：仅在Replica和ScanNet室内静态场景上评估；未进行大规模场景、室外场景、动态场景的测试；未与部分近期高级方法（如LangSplat）直接比较。
- **LPIPS稍差**：在Replica和ScanNet上LPIPS略高于Feature-3DGS（表1），说明感知相似度有提升空间。
- **可编辑性**：物体级编辑需要手动选择基元组（尽管数量少），尚未实现自动物体识别；刚性变换（SE(3)）实现简单，但颜色编辑通过调整MLP偏置实现，精细纹理编辑需额外优化。

（完）
