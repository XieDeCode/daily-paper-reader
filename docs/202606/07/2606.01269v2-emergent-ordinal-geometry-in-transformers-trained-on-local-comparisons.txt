Title: Emergent Ordinal Geometry in Transformers Trained on Local Comparisons

URL Source: https://arxiv.org/pdf/2606.01269v2

Published Time: Wed, 03 Jun 2026 00:54:28 GMT

Number of Pages: 11

Markdown Content:
# Emergent Ordinal Geometry in Transformers Trained on Local Comparisons 

Nishit Singh ∗

Birla Institute of Technology and Science, Pilani India 

f20221317@pilani.bits-pilani.ac.in 

## Abstract 

Transitive inference is the challenge of inferring that A < C from knowing only adjacent relations (A < B, B < C). It is solved by humans and animals not through logical chaining but via an analogue mental number line, whose signature is the symbolic distance effect: distant comparisons are easier than nearby ones. We ask whether Transformers acquire the same primitive, training small models exclusively on adjacent comparisons from a hidden total order and evaluating generalization to unseen distant pairs. We find that out-of-distribution generalization emerges alongside a striking geometric reorganization: entity embeddings collapse onto a one-dimensional manifold whose principal axis recovers the hidden rank order with near-perfect fidelity, and this structure is sensitive to optimization in ways that produce grokking-like transient dynamics. Critically, even when accuracy is at ceiling, decision confidence and geometric separation both scale monotonically with rank distance, directly mirroring the symbolic distance effect observed across decades of behavioural experiments on humans, primates, and rodents. We further show the same rank-aligned geometry in a pretrained large language model, where it tracks the topology of each ordinal relation: linear for sizes and digits, cyclic for months. These results ground a 50-year-old behavioural regularity in the geometry of learned representations, offering a mechanistic account of transitive inference that bridges cognitive science and modern neural networks. 

## 1 Introduction 

Knowing that Alice is taller than Bob and Bob taller than Carol, one infers that Alice is taller than Carol without direct comparison. This capacity, transitive inference (TI), is phylogenetically widespread and documented in children (Bryant and Trabasso [1971]), primates (Mcgonigle and Chalmers [1977]), rodents (Davis [1992]), and birds (Steirn et al. [1995]) and, crucially, is not solved by formal logic. Behavioral work instead supports an analog mental number line: items are arranged along a continuous internal ordering from which comparisons are read off spatially (Riley [1976]). The signature of this account is the symbolic distance effect: distant comparisons are faster and more accurate than adjacent ones (Bryant and Trabasso [1971]), the opposite of what step-by-step chaining predicts, and exactly what one expects if order is encoded as position on a line. Transformers are under no explicit pressure to represent order this way, yet abstract relational structure is known to emerge in their representations as a byproduct of training (Minegishi et al. [2026], Mikolov et al. [2013]), sometimes abruptly and late, in grokking-like transitions (Power et al. [2022]). We ask a question at the intersection of cognitive science and interpretability: when a Transformer learns to order items from only local comparisons, does it discover the same primitive, amental number line , and exhibit the same behavioral signatures as biological cognition? 

> ∗

Personal webpage Preprint. 

> arXiv:2606.01269v2 [cs.AI] 2 Jun 2026

Mirroring the original Bryant and Trabasso [1971] paradigm, in which subjects learn adjacent comparisons and are tested on inferred distant pairs, we train small Transformers only on adjacent relations from a hidden total order and evaluate on held-out distant pairs. We find that out-of-distribution generalization coincides with a geometric reorganization: entity embeddings collapse onto a one-dimensional manifold whose principal axis recovers the hidden rank order. The resemblance is behavioral as well as structural, even at accuracy ceiling, decision confidence and geometric separation scale with rank distance, a computational analogue of the symbolic distance effect. Our contributions are: (i) a minimum-norm account predicting the geometry, including end-anchoring; 

(ii) A synthetic task, modeled on the classic TI paradigm, isolating ordinal generalization from local comparisons. (iii) Evidence that Transformers spontaneously form a 1D ordinal manifold and reproduce the symbolic distance effect. (iv) evidence that the same geometry appears in a pretrained 1.5B LLM, tracking the topology of each relation. 

## 2 Background 

Transitive inference and the distance effect: Since Bryant and Trabasso [1971], TI has been studied as ordinal rather than logical reasoning; the symbolic distance and end-anchor effects are its canonical behavioral signatures across species (Steirn et al. [1995]). 

Emergent structure in Transformers: Generalizing solutions can appear abruptly after memoriza-tion, a phenomenon termed grokking (Power et al. [2022]), driven by weight decay and trackable through internal progress measures as discussed in texts like Nanda et al. [2023]; Minegishi et al. [2026] formalize relational reasoning as emergent representational geometry. 

Linear representations: Semantic and relational structure is often encoded linearly in embedding space (Park et al. [2025], Elhage et al. [2022]), motivating our use of principal-component analysis as a probe. 

## 3 Theoretical Framework 

The rank-ordered one-dimensional manifold is not an accident of this task. It is the configuration that 

L2 regularization must prefer (Neyshabur et al. [2015]). We make this precise under a standard idealization: the comparison is read from a linear functional of the entity embeddings. We then show that the predicted geometry matches our empirical findings, including a characteristic departure from uniformity that turns out to be a second cognitive signature. 

Setup 

Let the relation be decided by a fixed readout direction w ∈ Rd with ∥w∥ = 1 , through the scalar 

score 

si = ⟨w, h ei ⟩.

The model predicts ei ≺ ej whenever sj − si > 0. This score is exactly the quantity our PC1 projection πi estimates empirically. Training supervises only adjacent pairs. We encode this as a margin constraint with target margin 

γ > 0:

si+1 − si ≥ γ, 0 ≤ i < N − 1. (1) Weight decay then selects, among all feasible embeddings, the one of minimum norm: 

min   

> {hei}
> N−1

X

> i=0

∥hei ∥2 subject to (1). (2) 

The regularized solution is the rank line Proposition 1. Every minimizer of (2) satisfies: 

2(i) Collinearity: hei = si w for all i; every off-axis component vanishes. 

(ii) Monotonicity: s0 < s 1 < · · · < s N −1, so PC1 recovers the hidden order ( rrank = 1 ). 

(iii) Equal spacing 2: Every constraint is active, si+1 − si = γ, giving si = γ (i − N −12 ).Proof. Step 1 (collinearity): Split each embedding into its component along w and orthogonal to it, 

hei = si w + h⊥ 

> ei

, ⟨w, h ⊥ 

> ei

⟩ = 0 ,

so that ∥hei ∥2 = s2 

> i

+ ∥h⊥ 

> ei

∥2. The constraints (1) depend only on the scores si, never on the orthogonal parts. Setting every h⊥ 

> ei

= 0 therefore preserves feasibility while strictly lowering the objective unless it is already zero. Hence hei = siw at any optimum. The problem collapses to a convex quadratic program in the scalars alone, 

min 

> {si}

X

> i

s2 

> i

s.t. si+1 − si ≥ γ, 

which has a unique minimizer. 

Step 2 (equal spacing): Suppose for contradiction that some gap is slack: sk+1 − sk > γ for some 

0 ≤ k < N − 1. Consider shifting the upper block {sk+1 , . . . , s N −1} down by ϵ > 0 and the lower block {s0, . . . , s k} up by δ > 0, where 

(N − 1 − k) ϵ = ( k + 1) δ, 

so the total shift is zero. Every gap within each block is unchanged, the slack gap at k widens, and all other tight gaps remain at γ, so the move is feasible. The change in the objective is 

∆ = −2ϵ X

> m>k

sm + 2 δ X

> m≤k

sm + O(ϵ2).

Substituting δ = (N −1−k)(k+1) ϵ and taking ϵ → 0+, the sign of ∆ is determined by 

(k + 1) X

> m>k

sm − (N − 1 − k) X

> m≤k

sm.

If this quantity is positive, the original shift decreases the objective; if negative, reversing the shift (moving the upper block up and lower block down) decreases it. It is zero only when 

1

N − 1 − k

X

> m>k

sm = 1

k + 1 

X

> m≤k

sm,

i.e. the two block means are equal, which combined with sk+1 > s k + γ > s k is impossible for 

γ > 0. In either case a feasible descent direction exists, contradicting optimality. Every gap is therefore tight: si+1 − si = γ for all i.

Step 3 (monotonicity and centering): Equal gaps give si = s0 + γi , which is increasing in i.Minimizing P

> i

(s0 + γi )2 over the offset s0 yields si = γ (i − N −12 ).

What the proposition explains 

Proposition 1 recovers our central findings from first principles. Off-axis structure costs norm without affecting the fit, so regularization collapses the embeddings onto a single axis (claim (i)) and that axis is the rank axis (claim (ii)). This is also why weight decay is necessary : with λ = 0 there is no pressure toward the line, matching our observation that unregularized runs never organize. Claims (i) and (ii) appear directly in Figure 1, where the peak-checkpoint embeddings form a monotone curve with rrank = 0 .96 .

> 2Under uniform constraint multiplicity.

3End-anchoring: a predicted break from uniform spacing 

The equal-spacing claim (iii) assumes every entity faces the same number of constraints. It does not. Interior entities ei (with 0 < i < N − 1) appear in two adjacent constraints, with ei−1 and ei+1 , and are pulled toward the centre from both sides. The endpoints e0 and eN −1 appear in only one , and can drift outward at lower marginal norm cost. The minimum-norm solution therefore stretches at the ends and compresses in the middle . This is exactly what Figure 1 shows: interior ranks bunch together while e0 and eN −1 extend past the linear trend, and the geometric separation |πi − πj | accelerates at the largest distances (Figure 2, (b)). This same asymmetry is the geometric form of the end-anchor effect (Holyoak and Patterson [1981]), a second canonical signature of transitive inference in humans and animals, in which judgements involving the terminal items are made faster and more reliably than those confined to the interior. 

Scope 

The proposition characterizes the regularized optimum under a linear readout. Two caveats follow. First, it does not describe the optimization trajectory : the transient, grokking-like dynamics of Figure 3 are a property of that trajectory and lie outside this account. Second, the attention and MLP sub-layers of the trained model are abstracted into the single fixed readout w. We therefore regard the argument as an explanation of which solution is preferred, empirically corroborated by the recovered geometry, and leave a trajectory-level theory, and its possible link to description-length accounts of weight decay to future work. 

## 4 Task and Methods 

Setup: Let E = {e0, . . . , e N −1} carry a hidden total order e0 ≺ e1 ≺ · · · ≺ eN −1. Each entity is an atomic token and the order is never given explicitly. A comparison is a triple (ei, ?, e j ) whose target is the relation 

r(ei, e j ) = 

( ≺; i < j, 

≻; i > j. (3) 

Train/OOD split: The model is trained only on adjacent comparisons, in both directions, 

Dtrain = (ei, e i+1 ), (ei+1 , e i) : 0 ≤ i < N − 1 , (4) and evaluated on the disjoint held-out set of all distant pairs, 

DOOD = (ei, e j ) : |i − j| > 1 , Dtrain ∩ D OOD = ∅ (5) Since no distant pair is ever observed, success on DOOD requires reconstructing the global order ≺

from local comparisons alone, the computational form of the Bryant-Trabasso paradigm. 

Model: We use a single-layer Transformer fθ with entity embeddings {he ∈ Rd}, two attention heads, and unembedding head WU . The relation is read from the final-position residual; we train with cross-entropy on the relation token using AdamW (Loshchilov and Hutter [2019]) with weight decay λ.

Measuring the number line: Let H = [ he0 ; . . . ; heN −1 ] ∈ RN ×d be the entity embeddings and 

u1 their first principal component, with eigenvalues λ1 ≥ · · · ≥ λd. We quantify ordinal structure three ways: 1. Linearity: the variance fraction on u1, ρ1 = λ1/ P 

> k

λk, large when embeddings lie near a line. 2. Rank alignment: the correlation between projected position πi = ⟨hei , u 1⟩ and true rank i,

rrank = corr  {πi}i, {i}i

, (6) near ±1 when the line recovers the hidden order. 3. Geometric separation: gij = |πi − πj |, the distance between two items on the recovered line. 4Behavioral signatures: Because accuracy saturates once the order is learned, we measure graded effects that persist at ceiling. For a query (ei, e j ) with correct and incorrect relation tokens c, ¯c, the 

decision confidence is the logit margin 

mij =  WU zij 

 

> c

−  WU zij 



> ¯c

, (7) where zij is the final-position residual. The symbolic distance effect predicts that both mij and gij 

increase with rank distance |i − j|; we report their Pearson correlation with |i − j| over DOOD .

## 5 Synthetic Experiment on Small Transformers 

The synthetic model is a single-layer, single-block Transformer with hidden dimension 3 d = 32 ,

2 attention heads, a 4d-wide GELU MLP, learned positional embeddings, and pre-LayerNorm; the relation is decoded from the final-position residual by a linear unembedding head ( ≈ 5 × 10 4

parameters). We optimize the relation-token cross-entropy with AdamW (learning rate 10 −3, batch size 64 ) and weight decay λ swept per chain length N , training for up to 5 × 10 4 steps and reporting all quantities at the peak-OOD accuracy checkpoint. The experiment is run on a 16GB M2 Macbook Air. We run the experiment for N = 10, 15, and 20 entities. The figures for N = 10, 20 can be found in the appendix. The following discussion is a qualitative breakdown of the trends seen in all three results. 

Figure 1: First principal projection vs. true rank in chain for N = 15 entities. 

Figure 2: (a) Decision confidence as a function of distance between compared entities. (b) PC1 distance as a function of pair distance. Figure 1 (rank vs. PC1) makes the linearity argument explicit by plotting each entity’s projection onto the first principal component against its true rank: at initialization the relationship is essentially    

> 3We use d= 64 for reliable grokking when training with more entities (N > 15).

5Figure 3: Training dynamics for N = 15 entities. random, whereas at peak the points form a clean monotonic staircase, confirming that the principal axis recovers the hidden total order almost exactly. Figure 2 tests for the symbolic distance effect using measures that remain informative once accuracy saturates: decision confidence and geometric separation on the recovered line, both increase monotonically with rank distance, exactly as predicted if comparisons are read off a mental number line. Figure 3 overlays PC1 variance, OOD accuracy, and train accuracy across training; train accuracy saturates almost immediately while OOD generalization and embedding linearity rise together and later, and crucially both degrade after the peak (dashed line), illustrating that the structured solution is transient under fixed weight decay — which is why all analyses are reported at the peak-OOD checkpoint. 

## 6 Scaling to Large Language Models 

The synthetic results raise the obvious question: is the rank line an artifact of a tiny model trained on a single hand-built order, or does the same geometry survive in large models trained on natural language? A pretrained LLM is never given an explicit comparison task, yet its training corpus is saturated with ordinal structure (number sequences, calendars, magnitude adjectives). If the rank-line account captures something general, fragments of it should be legible in the frozen representations of an off-the-shelf model (Gurnee and Tegmark [2024]). We probe Qwen2.5-1.5B on three ordinal domains chosen to vary in how cleanly they form a total order: the digits 0-9, the twelve months of the year, and a set of size adjectives ordered by magnitude (tiny , little , small , . . . , enormous , gigantic ). Each item is tokenized in isolation with special tokens (BOS, etc.) dropped, and its layer-ℓ representation is the mean of the residual-stream activations over the item’s sub-token positions at that layer: a single-token item contributes its one vector, while a word that splits (e.g. Sept +ember ) contributes the average of its pieces. We mean-pool rather than take the final sub-token, the standard alternative, which leans on the final position having attended over the whole word under causal masking because averaging privileges no single piece; neither choice is wrong, and we report the mean-pooled result. Stacking these into H ∈ RN ×d, we apply the diagnostics of Section 4: PCA for linearity, the rank-PC1 correlation rrank (Figure 4), and because a model carrying many other features need not place the ordinal axis on its leading component, the best |rrank | over the top three PCs as a function of depth (Figure 5), together with a leave-one-out (LOO) (Belinkov [2021]) linear probe regressing the hidden state onto rank, reported as R2. To ask whether distinct domains share an encoding direction, we also compute the cosine similarity between the probe directions fit on each pair of domains, layer by layer. 6Figure 4: PCA plots for the three ordinal domains: Digits (0-9), months of the year, and size adjectives, in early and late layers. 

Ordinal structure is present but domain-dependent. All three domains exhibit a positive rank-PC correlation that is stable between early and late layers (Figure 4): digits are recovered most cleanly (r ≈ 0.81 on PC2), sizes moderately, and months most weakly ( r = 0 .41 ). The size adjectives form the tightest ordering, with the diminutives at one extreme and enormous /gigantic at the other, consistent with magnitude being an unusually direct, frequently verbalised ordinal relation. Digits show a characteristic distortion: the token “0” sits far from 1-9 along PC1, so the ordinal structure of the remaining digits is carried by a lower component, perhaps due to the vastly different use-cases of zero as a digit relative to the other digits. 

The cyclic domain resists the line. Months are the weakest of the three, which is exactly what the rank-line picture predicts. A calendar is cyclic; December is adjacent to January. A cycle cannot be embedded isometrically on a single linear axis. The weak, layer-stable months correlation is therefore not a failure of the probe but a signature that the underlying relation is not a total order, directly addressing the cyclic-relation question raised in Section 7. 

Structure is layer-stable, not late-emerging. Across all three domains the rank-PC correlation and probe R2 are roughly flat over network depth (Figure 5), in contrast to the late, transient emergence seen in the synthetic model (Figure 3). This is expected: the LLM is not learning the order during measurement, the ordinal geometry is already fixed in its pretrained weights, so we observe its steady state rather than its formation. The digit probe R2 is the noisiest, dipping sharply (even below zero under LOO) in the middle layers, where the “0” outlier most distorts the fit. 7Figure 5: Probe R² (LOO) and best rank–PC correlation as a function of layer depth, for each ordinal domain. 

Each domain occupies its own axis. Figure 6 shows that the probe directions for different domains are nearly orthogonal at every layer ( | cos | < 0.2 throughout, with only a mild rise in the final layers). The model does not appear to encode a single, domain-general “ordinality” direction; instead each ordinal concept occupies its own subspace, even while all three individually obey the same low-dimensional, rank-aligned geometry. This is consistent with the synthetic theory, which fixes a separate readout w per task and makes no claim that independent orders share an axis. 

Figure 6: Cosine similarity between per-domain probe directions, by layer. 

Takeaway. The qualitative prediction of the rank-line account survives the jump from a ∼5 ×

10 4-parameter toy model to a 1.5B-parameter pretrained LLM: ordinal concepts are stored along approximately linear, rank-aligned manifolds whose cleanliness tracks how genuinely total the underlying order is (sizes > digits > the cyclic months). What does not transfer is universality: the model allocates a distinct axis per domain rather than reusing one. As in the synthetic study these observations are correlational; we leave causal interventions and a broader model and domain sweep to future work. 

## 7 Future Work 

The results in this paper point to several natural extensions. The observations are correlational, we make no claims on the causality of these observations. The effect of different depths and architectures on these results remain an interesting avenue, which also motivate theoretical work in trajectory level transient grokking dynamics. Finally, the LLM study is observational and limited to one 1.5B model and three hand-chosen domains; scaling across model sizes, probing cyclic and partial orders directly (the months result suggests a loop, not a line), and testing whether interventions on the recovered axis change model behaviour would together turn the present geometric description into a mechanistic and causal account. 8References 

Yonatan Belinkov. Probing classifiers: Promises, shortcomings, and advances, 2021. URL https: //arxiv.org/abs/2102.12452 .P E Bryant and T Trabasso. Transitive inferences and memory in young children. Nature , 232(5311): 456–458, August 1971. H Davis. Transitive inference in rats (rattus norvegicus). J Comp Psychol , 106(4):342–349, December 1992. Nelson Elhage, Tristan Hume, Catherine Olsson, Nicholas Schiefer, Tom Henighan, Shauna Kravec, Zac Hatfield-Dodds, Robert Lasenby, Dawn Drain, Carol Chen, Roger Grosse, Sam McCan-dlish, Jared Kaplan, Dario Amodei, Martin Wattenberg, and Christopher Olah. Toy models of superposition, 2022. URL https://arxiv.org/abs/2209.10652 .Wes Gurnee and Max Tegmark. Language models represent space and time, 2024. URL https: //arxiv.org/abs/2310.02207 .K J Holyoak and K K Patterson. A positional discriminability model of linear-order judgments. JExp Psychol Hum Percept Perform , 7(6):1283–1302, December 1981. Ilya Loshchilov and Frank Hutter. Decoupled weight decay regularization, 2019. URL https: //arxiv.org/abs/1711.05101 .Brendan O Mcgonigle and Margaret Chalmers. Are monkeys logical? Nature , 267(5613):694–696, June 1977. Tomas Mikolov, Wen-tau Yih, and Geoffrey Zweig. Linguistic regularities in continuous space word representations. In Lucy Vanderwende, Hal Daumé III, and Katrin Kirchhoff, editors, Proceedings of the 2013 Conference of the North American Chapter of the Association for Computational Lin-guistics: Human Language Technologies , pages 746–751, Atlanta, Georgia, June 2013. Association for Computational Linguistics. URL https://aclanthology.org/N13-1090/ .Gouki Minegishi, Jingyuan Feng, Hiroki Furuta, Takeshi Kojima, Yusuke Iwasawa, and Yutaka Matsuo. Emergent analogical reasoning in transformers, 2026. URL https://arxiv.org/abs/ 2602.01992 .Neel Nanda, Lawrence Chan, Tom Lieberum, Jess Smith, and Jacob Steinhardt. Progress measures for grokking via mechanistic interpretability, 2023. URL https://arxiv.org/abs/2301.05217 .Behnam Neyshabur, Ryota Tomioka, and Nathan Srebro. In search of the real inductive bias: On the role of implicit regularization in deep learning, 2015. URL https://arxiv.org/abs/1412. 6614 .Core Francisco Park, Andrew Lee, Ekdeep Singh Lubana, Yongyi Yang, Maya Okawa, Kento Nishi, Martin Wattenberg, and Hidenori Tanaka. Iclr: In-context learning of representations, 2025. URL 

https://arxiv.org/abs/2501.00070 .Alethea Power, Yuri Burda, Harri Edwards, Igor Babuschkin, and Vedant Misra. Grokking: Gener-alization beyond overfitting on small algorithmic datasets. CoRR , abs/2201.02177, 2022. URL 

https://arxiv.org/abs/2201.02177 .Christine A Riley. The representation of comparative relations and the transitive inference task. 

Journal of Experimental Child Psychology , 22(1):1–22, 1976. ISSN 0022-0965. doi: https: //doi.org/10.1016/0022-0965(76)90085-0. URL https://www.sciencedirect.com/science/ article/pii/0022096576900850 .Janice N. Steirn, Janice E. Weaver, and Thomas R. Zentall. Transitive inference in pigeons: Simplified procedures and a test of value transfer theory. Animal Learning & Behavior , 23(1):76–82, Mar 1995. ISSN 1532-5830. doi: 10.3758/BF03198018. URL https://doi.org/10.3758/BF03198018 .9A Technical Appendices and Supplementary Material 

A.1 Data for N = 10 entities 

Figure 7: First principal projection vs. true rank in chain for N = 10 entities. 

Figure 8: (a) Decision confidence as a function of distance between compared entities. (b) PC1 distance as a function of pair distance. 

Figure 9: Training dynamics for N = 10 entities. 10 A.2 Data for N = 20 entities 

Figure 10: First principal projection vs. true rank in chain for N = 20 entities. 

Figure 11: (a) Decision confidence as a function of distance between compared entities. (b) PC1 distance as a function of pair distance. 

Figure 12: Training dynamics for N = 20 entities. 11