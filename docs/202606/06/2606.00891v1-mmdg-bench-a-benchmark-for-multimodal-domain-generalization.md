---
title: "MMDG-Bench: A Benchmark for Multimodal Domain Generalization"
title_zh: MMDG-Bench：多模态领域泛化基准
authors: "Qianshan Zhan, Qian Wang, Da Li, Xiao-Jun Zeng, Xiatian Zhu"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00891v1"
tags: ["query:cross-domain"]
score: 9.0
evidence: 多模态领域泛化基准
tldr: 多模态领域泛化（MMDG）研究存在评估协议不统一、任务局限等问题。本文提出MMDG-Bench基准，包含D2M（先DG后多模态学习）和M2D（先多模态学习后DG）两种框架，覆盖动作识别、人脸防伪等任务。通过组合统一多模态配置与五种DG技术，发现结构化方法常优于现有SOTA。三个关键发现：DG技术带来一致泛化增益；框架选择取决于模态跨域稳定性，D2M适合稳定关系，M2D鲁棒于关系变化；强骨干网络在结构化框架中收益更大。该基准为多模态鲁棒性研究提供了原则性基础和设计指南。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00891v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1253, \"height\": 1010, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00891v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 655, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00891v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 581, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00891v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 582, \"height\": 352, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00891v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1260, \"height\": 340, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00891v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1214, \"height\": 1558, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00891v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1305, \"height\": 669, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00891v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1253, \"height\": 603, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00891v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 591, \"height\": 417, \"label\": \"Table\"}]"
motivation: 现有MMDG研究局限于动作识别且缺乏标准化评估协议，制约了多模态与领域泛化系统的性整合。
method: 构建MMDG-Bench基准，提出D2M与M2D两种框架，通过配对统一多模态配置与五种DG技术，在多种任务上建立评估协议。
result: 结构化组合频繁超越现有SOTA；DG技术提供一致泛化增益；D2M在模态关系稳定时更优，M2D对跨域关系变化更鲁棒。
conclusion: MMDG-Bench为多模态鲁棒性研究提供了基准平台与设计准则，强骨干网络在结构化框架中增益显著。
---

## 摘要
多模态领域泛化（MMDG）旨在利用互补模态增强模型在未见领域上的鲁棒性。尽管多模态学习（MML）和领域泛化（DG）作为独立领域取得了广泛进展，但它们的系统集成仍未得到充分探索。当前的MMDG研究主要局限于动作识别，且缺乏标准化的评估协议。为此，我们提出了MMDG-Bench，这是一个全面的基准，包含两个基础框架：先DG后MML（D2M）和先MML后DG（M2D）。我们提供了跨多种任务的统一实验协议，包括视频-音频-光流动作识别和RGB-深度-红外人脸防伪。通过将统一的多模态配置与五种DG技术配对，并在D2M和M2D两种顺序下实例化十个MMDG基线，我们证明了这些结构化组合通常优于现有最先进方法，突显了统一基准的必要性。我们的分析得出三个关键见解：（1）集成DG技术可在各种骨干网络上提供一致的泛化提升，而非DG方法对骨干网络变化高度敏感；（2）最优框架选择取决于模态间稳定性：当模态关系在领域间稳定时，D2M表现优异，而M2D对跨领域关系变异更具鲁棒性；（3）更强的骨干网络在集成到我们的结构化框架中时能带来更大的性能红利。MMDG-Bench为未来多模态鲁棒性研究提供了原则性基础和可操作的设计指南。代码发布于https://github.com/qszhan/MMDG-Bench。

## Abstract
Multi-modal Domain Generalization (MMDG) seeks to leverage complementary modalities to enhance model robustness on unseen domains. Despite extensive progress in Multi-modal Learning (MML) and Domain Generalization (DG) as individual fields, their systematic integration remains under-explored. Current MMDG research is largely confined to action recognition and lacks standardized evaluation protocols. To address this, we introduce MMDG-Bench, a comprehensive benchmark featuring two foundational frameworks: DG then MML (D2M) and MML then DG (M2D). We provide unified experimental protocols across diverse tasks, including video-audio-flow action recognition and RGB-Depth-IR face anti-spoofing. By instantiating ten MMDG baselines through pairing a unified MML configuration with five DG techniques under both D2M and M2D orderings, we demonstrate that these structured combinations frequently outperform existing state-of-the-art methods, underscoring the necessity of a unified benchmarking effort. Our analysis yields three key insights: (1) Integrating DG techniques provides consistent generalization gains across various backbones, whereas non-DG methods are highly sensitive to backbone shifts; (2) The optimal framework choice depends on inter-modal stability: D2M excels when modal relations are stable across domains, while M2D is more robust to cross-domain relational variance; (3) Stronger backbones yield amplified performance dividends when integrated into our structured frameworks. MMDG-Bench provides a principled foundation and actionable design guidelines for future research in multi-modal robustness. Code is released at https://github.com/qszhan/MMDG-Bench.

---

## 论文详细总结（自动生成）

好的，以下是基于您提供的论文内容，以 Markdown 格式生成的结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

-   **研究动机**：多模态学习（MML）和领域泛化（DG）作为两个独立的研究领域已经取得了显著进展。然而，它们的系统结合——多模态领域泛化（MMDG）——仍是一个未被充分探索的领域。
-   **核心问题**：
    1.  **缺乏系统性评估协议**：现有的 MMDG 研究没有统一的实验标准和评估协议，导致不同研究工作难以进行公平比较。
    2.  **任务局限性**：目前绝大多数的 MMDG 研究都局限于动作识别这一个单一任务，其泛化能力和方法的通用性没有得到广泛验证。
    3.  **方法整合不足**：此前的方法要么只关注多模态对齐而忽视了 DG 文献中的丰富技术，要么在引入 DG 概念时缺乏系统性。
-   **整体含义**：为了解决上述问题，本文提出了 **MMDG-Bench**，旨在建立一个全面、标准化的基准测试平台，用于系统性地评估和整合不同的 MML 和 DG 方法，从而推动 MMDG 领域的规范化发展。

### 2. 论文提出的方法论：核心思想、关键技术细节

#### 2.1. 核心思想：两种互补框架

文章的核心贡献是提出了两种用于系统整合 MML 和 DG 方法的互补框架：

1.  **D2M (DG then MML)**：先领域泛化，后多模态学习。
    -   **流程**：首先，在每个模态内部使用 DG 方法对齐不同源域的特征，消除模态内的领域差异。然后，将得到的领域不变、模态特定的特征进行多模态融合。
    -   **思想**：确保后续的多模态融合操作是基于已经过领域对齐的特征，从而提升模型在未知领域的泛化能力。

2.  **M2D (MML then DG)**：先多模态学习，后领域泛化。
    -   **流程**：首先，在每个源域内部进行多模态融合（对齐模态间的差异），捕获互补信息。然后，在融合后的特征上应用 DG 方法，对齐不同源域之间的分布。
    -   **思想**：先利用模态互补信息，再进行领域对齐，更适合跨模态关系随领域变化较大的场景。

#### 2.2. 关键技术细节

-   **MML 组件**：论文选取了四种代表性的多模态学习方法，覆盖了度量学习、生成模型、优化策略和注意力机制四大范式，并将其组合为一个统一的 **MM** 模块：
    1.  **监督对比学习（C）**：拉近相同标签样本的跨模态特征。
    2.  **模态翻译（T）**：从一个模态重建另一个模态，增强跨模态一致性。
    3.  **梯度混合（G）**：根据模态的过拟合-泛化行为动态调整模态梯度权重，防止某一模态主导训练。
    4.  **注意力瓶颈融合（F）**：通过紧凑的潜在空间进行模态间交互，捕获最有信息量的交叉模态特征。

-   **DG 组件**：论文选取了五种典型 DG 方法，覆盖了标准训练、数据增强、分布对齐、表示学习和信息论五大思路：
    1.  **ERM**：经验风险最小化，作为基线。
    2.  **Mixup**：通过线性插值样本和标签进行数据增强。
    3.  **MMD**：最小化不同源域特征分布之间的最大均值差异。
    4.  **CL**：通过对比学习对齐不同源域的语义特征。
    5.  **MIRO**：通过互信息正则化学习领域不变特征。

-   **MMDG 变体**：将统一的 MML 配置（MM）与每种 DG 方法组合，并在 D2M 和 M2D 两种框架下进行评估，最终形成 **10 种 MMDG 变体**。
-   **优化器**：所有实验统一使用 SAM 优化器来增强鲁棒性。

### 3. 实验设计

-   **数据集与场景**：
    -   **动作识别**：使用 **EPIC-Kitchens**（三个不同厨房，8 种动作）和 **HAC**（人类、动物、卡通三个域，7 种动作）数据集。每个样本包含视频、音频和光流三种模态。
    -   **人脸防伪**：使用 **CASIA-CeFA (C)**、**CASIA-SURF (S)** 和 **WMCA (W)** 三个数据集。每个数据集视为一个域，提供 RGB、Depth 和 IR 三种模态。
-   **基准与对比方法**：
    -   将提出的 10 种 MMDG 变体与 **DeepAll**（无 DG 策略的基线）、**SimMMDG**、**CMRF**、**MDJAT** 和 **MAD-DG** 等现有 MMDG 方法进行对比。
    -   在人脸防伪任务上，还对比了 **ViT-Base-CA** 和 **mmdg**。
    -   所有对比方法都在统一的实验设置下重新实现，确保公平性。

### 4. 资源与算力

-   **GPU 型号和数量**：单张 **NVIDIA H100 (80 GB) GPU**。
-   **训练时长**：
    -   **动作识别**：CNN 骨干网络平均耗时约 1 小时；ViT 骨干网络平均耗时约 5 小时。
    -   **人脸防伪**：每次实验通常需要 6-9 小时。

### 5. 实验数量与充分性

-   **实验数量**：实验非常丰富，包括：
    -   **主要结果**：在两种任务、多个数据集、多种骨干网络（CNN, ViT）和多种模态组合上的性能对比。
    -   **消融实验**：对 MML 组件（C, T, G）和优化器 (SAM, AdamW, SGD) 进行了详细的消融分析。
    -   **鲁棒性分析**：评估了测试时模态缺失（Missing Modalities）情况下的性能，并提出了 **CMRS** 指标来分析跨模态关系稳定度，以此来解释框架选择。
    -   **超参数分析**：对所有 DG 正则化权重进行了参数敏感性分析。
    -   **部署分析**：研究了骨干网络层解冻（fine-tuning depth）对性能与效率的影响。
-   **充分性与客观性**：
    -   实验设计较为充分，涵盖了多任务、多数据集、多种骨干网络和多种方法组合。
    -   所有对比方法都在统一框架下复现，确保了公平比较。
    -   消融实验和分析实验设计合理，能够有效支撑论文的核心观点。
    -   结论客观，明确指出了方法的优势和可能的局限性（如框架选择依赖于数据特性）。

### 6. 论文的主要结论与发现

论文得出了三个关键见解：

1.  **集成 DG 是关键**：在所有任务和骨干网络上，结合 DG 的 MMDG 变体（MM+DG）的性能始终优于主要依赖 MML 的现有方法。更强的骨干网络（如 ViT）在现有 MMDG 方法上可能带来不稳定的变化，但在所提出的结构化框架中能放大 DG 带来的增益。
2.  **框架选择取决于跨模态关系稳定性**：
    -   当跨模态关系在不同领域间相对稳定时（如 EPIC-Kitchens 中，不同厨房的语义映射相似），**D2M** 框架表现更好。
    -   当跨模态关系随领域变化较大时（如 HAC 中，人与卡通/动物的音频-视觉映射不同），**M2D** 框架更鲁棒。作者提出的 **CMRS** 指标可以量化这种稳定性，作为选择框架的指南。
3.  **强骨干网络提升性能上限并放大框架差异**：更强的骨干网络不仅提升了整体性能，也使得 D2M 和 M2D 之间的性能差距更加明显，使得正确的框架选择变得更为关键。

### 7. 优点

-   **系统性/标准化**：首次为 MMDG 领域提供了一个统一的、可扩展的基准测试框架（D2M, M2D），解决了该领域长期缺乏标准评估协议的问题。
-   **方法全面**：涵盖了对多模态学习和领域泛化两大领域主流方法的系统性整合，实验覆盖广泛。
-   **洞察深刻**：不仅给出了性能结果，还通过分析得出了关于框架选择、DG 重要性等具有指导意义的结论，为未来研究提供了设计准则。
-   **公平可靠**：所有对比方法均在统一框架下复现，并使用统一的优化器（SAM），确保了对比的公平性。

### 8. 不足与局限

-   **实验覆盖的局限性**：虽然覆盖了动作识别和 FAS 两个任务，但 MMDG 场景的多样性远不止于此（例如，在自然语言处理或自动驾驶等领域的多模态数据上的泛化性尚未验证）。
-   **框架选择的预判性**：虽然提出了 CMRS 指导框架选择，但该指标是在整个训练集上计算得出的，对于全新的、完全未知的领域，如何准确预判跨模态稳定性仍是一个开放问题。
-   **DG 收益的边界**：结果表明显式加入 DG 是普遍有益的，但未深入探讨在何种极端情况下（如极度缺乏数据或模态高度不匹配）DG 带来的增益会消失甚至带来负面效果。
-   **计算成本**：ViT 骨干和 SAM 优化器的组合带来了显著的计算开销（5小时/单任务），这可能对其在资源受限场景下的应用造成一定限制。

（完）
