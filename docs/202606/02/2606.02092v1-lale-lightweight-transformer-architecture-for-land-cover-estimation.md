---
title: "LALE: Lightweight-Transformer Architecture for Land-Cover Estimation"
title_zh: LALE：用于土地覆盖估算的轻量级Transformer架构
authors: "Ümit Mert Çağlar, Alptekin Temizel"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02092v1"
tags: ["query:rs-fm"]
score: 9.0
evidence: 用于遥感土地覆盖估计的轻量级Transformer
tldr: 遥感图像语义分割需平衡全局上下文与局部细节，但现有混合架构计算开销大。本文提出LALE，通过将编码器按分辨率分支：高分辨率用轻量级ConvMixer提取局部特征，低分辨率用Transformer捕获全局上下文，将自注意力限制在深层低分辨率特征图上，大幅降低计算量。全MLP多尺度解码器配合RMSNorm和StarReLU进一步压缩参数。在ARAS400k上，最小模型仅1.6M参数，相比UPerNet参数减少4.5倍，存储减少7倍，GMACs减少17倍，吞吐量提升1.8倍，而F1仅低2.6点，实现效率与性能的强折中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02092v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1629, \"height\": 265, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02092v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1426, \"height\": 870, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02092v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1583, \"height\": 592, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02092v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1203, \"height\": 516, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02092v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1620, \"height\": 804, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02092v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1607, \"height\": 538, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02092v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1230, \"height\": 846, \"label\": \"Table\"}]"
motivation: 现有遥感分割模型难以同时高效捕获全局与局部信息。
method: 采用分辨率分支编码：高分辨率用ConvMixer，低分辨率用Transformer，结合全MLP解码器与RMSNorm、StarReLU。
result: 最小变体1.6M参数，在ARAS400k上比UPerNet参数少4.5倍，GMACs少17倍，F1仅低2.6。
conclusion: LALE为遥感语义分割提供了兼顾效率与性能的轻量级解决方案。
---

## 摘要
遥感影像的语义分割需要模型在严格的计算预算下同时捕捉全局上下文和局部细节。先前的工作通常优化其中一个方面：利用注意力机制捕捉全局上下文、卷积捕捉局部细节，或通过紧凑性提高效率。尽管混合方法旨在同时捕捉两者，但它们需要架构更改和带有计算开销的编码器主干网络，限制了效率和性能。我们提出LALE（用于土地覆盖估算的轻量级Transformer架构），一种端到端的遥感图像分割架构，其编码器按分辨率分为两支：轻量级ConvMixer阶段处理高分辨率局部特征，而Transformer阶段处理低分辨率全局上下文，将自注意力的二次成本限制在深层、下采样的特征图上。全MLP多尺度解码器，结合全网络的RMSNorm和StarReLU，进一步减少了计算量和参数量。在大型ARAS400k遥感分割基准上，LALE相对于CNN、Transformer和混合基线模型建立了强大的效率-性能权衡。我们最小的变体（仅1.6M参数）在F1分数上比最佳基线（UPerNet）低2.6个百分点，但参数量减少4.5倍，存储空间减少7倍，GMACs减少17倍，吞吐量提高1.8倍。

## Abstract
Semantic segmentation of remote sensing imagery requires models that capture both global context and local detail under tight computational budgets. Prior work typically optimizes for one of these axes: attention for global context, convolution for local detail, or compactness for efficiency. While hybrid approaches aim to capture both, they require architectural changes and encoder backbones with computational overhead, limiting efficiency and performance. We present LALE (Lightweight-transformer Architecture for Land-cover Estimation), an end-to-end remote sensing image segmentation architecture, that bifurcates its encoder by resolution: lightweight ConvMixer stages handle high-resolution local features, while transformer stages handle low-resolution global context, confining the quadratic cost of self-attention to deep, downsampled feature maps. An all-MLP multi-scale decoder, together with RMSNorm and StarReLU throughout, further reduces compute and parameter count. On the large-scale ARAS400k remote-sensing segmentation benchmark, LALE establishes a strong efficiency-performance trade-off against CNN, transformer, and hybrid baselines. Our smallest variant, (just 1.6M parameters), reaches within 2.6 F1 points of the best baseline (UPerNet) while using 4.5x fewer parameters, 7x less storage, 17x fewer GMACs, and delivering 1.8x higher throughput.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

遥感图像语义分割对于环境监测、城市规划、灾害感知和精确土地覆盖估算至关重要。现有方法面临以下困境：

- **CNN**：具有强归纳偏置和计算效率，但有限感受野限制了长距离推理能力。
- **Vision Transformer**：通过自注意力有效建模全局上下文，但在高分辨率下二次计算成本高昂，尤其不适合高分辨率、高吞吐量、资源受限的遥感部署场景。
- **混合架构**：试图结合两者优势，但大多基于通用ImageNet骨干网和参数沉重的解码器，继承了不必要的计算开销，未能针对分割任务本身优化。

因此，如何在保证分割精度的同时显著降低计算量和参数量，成为亟待解决的效率-性能权衡问题。

**本文意义**：提出LALE（Lightweight-transformer Architecture for Land-cover Estimation），一种专为遥感语义分割设计的端到端轻量级混合架构，在ARAS400k基准上实现了极佳的效率-性能折中。

## 2. 方法论：核心思想与关键技术细节

### 2.1 核心思想：按分辨率分支的编码器

编码器分为四个阶段，根据分辨率高低采用不同模块：
- **高分辨率阶段（Stage 1 & 2）**：使用**ConvMixer模块**提取局部细节特征，计算高效。
- **低分辨率阶段（Stage 3 & 4）**：使用**Transformer模块**（带卷积MLP）捕获全局上下文，将自注意力二次成本限制在深层小特征图上。
- 阶段之间使用**3×3步长2卷积 + RMSNorm**进行下采样。

### 2.2 关键技术细节

1. **卷积茎（Convolution Stem）**：两个连续3×3卷积（步长2，填充1），后接RMSNorm和StarReLU，将输入从H×W降至H/4×W/4，通道数增至32。相比常见7×7卷积更高效。
2. **ConvMixer块**（用于早期阶段）：深度可分离卷积进行空间混合，逐点卷积进行通道混合，计算轻量。
3. **Transformer块**（用于后期阶段）：包含多头注意力（MHA）和卷积MLP（ConvMLP）。ConvMLP在两层全连接之间插入3×3深度卷积，注入空间归纳偏置。
4. **高效归一化与激活**：使用**RMSNorm**替代LayerNorm（省略重中心化，更高效）；使用**StarReLU**替代GELU（避免指数运算，更高效）。
5. **轻量级多尺度解码器**：
   - 对四个阶段输出的特征图用1×1卷积统一投影到C_dec=128通道。
   - 将深层次特征双线性上采样到F1分辨率（H/4×W/4）。
   - 拼接后融合，使用批归一化（BN）和StarReLU。
   - 最终1×1卷积预测N类logits，并双线性插值回原始分辨率。
6. **小核策略**：所有卷积使用3×3核（而非7×7），提高局部细节能力并减少计算量（3×3步长2比7×7步长1高效22倍）。

## 3. 实验设计

### 3.1 数据集与基准

- **主数据集**：ARAS400k（大规模遥感图像分割数据集，80,192张图像）。
- **拓展验证**：LiTS（肝脏和肿瘤分割基准），验证跨领域泛化性。
- **数据划分**：训练-验证-测试集分割固定，采用相同的数据增强、早停、学习率衰减、Dice损失等配置，保证公平性。

### 3.2 对比方法

- **CNN基线**：DeepLabV3, DeepLabV3+, FPN, Linknet, PAN, Unet, Unet++, UPerNet, SegFormer（使用EfficientNet编码器）。
- **Transformer基线**：EfficientFormer-L1/L3/L7, DeiT3-Base, MaxViT-Tiny, FastViT-SA12, FastViT-MCI0。
- **LALE变体**：LALE-S1至S4（不同模型规模），以及消融中的B-K7（基线7×7核）、K3（3×3核）、PT（ImageNet预训练）等配置。

## 4. 资源与算力

论文明确给出了算力消耗：

- **总耗时**：ARAS400k实验共321小时（架构搜索160h + 基准测试106h + 消融55h）。
- **单个模型平均训练时间**：约2.2小时。
- **LiTS数据集实验**：共76小时（肝脏9h + 肿瘤42h + 架构搜索25h）。
- **硬件**：单张NVIDIA H100 GPU（80GB）。
- **辅助设施**：TUBITAK ULAKBIM高性能计算中心（TRUBA）。

## 5. 实验数量与充分性

### 实验组数

1. **主基准（Table 1）**：9种CNN/Attention/混合架构 + 2种LALE变体 + 7种Transformer模型，共18种配置，报告F1、精度、召回、IoU、模型大小、吞吐量、参数量、GMACs。
2. **消融研究（Table 2 & Figure 4）**：12种配置，包括不同尺度（S1-S4）、是否使用3×3核（K3 vs K7）、是否ImageNet预训练（PT），系统验证设计选择。
3. **跨领域泛化（Table 3）**：在LiTS上评估肝脏分割（18种架构）和肿瘤分割（18种架构），共36个结果。
4. **进一步**：训练配置统一（相同增强、早停、学习率策略、损失函数、梯度裁剪、硬件），保证公平对比。

**充分性评价**：实验覆盖全面，包括多个尺度、消融、跨领域验证；对比方法多样且均为主流；严格控制变量。但缺乏其他遥感数据集（如ISPRS Vaihingen等）的验证，且未在边缘设备上实际部署测试。

## 6. 主要结论与发现

1. **LALE实现最佳效率-性能折中**：最小变体（1.6M参数）相比最佳基线UPerNet（11.6M参数），F1仅低2.6点，但参数量减少4.5倍、存储减少7倍、GMACs减少17倍、吞吐量提高1.8倍。
2. **CNN模型精度最高但计算中等**，Transformer模型精度接近但计算成本极高（参数量大、吞吐量低）。
3. **LALE的端到端设计优势**：无需修改现有编码器或解码器，直接为遥感分割任务优化，避免了传统混合架构的额外计算开销。
4. **消融证实**：使用3×3核、StarReLU、RMSNorm、ImageNet预训练均带来一致性能提升，且能进一步降低计算量。
5. **跨领域泛化**：在LiTS上，LALE在肝脏分割上接近最佳基线（～1 F1差距），肿瘤分割差距稍大（～5 F1），但仍优于许多CNN和Transformer模型，证明了架构的通用性。

## 7. 优点

- **创新性**：首次提出按分辨率分支的混合编码器设计，将自注意力限制在低分辨率阶段，有效控制计算复杂度。
- **高效性**：所有组件（RMSNorm、StarReLU、小核卷积、全MLP解码器）均经过精心选择，在保持表现力的同时极低计算量。
- **公平性**：所有对比实验使用相同训练配置、硬件、数据划分，消融研究系统完整。
- **可复现性**：公开架构细节、训练策略，并明确说明算力使用。
- **跨领域验证**：在医学图像（LiTS）上也验证了方法的泛化能力，增强说服力。

## 8. 不足与局限

1. **数据集覆盖不足**：仅在ARAS400k和LiTS上验证，缺乏其他遥感常见数据集（如ISPRS Vaihingen、Potsdam、DeepGlobe等）的评估，可能限制结论普适性。
2. **肿瘤分割性能下降**：在LiTS肿瘤分割任务上，LALE与最佳基线（Linknet、Unet）差距较大（～5 F1），说明面对小目标严重不平衡场景时仍有改进空间。
3. **未进行部署级分析**：论文提到未来工作包括部署到计算受限硬件，但当前实验仅在H100 GPU上测试吞吐量，缺乏在边缘端（如Jetson、手机）的实际延迟和功耗评估。
4. **消融缺乏更深层分析**：虽然对比了不同配置，但未深入分析每个设计选择（如RMSNorm vs LayerNorm、StarReLU vs GELU、ConvMixer vs 其他轻量模块等）对精度和速度的具体影响，仅给出了整体比较。
5. **对比方法编码器选择单一**：CNN基线均使用EfficientNet编码器，未与ResNet等更常见但更重的编码器对比，可能影响效率比较的全面性。
6. **未进行更广泛规模的训练**：仅尝试了S1-S4四种尺度，未探索更大或更小变体，也未分析尺度与性能之间的关系。

（完）
