---
title: Active Source-free Domain Adaptation in Open-set Medical Image Segmentation via Decomposed Uncertainty and Prototype Discrepancy
title_zh: 基于分解不确定性与原型差异的主动无源域适应在开放集医学图像分割中的应用
authors: "Jin Yang, Yichi Zhang, Peijie Qiu, Xiaobing Yu"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08749v1"
tags: ["query:cross-domain"]
score: 8.0
evidence: 医学图像分割中的主动源域缺失域适应
tldr: 面对医学图像分割中的域偏移和数据隐私问题，现有闭集域适应方法无法处理目标域中出现的新类别（开集）。本文首次提出主动源自由开集域适应方法ASFOSDA，利用类感知分解不确定性（CDU）和类不可知原型差异（CPD）选择最有信息量的目标样本查询，并通过目标精炼自训练生成高质量伪标签进行半监督学习。在跨域开集体积分割任务上，该方法显著优于现有方法，实现了无源数据下的开集域适应，具有临床实用价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08749v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1250, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08749v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1250, \"height\": 237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08749v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1259, \"height\": 322, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08749v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 933, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08749v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 235, \"label\": \"Table\"}]"
motivation: 域偏移和隐私限制下，现有主动域适应方法局限于闭集，无法处理目标域中的新类别，亟需开集解决方案。
method: 提出CDU和CPD的主动查询策略，结合目标精炼自训练生成伪标签，进行半监督域适应。
result: 在多个跨域开集分割任务中，ASFOSDA优于现有闭集和开集域适应方法。
conclusion: 首个无源数据开集主动域适应方法，提升了医学图像分割在临床上的泛化性和实用性。
---

## 摘要
深度学习方法因域偏移而在不同分割数据集上难以展现稳健性能，但主动域适应技术通过从目标域查询少量样本进行适应训练来增强其泛化性能。然而在临床实践中，目标域常包含源数据中未出现的新解剖结构或病理的私有类别，而现有方法实施的是源域与目标域具有相同分割类别的封闭集分割。此外，由于严格的数据隐私规定，适应过程中源数据常不可访问。为解决这些局限，我们提出了一种主动无源开放集域适应（ASFOSDA）方法，这是首个在无需源数据的情况下，将主动学习应用于开放集医学图像分割模型适应的工作。该方法采用基于类别感知分解不确定性（CDU）和类别无关原型差异（CPD）的主动开放集查询策略，以选择信息量最大的目标样本用于模型训练。CDU通过在随机过程中运用测试时增强来测量样本的偶然不确定性和模型的认知不确定性。CPD则通过测量跨域和自域差异来选择多样性样本。随后，为通过增强训练样本来提升适应性能，提出了一种目标精细化自训练策略，为未选中样本生成高质量伪标签，从而将其与有标签样本结合进行半监督训练。我们在跨域开放集体积医学图像分割任务上评估了该方法，其表现优于现有最先进的适应方法。

## Abstract
Deep learning (DL) methods are challenged to demonstrate robust performance across different segmentation datasets due to domain shifts, but active domain adaptation techniques enhance their generalization performance by querying a few samples from target domains for adaptation training. However in clinical practice, target domains often include private classes of new anatomical structures or pathologies that are not presented in the source data, and existing methods implement closed-set segmentation where source and target domains have the same segmentation classes. Additionally, source data are often inaccessible during adaptation due to strict data privacy regulations. To address these limitations, we propose an Active Source-free Open-set Domain Adaptation (ASFOSDA) method which is the first work to implement active learning for adapting DL models in open-set medical image segmentation without the access to source data. This method employs an active open-set query strategy to select the most informative target samples for training models based on Class-aware Decomposed Uncertainty (CDU) and Class-agnostic Prototype Discrepancy (CPD). CDU measures sample aleatoric uncertainty and model epistemic uncertainty by employing test time augmentation in stochastic processes. CPD measures cross-domain and self-domain discrepancy for selecting diverse samples. Subsequently, to boost the adaptation performance by enhancing training samples, a Target-refined Self-training strategy is proposed to generate high-quality pseudo labels for unselected samples, thus combining them with labeled samples for a semi-supervised training. We evaluated our method on cross-domain open-set volumetric medical image segmentation tasks, and it outperformed state-of-the-art adaptation methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：深度学习模型在跨域医学图像分割中面临域偏移（domain shift），而传统域适应方法通常假设源域和目标域类别相同（闭集），且依赖源数据。然而临床实践中，目标域常包含源数据中未曾出现的解剖结构或病理类别（开放集），同时严格的隐私法规禁止共享源数据。现有主动域适应方法缺乏对未知类别信息量的评估机制，无法适应开放集场景。
- **整体含义**：本文首次提出**主动无源开放集域适应（ASFOSDA）** 方法，在不访问源数据、且目标域包含新类别的情况下，通过少量目标样本注释实现高性能分割，填补了该领域空白，具有临床实用价值。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：结合**主动学习**与**无源开放集域适应**，设计两级查询策略（不确定性+多样性），并利用目标精炼自训练增强训练数据，实现半监督域适应。
- **关键技术细节**：
  - **Active Open-set Query Strategy**（主动开放集查询策略）：
    - **Class-aware Decomposed Uncertainty (CDU)**：分解为类感知偶然不确定性（CAU）和类感知认知不确定性（CEU）。通过测试时增强（TTA）获取原始图像与增强图像的概率图/对数几率，对**未知类别**计算熵和能量方差，反映模型对未知类别的信心和泛化能力。
    - **Class-agnostic Prototype Discrepancy (CPD)**：选取低不确定性样本作为源知识原型，高不确定性样本作为候选；计算跨域差异（候选与原型间的语义距离）和自域差异（候选间的不相似度），避免冗余。
  - **Target-refined Self-training**：先用已标注样本（查询后人工标注）全监督训练模型，再用该模型为未选中样本生成并精炼伪标签，最后将两者混合进行半监督训练（联合监督损失与无监督损失）。
- **算法流程**（文字说明）：
  1. 使用源数据预训练分割模型。
  2. 对目标域未标注样本，通过TTA计算CAU和CEU得到CDU分数。
  3. 选取CDU得分最低和最高的K个样本分别作为原型和候选，计算CPD分数。
  4. 综合CDU和CPD选出信息量最大的N_l个样本进行人工标注。
  5. 用这些标注样本全监督训练模型。
  6. 用更新后的模型为剩余未标注样本生成伪标签并精炼。
  7. 将标注样本和伪标注样本混合进行半监督训练一定轮数。

### 3. 实验设计：数据集/场景、基准（benchmark）、对比方法
- **数据集与场景**：
  - **CT分割任务**：
    - MSD Spleen → FLARE（单器官→四器官）
    - FLARE → AMOS CT（四器官→14器官）
  - **MR分割任务**：
    - Abdominal MR → AMOS MRI（八器官→13器官）
  - 均为开放集场景（目标类别数 > 源类别数）。
- **基准（Benchmark）**：
  - **Source Only**：无适应，下界。
  - **Target Supervision**：使用全部目标域标注，上界。
- **对比方法**：
  - **无源域适应（SFDA）**：DPL、FSM、TT-SFUDA。
  - **主动无源域适应（ASFDA）**：Random、PENT、LC、PMAR、Core-set、MAXREP、CENT、UGTST（均使用5%目标样本标注预算）。
- **网络架构**：3D U-Net、Swin UNETR。
- **评价指标**：Dice相似系数（DSC, %）、95% Hausdorff距离（95HD, mm）。

### 4. 资源与算力
- 文中明确提到使用 **PyTorch**，单张 **NVIDIA Tesla A100 PCI-E Passive GPU**。
- 训练轮数：源域预训练1000 epochs，目标域适应500 epochs。
- 优化器：AdamW，初始学习率0.001，多项式衰减（power=0.9）。
- Batch size：2。
- **未提及训练总时长、单次实验具体耗时**，也未说明并行GPU数量。

### 5. 实验数量与充分性
- **主要实验（表1）**：在两种网络×三个任务×十种以上对比方法上报告DSC和95HD，共约2×3×(1+1+3+8+1)=约84个配置，每个结果有均值±标准差。
- **消融实验（表2）**：逐步添加CAU、CEU、CPD、Self-training四个组件，在三任务上验证，共4×3=12组。
- **超参数分析（图3）**：
  - (A) 查询预算从0%到20%对性能的影响（3条曲线）。
  - (B) 原型/候选数量K的影响（K从10%到50%）。
  - (C) 不同不确定性度量方法对比（PENT、LC、PMAR、CAU、CEU、CDU）。
- **定性结果（图2）**：展示了部分可视化分割结果。
- **充分性评估**：实验设计较为全面，覆盖了不同器官数、不同模态（CT/MR）、不同网络结构，并进行了充分的消融和超参数分析，对比方法涵盖当前主流SFDA和ASFDA方法。不足在于未验证其他查询预算（除5%和0-20%曲线外），且未报告多次重复实验的统计功效（如仅用Mann-Whitney U检验，未给出重复次数）。

### 6. 论文的主要结论与发现
- ASFOSDA在所有三个开放集任务中显著优于现有SFDA和ASFDA方法，U-Net可达85%以上的上界性能（Target Supervision 的85~90%）。
- 在MSD→FLARE（单→四器官）中，ASFOSDA达到上界的84%以上；在FLARE→AMOS CT（四→14器官）中达到85%以上；在难任务MR（8→13器官）中，U-Net仍达到82.62%上界。
- CDU（特别是结合CAU和CEU）比单独使用任一不确定性指标更优；CPD进一步提升了性能。
- Target-refined Self-training对提升性能贡献显著（表2最后两行对比）。

### 7. 优点
- **首创性**：首个同时解决无源、开放集、主动学习三个挑战的医学图像分割方法。
- **方法合理**：分解偶然与认知不确定性，并同时考虑类内变异与跨域差异，避免冗余采样。
- **实用性强**：无需源数据，仅需少量目标标注，符合临床隐私与成本约束。
- **实验充分**：在多种器官数、CT/MR模态、两个经典骨干网络上验证，对比基线全面。
- **消融清晰**：逐步验证各组件贡献，结论可信。

### 8. 不足与局限
- **算力资源披露不足**：未提供总训练时间或FLOPs，不利于复现和成本评估。
- **重复性验证缺失**：未说明实验重复次数（如5次交叉验证），仅使用Mann-Whitney U检验，统计稳健性有限。
- **查询预算范围有限**：仅详细分析了0%-20%（图3A），未探索更高预算或极低预算（如1%）下的表现。
- **仅关注体积医学图像**：未验证在2D、病理图像或其他模态（如超声、X光）上的泛化性。
- **未知类别处理**：方法依赖对未知类别的能量和熵计算，但假设未知类别在训练时被模型采样（通过softmax输出层包含全部C_t类），实际中若模型从未见过未知类，其输出可靠性较低，可能存在偏差。
- **伪标签质量**：Target-refined Self-training依赖于首轮训练的模型生成伪标签，若初始查询样本代表性不足，伪标签错误可能累积。

（完）
