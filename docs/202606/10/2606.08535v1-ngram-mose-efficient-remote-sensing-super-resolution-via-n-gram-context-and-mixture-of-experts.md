---
title: "NGram-MoSE: Efficient Remote Sensing Super-Resolution via N-Gram Context and Mixture-of-Experts"
title_zh: NGram-MoSE：基于N-Gram上下文和混合专家模型的高效遥感超分辨率
authors: "Yun-Hsuan Huang, Trong-An Bui, Chih-Hung Chuang"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08535v1"
tags: ["query:rs-fm"]
score: 9.0
evidence: 面向遥感超分辨率的轻量Transformer和混合专家模型，处理分布外鲁棒性
tldr: "遥感超分辨率面临时空权衡与计算成本高的挑战。本文提出NGram-MoSE轻量级Transformer，通过N-Gram上下文注入增强局部连续性，并采用混合专家前馈设计稀疏激活参数。在OOD测试集上PSNR达31.68 dB，FLOPs降低14倍；下游滑坡分割mAP@50提升4.47%。该模型为资源受限的遥感系统提供了高效鲁棒的超分辨率方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08535v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 202, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08535v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 911, \"height\": 277, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08535v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 904, \"height\": 327, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08535v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1880, \"height\": 966, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08535v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1879, \"height\": 433, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08535v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1323, \"height\": 770, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08535v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 174, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08535v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 912, \"height\": 122, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08535v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 760, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08535v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 859, \"height\": 311, \"label\": \"Table\"}]"
motivation: 现有Transformer超分辨率模型计算昂贵且对分布外数据鲁棒性差，需要轻量高效同时保持纹理连续性的方案。
method: 提出NGram-MoSE架构，包含N-Gram上下文注入模块加强跨窗口一致性，以及稀疏激活的混合专家前馈层。
result: "在OOD测试集PSNR 31.68 dB，FLOPs减少14倍；滑坡分割任务mAP@50绝对提升4.47%。"
conclusion: NGram-MoSE在资源受限遥感场景下实现了高效且鲁棒的超分辨率，并展现良好的跨尺度一致性。
---

## 摘要
环境监测和灾害管理中的遥感应用常常受限于时空权衡：具有精细空间细节的图像获取频率较低，而时间上可获得的观测通常较粗糙。单图像超分辨率提供了一种无需改变采集计划就能增强粗糙图像的实用方法，但许多基于Transformer的SR模型计算成本高昂，并且可能对有限或地理上有偏的训练数据敏感，从而降低在分布外条件下的鲁棒性。本文提出了一种轻量级Transformer架构NGram-MoSE，旨在提高效率和纹理连续性。NGram-MoSE引入了N-Gram上下文注入以增强跨窗口局部一致性并减轻窗口边界伪影，并通过混合专家（MoE）前馈设计，通过稀疏激活扩展容量，同时不增加推理成本。在地理上不相关的OOD测试集上的实验表明，NGram-MoSE实现了31.68 dB的PSNR，同时相对于重型Transformer参考模型将FLOPs减少了14倍。在滑坡分割基准上的下游评估进一步表明，将退化输入恢复到检测器训练尺度可提高性能，与双三次上采样相比，mAP@50绝对增益达4.47%，并在尺度外推下表现出更强的跨尺度一致性。这些结果表明，NGram-MoSE为需要鲁棒泛化的资源受限遥感流程提供了一种有效的SR模块。

## Abstract
Remote sensing applications for environmental monitoring and disaster management are frequently constrained by a spatial--temporal trade-off: imagery with fine spatial detail is often acquired less frequently, whereas more temporally available observations are typically coarser. Single-image super-resolution provides a practical means to enhance coarse imagery without changing acquisition schedules, yet many Transformer-based SR models remain computationally expensive and can be sensitive to limited or geographically biased training data, which degrades robustness under out-of-distribution conditions. This paper presents NGram-MoSE, a lightweight Transformer architecture designed to improve both efficiency and texture continuity. NGram-MoSE introduces N-Gram Context Injection to strengthen cross-window local consistency and mitigate window-boundary artifacts, and incorporates a Mixture-of-Experts (MoE) feed-forward design to scale capacity through sparse activation without proportional growth in inference cost. Experiments on a geographically disjoint OOD test set show that NGram-MoSE achieves 31.68\,dB PSNR while reducing FLOPs by \(14\times\) relative to a heavyweight Transformer reference. Downstream evaluation on a landslide segmentation benchmark further demonstrates that restoring degraded inputs to the detector training scale improves performance, yielding a 4.47\% absolute gain in mAP@50 over bicubic upsampling, and exhibits stronger cross-scale consistency under scale extrapolation. These results indicate that NGram-MoSE provides an effective SR module for resource-constrained remote sensing pipelines requiring robust generalization.

---

## 论文详细总结（自动生成）

# 论文总结：NGram-MoSE: Efficient Remote Sensing Super-Resolution via N-Gram Context and Mixture-of-Experts

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：遥感图像在环境监测与灾害管理中面临空间-时间权衡：高空间分辨率图像获取频率低，而高时间分辨率图像通常空间分辨率粗。单图像超分辨率（SISR）可在不改变采集计划的前提下增强低分辨率图像，但现有SR模型（尤其是Transformer）存在两大障碍：
  - **计算成本高昂**：高参数量导致大面积处理困难，不适合资源受限场景。
  - **泛化能力差**：训练数据有限或地理分布有偏时，模型易过拟合，产生过锐化和高频伪影，在**分布外（OOD）** 条件下鲁棒性下降。
- **特定挑战**：窗口注意力机制在非重叠窗口边界产生不连续性，破坏连续地形纹理；密集前馈网络在扩大模型容量时计算成本线性增长。

## 2. 方法论：核心思想与关键技术细节

### 整体架构
- **NGram-MoSE框架**：轻量级Transformer，由浅层特征提取、深层特征提取（堆叠NSTB-MoSE块）、高分辨率重建三阶段组成。
  - 浅层特征提取：3×3卷积，将输入映射到嵌入空间。
  - 深层特征提取：L个NSTB-MoSE块逐步细化高频细节。
  - 重建头：子像素上采样（PixelShuffle）避免棋盘伪影，得到超分输出。

### 关键技术模块
- **N-Gram Swin Transformer块（NSTB-MoSE）**：解决窗口边界不连续与高计算成本。
  - **N-Gram上下文注入**（见图3）：
    - 用滑动窗口操作提取相邻n×n重叠邻域（n=2），形成N-Gram上下文。
    - 通过点卷积投影、轻量滑动窗口注意力聚合邻域依赖，与原始窗口标记相加，注入跨窗口局部连续性，无需扩大注意力范围。
    - 公式：\(\tilde{X}_{win} = X_{win} + X_{context}(X_{ngram})\)。
  - **混合专家（MoE）前馈网络**：
    - 使用门控函数G(·)将每个标记路由至Top-K个专家（稀疏激活），实现参数容量扩展而推理成本不比例增长。
    - 输出为选中专家的加权混合。
    - **Smart Merger**：为防止相邻标记路由不同专家而破坏空间一致性，在重建特征图后增加残差深度可分离卷积（DWConv），进行空间平滑。
    - 公式：\(y_{merged} = DWConv_{k×k}(y) + y\)。
  - **残差连接**：块输出 = \(\hat{X} + y_{merged}\)。

### 损失函数（加权融合目标）
- \(L_{total} = \lambda_1 L_{pix} + \lambda_2 L_{ssim} + \lambda_3 L_{ncc} + \lambda_4 L_{perceptual} + \lambda_5 L_{moe}\)
  - \(L_{pix}\)：ℓ1像素损失
  - \(L_{ssim}\)：SSIM结构损失
  - \(L_{ncc}\)：归一化互相关损失（全局统计一致性）
  - \(L_{perceptual}\)：VGG-19 ReLU5_4特征ℓ1损失（感知损失）
  - \(L_{moe}\)：路由器负载平衡正则项（抑制专家使用偏斜）

## 3. 实验设计

### 数据集与设置
- **训练数据**：5张大尺度高分辨率遥感场景（5000×5000像素，覆盖城乡山区），分割为约400个非重叠瓦片（480×480），训练时动态采样128×128子块。
- **评估数据集**：29个地理不相关场景（480×480），作为**分布外（OOD）测试集**。
- **任务**：×3超分辨率（高分辨率图像经×3降采样生成低分辨率输入）。
- **下游评估**：Landslide4Sense滑坡分割基准，使用YOLOv8-Seg检测器。

### 对比方法
- 双三次上采样（Bicubic）
- SwinIR（重型Transformer参考，Depth=36, Dim=180）
- NGram-MoSE（轻量架构，Depth=14, Dim=64）

### 评估指标
- 恢复质量：PSNR、SSIM
- 计算复杂度：参数量、FLOPs（输入128×128）
- 下游性能：mAP@50、mAP@50-95
- 两种下游设置：
  1. **恢复至检测器训练尺度**：低分辨率输入经SR恢复至原生分辨率，检测器在原生分辨率上训练。
  2. **尺度外推**：输入被上采样超过原生分辨率，分别用SwinIR和NGram-MoSE训练检测器，评估各自域（B3）及原生分辨率（B1）的表现。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量及训练时长。仅提及训练使用AdamW优化器（β1=0.9, β2=0.999），未提供具体硬件配置或训练时间。
- 模型参数量：NGram-MoSE为1.70M，SwinIR为11.89M；FLOPs：13.94G vs 195.34G。

## 5. 实验数量与充分性

- **实验数量**：
  - 主要恢复实验：表I给出OOD测试集PSNR/SSIM，表II给出复杂度对比。
  - 下游评估：表III（恢复至训练尺度）、表IV（尺度外推），并附定性图（图4-6）。
  - 无显式消融实验报告（例如去掉N-Gram或MoE的效果）。
- **充分性评估**：
  - **优点**：实验覆盖了OOD泛化、下游实际任务和跨尺度鲁棒性，评价维度较全面。
  - **不足**：
    - 仅有一个OOD测试集（29场景），缺乏多数据集验证（如不同卫星传感器、不同地理区域）。
    - 未与其他轻量级SR方法（如IMDN、N-Gram in Swin）对比，仅与重型SwinIR和双三次对比。
    - 消融研究缺失，难以直接证明N-Gram和MoE各自的贡献。
    - 未报告推理速度（FPS）或内存占用，仅FLOPs不能完全反映部署效率。
    - 训练数据的详细分布、数据增强等未充分说明，可能存在偏差风险。

## 6. 主要结论与发现

- **效率提升**：NGram-MoSE在OOD测试集上PSNR达31.68 dB（SSIM 0.9089），优于SwinIR（31.31 dB, 0.8946）和双三次（31.43 dB, 0.8873），同时FLOPs减少14倍（13.94G vs 195.34G）。
- **下游应用增益**：
  - 恢复至训练尺度时，mAP@50达30.11%，比双三次（25.64%）绝对提升4.47%，且高于SwinIR（25.61%）。
  - 尺度外推下，NGram-MoSE训练的检测器在原生分辨率（B1）上mAP@50为39.65%，显著高于SwinIR训练的34.23%，表明其生成图像更接近真实分布，跨尺度一致性好。
- **纹理连续性**：N-Gram上下文注入有效减轻窗口边界伪影，增强地形纹理连续性（定性图可见）。

## 7. 优点

- **轻量高效设计**：结合稀疏MoE和N-Gram注意力的轻量架构，在保持竞争力的同时大幅降低计算成本，适合资源受限场景。
- **针对性问题解决**：明确针对传统窗口Transformer的边界不连续性，提出N-Gram上下文注入，方法简单有效。
- **下游验证全面**：不仅关注像素级指标，还在实际滑坡分割任务上评估，并设计跨尺度外推实验验证鲁棒性，增强应用说服力。
- **损失函数设计合理**：融合像素、结构、感知、统计一致性和负载平衡的联合损失，有助于训练稳定和泛化。

## 8. 不足与局限

- **缺乏消融实验**：未单独验证N-Gram或MoE模块的贡献，难以归因性能提升来源。
- **对比方法有限**：仅与重型SwinIR和双三次比较，未与同等轻量级方法（如IMDN、N-Gram in Swin）或当前SOTA轻量SR模型对比。
- **实验多样性不足**：仅用一个OOD测试集（29场景），不同卫星、不同分辨率、不同退化类型未覆盖；下游仅针对滑坡分割，未验证其他遥感任务（如目标检测、语义分割等）。
- **硬件与运行时间信息缺失**：未提供训练/推理的具体GPU型号、训练时长、推理帧率，难以评估实际部署可行性。
- **训练数据偏倚风险**：仅使用5个大场景（可能来自相似地域），虽分区测试地理不相关，但训练域多样性有限，泛化至全球多地形仍需验证。
- **消融与基线的公平性**：SwinIR配置参数（Depth=36, Dim=180）远大于NGram-MoSE（14,64），虽说明为“heavyweight reference”，但非公平尺度下的对比；更合适的做法是控制参数量或FLOPs相近进行对比。
- **代码与可复现性**：论文未提供代码或预训练模型，降低结果可复现性。

（完）
