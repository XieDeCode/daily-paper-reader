---
title: How Useful is Causal Invariance for Domain Adaptation in Finite-Sample Settings?
title_zh: 因果不变性在有限样本设置下对域适应的用处有多大？
authors: "Julia Kostin, Kasra Jalaldoust, Elias Bareinboim, Samory Kpotufe, Fanny Yang"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12680v1"
tags: ["query:cross-domain"]
score: 9.0
evidence: 探索因果不变性在监督域适应中的实用性
tldr: 机器学习模型在目标分布偏移时性能下降。因果不变性可提供稳定预测器，但有限样本下的增益未知。本文在线性回归中利用因果知识得到候选预测器，通过自适应聚合方法选择最佳。结果显示，当目标风险间隔足够大时，可避免负迁移；间隔过小时无法加速。理论刻画了因果不变性的实际效用，并在真实因果基准上验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12680v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1190, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12680v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1553, \"height\": 710, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12680v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1245, \"height\": 236, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12680v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1245, \"height\": 238, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12680v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1749, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12680v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1750, \"height\": 649, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12680v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 700, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12680v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1215, \"height\": 253, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12680v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 695, \"height\": 589, \"label\": \"Figure\"}]"
motivation: 探究因果不变性在有限样本下能否提升监督领域自适应性能，并明确其有效条件。
method: 在线性回归中，利用完全或部分因果知识生成候选不变特征子集，设计自适应聚合方法选取目标风险最小的预测器。
result: 有限样本增益由目标风险间隔和源估计误差决定；间隔足够大时，自适应聚合可匹配最佳候选并避免负迁移。
conclusion: 因果不变性的有限样本优势依赖于结构偏移大小，理论在真实因果基准上得到验证。
---

## 摘要
机器学习模型在部署到与训练源分布不同的目标分布时，性能往往会下降。基于因果关系的域泛化近期研究表明，域之间的共享因果结构可以诱导出不变预测器，例如，在特征子集上的模型在结构化域偏移下具有稳定的风险。然而，这种总体层面的因果不变性在有限样本设置中能带来多大收益尚待探索。特别是，在实践中我们通常能获得少量带标签的目标样本，这种设置称为监督域适应（sDA）。本文探讨了（完全或部分）因果知识何时能显著改进监督域适应。作为第一步，我们研究线性回归，其中完全或部分因果知识指定了一组不变或可能不变的特征子集，每个子集产生一个基于源训练的候选预测器。我们推导了匹配的上界和下界，表明有限样本收益由候选预测器之间的目标风险间隔以及源有限样本估计误差共同决定。当这些间隔相对于$n_Q$足够大时，自适应聚合过程可以匹配最佳候选预测器，同时避免相对于仅用目标样本学习的负迁移。另一方面，当间隔过小时，没有算法能够可靠地利用候选集合获得更快的有限样本速率。我们进一步将这些间隔与线性SCM中的结构偏移幅度联系起来，并在真实因果基准上验证了理论。

## Abstract
Machine learning models often degrade when they are deployed on a target distribution that differs from the source distributions they were trained on. Recent work in causality-based domain generalization has shown how shared causal structure between domains can induce invariant predictors, e.g., models on a subset of features which have stable risk across structured domain shifts. However, the extent to which such population-level causal invariances can lead to gains in finite-sample settings remains underexplored. In particular, in practice we often have access to a few labeled target samples, a setting called supervised domain adaptation (sDA). In this paper, we explore when (full or partial) causal knowledge can provably improve supervised domain adaptation.   As a first step, we study linear regression, where full or partial causal knowledge specifies a collection of invariant or possibly invariant feature subsets, each yielding a source-trained candidate predictor. We derive matching upper and lower bounds showing that finite-sample gains are governed by the target-risk margins separating the candidates, together with the finite-source estimation error. When these margins are sufficiently large relative to $n_Q$, an adaptive aggregation procedure can match the best candidate predictor while avoiding negative transfer relative to target-only learning. On the other hand, when the margins are too small, no algorithm can reliably exploit the candidate collection to obtain faster finite-sample rates. We further connect these margins to structural shift magnitude in linear SCMs and validate the theory on real-world causal benchmarks.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：机器学习模型在目标分布与源分布不同时性能下降。因果域泛化利用因果结构诱导的不变预测器（在特征子集上模型风险稳定），但**有限样本设置下**（尤其是有少量带标签目标样本的监督域适应 sDA）这些不变性带来多大收益仍不清楚。
- **核心问题**：在有限样本下，完全或部分的因果知识能否提升监督域适应性能？何时能提升？何时不能？
- **背景**：已有因果域泛化工作大多关注总体层面（population level）的识别和风险保证，忽略有限样本效应；而域适应理论常关注无结构偏移，因果结构在有限样本下的增益未被严格刻画。

## 2. 方法论

### 核心思想
- 假设线性回归场景，通过**完全或部分因果知识**得到一个候选不变特征子集集合 `I`（例如 `I = {I1, I2, ...}`），每个子集对应一个在源数据上训练的线性预测器 `h_{I,P}`。
- 设计一种**自适应两阶段聚合算法**，利用少量目标样本 `n_Q` 在候选预测器中选出（或聚合得到）风险最低的模型，同时避免负迁移（即不差于仅用目标数据的基线）。

### 关键技术细节

1. **候选集合构造**：基于因果图知识（如充分不变性集合）、多源环境检验（如源不变性集合 `I_inv`）、或穷举所有子集（无因果关系时）。
2. **算法（Algorithm 1）**：
   - **Step 0**：将目标数据等分为两份 `D^1_Q, D^2_Q`；在源数据上训练每个特征子集对应的线性模型 `h_{I,P}`。
   - **Step 1（防止负迁移）**：在 `D^1_Q` 上计算目标线性模型 `h_Q` 及其置信带，只保留那些与 `h_Q` 距离不超过 `O(d/n_Q)` 的候选模型（避免输出比目标基线差的模型）。若无可保留，则直接返回 `h_Q`。
   - **Step 2（迭代聚合与精炼）**：在 `D^2_Q` 上对当前保留集合进行**模型选择聚合**（例如指数权重聚合、Q-聚合等），得到聚合预测器 `h_agg`；然后丢弃那些与 `h_agg` 距离超过 `O(log|H_acc|/n_Q)` 的候选；重复直到收敛，最后返回聚合器或距离最近的候选。
3. **理论结果**：
   - **上界**：在一定条件下（风险间隔 `∆_I` 足够大、源样本数 `n_P` 足够多），算法输出 `h̃` 的过剩风险不超过 `min{ O(d/n_Q), ε_I⋆ + ∆_max + r(n_P) }`，其中 `∆_max` 随 `n_Q` 以 `O(log|H_acc|/n_Q)` 速率衰减。
   - **下界**（匹配）：当 `∆ ≲ log|I|/n_Q` 时，任何算法都无法可靠地从候选集合中获得优于 `∆` 的加速（即使源数据无穷多）。表明样本复杂度 `n_Q ≳ 1/∆` 是必要的。
4. **与结构偏移的关联**：在线性 SCM 加性偏移场景下，大偏移 `t` 导致非不变模型的风险以 `Θ(t²)` 增长，从而产生大间隔 `∆ = Θ(t²)`，使得算法能快速识别最优不变模型。

## 3. 实验设计

### 数据集/场景
1. **Causal Chambers 光隧道传感器数据**（线性回归）：
   - 真实物理系统，有已知因果图。使用3个源域（参考+两个干预），目标域是未见过的干预域。候选集合基于多源不变性检验（ICP）生成。
2. **Causal Chambers 图像数据**（利用 CLIP 嵌入的线性探针）：
   - 预测图像亮度。源域3个，目标域为另一干预。候选模型为在不同源环境子集上训练的线性探针（ERM/IRM/groupDRO），共21个候选。
3. **RPE1 单细胞基因表达数据**：
   - 10个基因，随机选一个为Y，其他为X。源域包括观察域和3个干预域。候选集合为所有特征子集（512个），因为ICP无法接受任何子集（强混淆）。

### 对比方法
- **源模型**（Pooled ERM）、**目标模型**（仅在目标小样本上训练）
- **因果 DG 方法**：ICP 交集、Anchor Regression
- **朴素域适应**：候选集合上的 ERM（包含目标模型）、指数权重聚合
- **本文方法**：Algorithm 1

### 评估指标
- MAE（平均绝对误差）或 MSE，随目标样本量 `n_Q` 变化，多次运行计算中位数/均值及标准误。

## 4. 资源与算力

- 文中未明确说明大规模 GPU 使用。仅在附录 C.1.2 图像实验提及：“所有实验在 Apple M2 Max GPU 上运行不到1小时”。
- 总体算力要求很低，线性回归和线性探针训练开销小。

## 5. 实验数量与充分性

- **主要实验**：3个数据集/场景（光隧道线性、光隧道图像、基因表达）。每个场景展示一个主图（图6a、6b、图8b）。
- **运行次数**：光隧道线性50 runs，基因表达20 runs，图像5 runs。展示了标准误（或16th-84th百分位）。
- **消融研究**：未系统进行。仅在光隧道线性实验中对比了不同候选集合（充分不变性集合 vs 源不变性集合 vs 全子集），以及不同 `n_Q` 值。
- **充分性评价**：实验客观，覆盖了线性回归和特征表示场景，但缺少对源样本数 `n_P`、常数选择、不同聚合方法的深入消融。总体上证明了理论预测的趋势。

## 6. 主要结论与发现

1. **因果知识有价值**当且仅当候选模型之间的**目标风险间隔足够大**（相对于目标样本量 `n_Q`）。此时自适应聚合可匹配最佳候选模型并避免负迁移。
2. **必要性与紧性**：当间隔过小（`∆ ≲ log|I|/n_Q`），即使无穷多源数据，也无法实现加速。
3. **与结构偏移关联**：大结构偏移（如线性SCM中大加性偏移）产生大风险间隔，使因果知识尤其有用。
4. **优于朴素ERM和直接聚合**：本文方法达到 `O(log|H_acc|/n_Q)` 而非 `O(sqrt(log|I|/n_Q))` 或 `O(log|I|/n_Q)`，且在间隔足够大时可完全消除聚合余项。
5. 在真实基准上验证：当有足够目标样本时，本文方法在中等偏移下优于基线，在大偏移下收敛到最佳不变模型。

## 7. 优点

- **理论严谨**：给出了匹配的上下界，精确刻画了什么条件下因果知识能带来收益，什么条件下不能。
- **方法实用**：算法避免负迁移，不需事先知道最佳候选；包含自适应拒绝步骤，可处理大候选集合。
- **联系实际**：将理论间隔与线性SCM的结构偏移幅度直接挂钩，解释了“因果知识在大偏移下特别有用”这一直觉。
- **扩展性**：实验跨越纯线性回归和预训练特征上的线性探针，验证了思想不止于简单特征子集。

## 8. 不足与局限

- **线性假设**：理论和方法仅在线性回归下严格成立。非线性扩展、表示学习、神经网络等场景未覆盖。
- **依赖分布假设**：需要协方差正定、有界性（`∥X∥≤B_X`, `|Y|≤σ_Y`）等，在高维或重尾噪声下可能不成立。
- **算法参数敏感**：需要预先设定常数 `C1, C2`（控制拒绝阈值），虽然实验中采用自适应估计，但理论常数未知，实际调参可能影响性能。
- **实验范围有限**：仅三个真实场景，且其中两个规模较小（维度≤512）。缺乏大规模、高维、多种偏移类型的系统评估。
- **因果知识获取困难**：在强混淆或非线性下，ICP等无法产生有效候选集合（如基因数据中只能用穷举），此时即使有理论保证，实际收益可能有限。
- **未深入探讨源样本量影响**：实验主要固定 `n_P` 很大，未系统展示 `n_P` 有限时对间隔和算法的影响。

（完）
