---
title: Continuous biome representations from Earth observation embeddings
title_zh: 从地球观测嵌入中获取连续生物群系表示
authors: "Maxwell B. Joseph, Flávia De Souza Mendes, Dieu My T. Nguyen, Camile Sothe, Christopher B. Anderson"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11510v1"
tags: ["query:rs-fm"]
score: 9.0
evidence: 利用地球观测基础模型嵌入进行连续生物群系映射
tldr: 传统生物群落地图采用离散分类，掩盖了生态过渡带的渐变特征。本文基于Clay v1.5卫星图像嵌入训练线性分类器，将离散标签转换为softmax概率向量，得到连续生物群落表示。在巴西六个生物群落的实验中，该连续表示预测物种出现的AUC达0.618，优于离散标签的0.570，且增益主要来自概率的连续性而非标签重分配。该方法保留了分类含义，同时编码了离散地图抑制的渐变信息。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11510v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1651, \"height\": 527, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11510v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 949, \"height\": 744, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11510v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1639, \"height\": 1014, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11510v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1688, \"height\": 494, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11510v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 927, \"height\": 264, \"label\": \"Table\"}]"
motivation: 离散生物群落地图压缩了生态过渡带的连续变化，需要一种能保留渐变信息的连续表示方法。
method: 在Clay v1.5卫星图像嵌入上训练线性分类器，以softmax概率向量作为连续生物群落表示。
result: 连续表示在物种出现预测中平均AUC为0.618，高于离散标签的0.570；原始嵌入AUC为0.646，但连续表示恢复了大部分增益。
conclusion: 该方法为离散地图标签提供了概率替代方案，既保留分类意义又编码渐变信息，可改进生态建模。
---

## 摘要
生物群落随空间连续变化，然而生物群系地图通过施加分类边界压缩了这种变化，尤其是在生态过渡带，过渡性群落在生态上是独特的。地球观测基础模型利用密集嵌入编码光谱、空间和时间信息，能否将离散的生物群系地图转换为能更好捕捉生态变化的连续表示？在此，我们在Clay v1.5卫星图像嵌入上拟合一个线性分类器，从分类地图预测生物群系标签。softmax输出产生一个连续概率向量，其维度对应于命名的生物群系类别。我们使用六个巴西生物群系、130万个嵌入以及涵盖4,672种植物的10,015个预留森林调查样地来评估这种方法。连续生物群系表示在预测物种出现方面优于离散生物群系标签（10次空间交叉验证中，平均每个物种的AUC为0.618对0.570）。分解这一增益表明，改进来自分级概率输出的连续性，而非标签重新分配；该模式在距生物群系边界的所有距离上都成立。原始的1024维嵌入仍然是我们测试的最强预测因子（平均AUC 0.646对0.618），但连续表示恢复了嵌入相对于离散标签的大部分增益。这种简单方法为分类地图标签提供了概率替代，既保留了其含义，又编码了离散地图所抑制的分级变化。

## Abstract
Biotic communities vary continuously across space, yet biome maps impose categorical boundaries that compress this variation, particularly at ecotones where transitional communities are ecologically distinct. Could Earth observation (EO) foundation models, which encode spectral, spatial, and temporal information with dense embeddings, convert discrete biome maps into continuous representations that better capture ecological variation? Here, we fit a linear classifier on Clay v1.5 satellite image embeddings to predict biome labels from a categorical map. The softmax output yields a continuous probability vector whose dimensions correspond to named biome classes. We evaluate this approach using six Brazilian biomes, 1.3 million embeddings, and 10,015 withheld forest inventory plots spanning 4,672 plant species. The continuous biome representation outperforms discrete biome labels for predicting species occurrence (mean per-species AUC 0.618 vs. 0.570 across 10 spatial cross-validation folds). Decomposing this gain shows that continuity in the graded probability output, rather than label reassignment, accounts for the improvement; the pattern holds across all distances from biome boundaries. The raw 1024-dimensional embedding remains the strongest predictor we tested (mean AUC 0.646 vs. 0.618), but the continuous representation recovers most of the embedding's gain over discrete labels. This simple approach provides a probabilistic replacement for categorical map labels, preserving their meaning while encoding graded variation that discrete maps suppress.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：生物群系（biome）在空间上连续变化，但传统生物群系地图采用离散分类，强制施加边界，压缩了渐变信息，特别是在**生态过渡带（ecotone）**，过渡群落在生态上往往是独特的。如何将离散地图转换为能更好捕捉生态渐变信息的连续表示？
- **研究动机**：地球观测（EO）基础模型（如 Clay v1.5）通过密集嵌入（embedding）编码光谱、空间和时间信息。如果能从这些嵌入中提取与生态梯度相关的结构，就可以将离散地图“提升”为连续表示，其维度对应已有类别，既保留分类语义又表达过渡地带的模糊性。
- **整体含义**：该方法提供了一种**概率替代方案**，可取代离散地图标签，在生态建模、生物多样性保护及政策实施（如巴西森林法典）中具有实用价值。

## 2. 方法论

### 核心思想
- **线性探测（Linear Probing）**：在预训练的 EO 嵌入上拟合一个线性分类器（多元逻辑回归），以预测离散的生物群系标签，并将分类器的 softmax 输出作为连续表示。

### 关键技术细节
1. **输入数据**：
   - 嵌入：Clay v1.5（掩码自编码器，基于 Sentinel-2 10波段，输出 1024 维向量），覆盖巴西的约 130 万个 tile（每个约 256×256 像素，对应 2.56 km 地面范围）。
   - 标签：巴西 IBGE 生物群系地图（6 类：Amazônia, Caatinga, Cerrado, Mata Atlântica, Pampa, Pantanal），通过多数面积规则赋予每个 tile。
2. **模型训练**：
   - 对嵌入向量进行标准化（零均值、单位方差）。
   - 使用 scikit-learn 的 `LogisticRegression`，默认 L2 正则化（C=1），saga 求解器。
   - 训练一次（无需空间外推，因地图覆盖整个研究区）。
3. **连续表示**：
   - Softmax 输出为 **K 维概率向量**（K=6），每个元素对应 I 类生物群系概率。
   - 可计算归一化熵 \( H_{\text{norm}} = -\frac{\sum p_k \ln p_k}{\ln K} \) 作为不确定性度量（0 表示单一类别，1 表示均匀分布）。

### 评估框架
- 对每个植物物种（共 4,672 种），使用三种 **6 维表示** 和一种 **1024 维嵌入** 分别拟合二分类逻辑回归，预测物种出现/不存在。
- **三种 6 维表示**：地图标签（one-hot）、预测标签（argmax 后的 one-hot）、连续表示（softmax 概率）。
- **参考**：原始 1024 维嵌入。
- **性能度量**：每个物种的 AUC，10 折空间交叉验证（按样地坐标聚类划分）。

## 3. 实验设计

### 数据集与场景
- **EO 嵌入数据**：Clay v1.5 预计算嵌入（~1.3M 个 tile，2024 年 6 月），来自 Planet Labs 分发。
- **地图标签**：IBGE 生物群系地图（2004 年出版，2019 年修订）。
- **评估数据**：巴西国家森林调查（NFI）10,015 个样地，覆盖 5 个生物群系（Pantanal 无样地），含 4,672 种植物，采集时间 2011–2024 年。

### Benchmark 与对比方法
- 对比 `地图标签`、`预测标签`、`连续表示` 以及 `原始 1024 维嵌入`。
- 对每个表示，独立调参（L2 正则化强度 C），使用 80/20 内部验证集选择最优 C。
- 按距生物群系边界的距离四分位分层，分解 ΔAUC 为标签重分配效应和连续性效应。

### 实验设置
- 10 折空间交叉验证，避免空间自相关。
- 每个折叠内，对每个可评估物种（在训练和测试集中都有出现和缺失）计算 AUC，再取平均。
- 分层分析中，保留三种表示均能评估的物种，确保公平比较。

## 4. 资源与算力
- **论文未明确说明**使用的 GPU 型号、数量或训练时长。
- 实验主要依赖 scikit-learn 的多元逻辑回归（L2 正则化），对 130 万样本训练线性分类器，计算开销很低（可在 CPU 上快速完成）。
- 未提及大规模深度学习训练，因而算力需求较小。

## 5. 实验数量与充分性

- **实验数量**：
  - 主要比较试验（10 折 × 4 种表示 × 每个物种的模型）。
  - 分层分析（4 个距离四分位 × 10 折 × 三种表示）。
  - 消融成分分析（标签重分配 vs 连续性）。
- **充分性与公平性**：
  - 空间交叉验证确保样地空间独立，减少伪复制。
  - 内部调参避免数据泄漏。
  - 物种只纳入同时满足训练和测试出现/缺失的，确保 AUC 计算有效。
  - 分解分析清晰隔离了连续性的独立贡献。
- **可能的不足**：仅使用巴西案例，且评估样地分布不均（Pantanal 缺失）；未在其他地理区域或基础模型上验证。

## 6. 主要结论与发现

- **连续表示显著优于离散标签**：平均每物种 AUC 0.618（连续） vs 0.570（地图标签），在所有 10 折中均一致。
- **增益来源于连续性而非标签重分配**：`连续表示 − 预测标签` 的 ΔAUC > 0，而 `预测标签 − 地图标签` 接近零。
- **原始 1024 维嵌入最强**（AUC 0.646），但连续表示恢复了其相对于离散标签大部分增益（仅低 0.028 AUC）。
- 优势在距边界所有距离上均成立，说明连续编码了全局生态梯度，而不只是边界附近。
- 高熵区域可标识 EO 信号与多个生物群系一致的过渡带，对法律合规（如巴西森林法典不同生物群系保护比例差异）具有实践意义。

## 7. 优点

- **简洁高效**：仅需线性分类器，且输入（预计算嵌入+分类地图）全球已有，易于迁移。
- **语义可解释**：连续表示的每个维度直接对应命名生物群系，保留了生态学意义，便于政策应用。
- **可即插即用**：可作为离散地图的“drop-in”替代特征，嵌入任何下游模型。
- **实验设计严谨**：空间交叉验证、分层分析、消融分解等，区分了连续性效应与标签错误效应。
- **实用价值**：在巴西森林法典争议区域（如 Cerrado–Amazonia 过渡带）可预警分类不确定性，而非提供错误确信。

## 8. 不足与局限

- **时间不匹配**：EO 嵌入来自 2024 年 6 月，而 NFI 样地调查时间跨度为 2011–2024 年，期间土地利用变化可能导致嵌入与实测条件不一致，且该偏差可能使连续表示的真实优势被低估。
- **地理覆盖有限**：仅测试巴西，且神农达（Pantanal）无评估样地。不同基础模型、不同分类系统、不同纬度带的表现未知。
- **绝对 AUC 偏低**（最高 0.646），说明仅用一个生物群系概率向量（或嵌入）无法完全预测物种分布；模型缺乏其他环境协变量。
- **线性假设**：仅用线性分类器，可能无法捕捉嵌入中的非线性生态模式。
- **单一基础模型**：仅用 Clay v1.5，未对比其他 EO 基础模型（如 AlphaEarth 等）。
- **嵌套分类未处理**：未扩展到生态区（ecoregion）等嵌套分级系统，也未处理有序类别（如 Holdridge 生命带）。

（完）
