---
title: Depth-Regularized JEPA World Models Learn More Transferable Representations from Real Outdoor Robot Data
title_zh: 深度正则化的JEPA世界模型从真实室外机器人数据中学习更可迁移的表征
authors: Usman M. Khan
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16314v1"
tags: ["query:world-models"]
score: 8.0
evidence: 深度正则化的JEPA世界模型用于户外机器人数据
tldr: "面向户外机器人视频数据，世界模型面临视觉复杂性与泛化挑战。本文提出深度正则化JEPA框架，结合深度监督与各向同性潜在正则化器(SIGReg)，在训练时引入过参数化以增强模型容量。在真实农业机器人数据上，相比基线LeWM，视觉里程计探测误差降低33%，惊奇分数分离度在域内和域外均显著提升，多步展开保真度随步长增加增益更大。结果表明轻量级几何先验能增强紧凑世界模型的表示可迁移性，且不增加推理开销。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1622, \"height\": 1150}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1707, \"height\": 679}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 1449}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 717}]"
motivation: 从真实户外机器人视频数据学习鲁棒潜动力学面临视觉复杂性与泛化挑战。
method: 引入深度作为几何先验，结合各向同性潜在正则化器SIGReg，训练时使用过参数化。
result: "视觉里程计探测误差降低33%，惊奇分数分离度与多步展开保真度显著提升。"
conclusion: 轻量级训练时几何先验增强紧凑JEPA世界模型的表示可迁移性，且无推理开销。
---

## 摘要
世界模型，尤其是基于JEPA架构的，已被证明能学习各种环境的鲁棒动力学。然而，从视觉复杂的真实世界数据中学习仍然是一个挑战，尤其是在不可预测的室外环境中。我们引入深度作为几何先验，在训练中直接从机器人视频数据学习更鲁棒的潜在动力学并处理视觉复杂性。这结合了深度监督与各向同性诱导的潜在正则化器（SIGReg），最大化任务无关的潜在多样性，同时约束多样性的组织方式，联合目标针对与场景几何一致的最高熵表示。为了在不增加推理时间的情况下满足这种更高的复杂性，我们还增加了仅训练时的过参数化。在真实农业机器人的视频上训练一个18M参数的模型，我们使用冻结表征的视觉里程计探针、基于预测器的意外检测和多步潜在展开保真度进行评估。与基线LeWM相比，我们的方法将视觉里程计探针误差降低了33%，显著提高了域内和域外TartanGround基准上的意外分数分离度，并改善了域迁移下的多步展开保真度，且增益随着展开时间步长增加。值得注意的是，我们在物理理解（如光照和阴影）方面的意外分数分离度也有所提升，而这些与3D几何不直接相关。这些结果表明，轻量级的训练时几何先验使得紧凑的JEPA世界模型在真实室外数据上更具实用性和可迁移性，且不增加推理开销。我们的工作表明，深度作为物理基础的先验可以增强世界模型在各种任务上的泛化能力。

## Abstract
World models, especially based on JEPA architectures, have been shown to learn robust dynamics of various environments. However, learning from visually complex real-world data remains a challenge, especially in unpredictable outdoor environments. We introduce depth as a geometric prior during training in learning more robust latent dynamics directly from robot video data and handling visual complexity. This combines depth supervision with an isotropy-inducing latent regularizer (SIGReg), maximizing task-agnostic latent diversity while constraining how that diversity is organized, with the combined objective targeting the highest-entropy representation consistent with scene geometry. To satisfy this greater complexity without increasing inference time, we also add training-only overparameterization. Training an 18M-parameter model on video from a real agricultural robot, we evaluate with frozen-representation visual odometry probes, predictor-based surprise detection, and multi-step latent rollout fidelity. Compared to the baseline LeWM, our method lowers visual odometry probe error by 33%, substantially increases surprise-score separation both in-domain and on the out-of-domain TartanGround benchmark, and improves multi-step rollout fidelity under domain shift, with gains that grow with rollout horizon. Notably, we also see improvements in surprise-score separation on physics understanding that is not directly tied to 3D geometry, such as lighting and shadows. These results show that a lightweight training-time geometric prior makes a compact JEPA world model more useful and more transferable on real outdoor data with strong underlying representations, without adding inference overhead. Our work suggests that depth as a physically grounded prior can enhance world model generalization on a variety of tasks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：从真实户外机器人视频数据（尤其是农业机器人）学习鲁棒的世界模型面临挑战——室外环境光照变化、阴影、运动模糊、植被杂乱、地形变化等视觉复杂性高。现有的JEPA世界模型（如LeWorldModel, LeWM）在模拟环境中表现良好，但在真实室外数据上性能下降，且容易陷入表征坍塌或学习到与任务无关的噪声信息。
- **整体含义**：本文旨在通过引入**深度作为几何先验**，结合**各向同性潜在正则化**，使紧凑的JEPA世界模型能够从真实户外数据中学到更稳定、更可迁移的潜在表征，从而提升对物理动态的理解和泛化能力，且不增加推理时的计算开销。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：在训练过程中加入**深度对齐损失**，将RGB编码器的嵌入与配对深度相机嵌入（带梯度停止）进行对齐，使潜在空间偏向几何结构；同时保留LeWM的SIGReg正则化（最大化任务无关多样性），构成联合目标：**最高熵且与场景几何一致的潜在分布**。此外，为补偿额外的约束，引入**训练时预测器过参数化**（低秩RepLinear分支），训练后可代数合并，不影响推理模型。
- **关键技术细节**：
  - 基础模型：LeWorldModel（LeWM），ViT-Tiny编码器（嵌入维度192），6层预测器，历史大小3，预测未来1步。
  - 训练损失：`L = L_pred + 0.09 * L_sigreg + 0.1 * L_depth`。其中 `L_depth = 1 - cos(RGB嵌入，sg(depth嵌入))`；深度分支使用stop-gradient。
  - 过参数化：在预测器的MLP上/下投影添加秩16的低秩线性分支，训练后合并到基础权重中（结构重参数化）。
  - 深度数据：截断到1米，复制为3通道灰度图像，与RGB共享相同编码器-投影器。
  - 推理时：仅使用RGB输入，深度分支和过参数化分支均移除，模型结构与基线LeWM完全相同，参数数量不变。

## 3. 实验设计：数据集、benchmark、对比方法
- **训练数据**：约3.1万帧，来自Aigen Element平台真实农业机器人前向行驶立体相机数据，85个episodes。验证集：3,587帧（9个episodes）。
- **域外（OOD）基准**：TartanGround（地面机器人模拟数据集），15,608帧，6个episodes，包含多个环境，同时改变机器人平台、环境和模拟-真实差距。
- **评估任务**：
  1. **视觉里程计线性探针（VO Probe）**：从冻结嵌入回归自运动（ego-motion），MSE衡量。
  2. **基于预测器的意外检测（Surprise Detection）**：对合成篡改片段（瞬移、时间反转、亮度跳变、颜色交换）计算预测误差，评估分离度（perturbed surprise - plausible surprise）。
  3. **多步潜在展开保真度（Latent Rollout Fidelity）**：给定真实动作序列，自回归展开h步，计算目标相似度和轨迹相似度（余弦相似度），h=10和h=20。
- **对比方法**：
  - 基线：纯RGB LeWM（无深度正则化）。
  - 参考：冻结DINOv2编码器（仅用于VO探针和展开相似度，作为单帧表征参考）。
  - 本文方法：深度正则化+过参数化的LeWM（Depth-Reg LeWM）。
- **公平性**：两个LeWM变体采用相同优化器、学习率调度、批量配置、训练100个epoch。深度正则化模型仅在训练时额外增加深度损失和过参数化分支，推理时架构一致。

## 4. 资源与算力
- **文中未明确说明**具体GPU型号、数量以及训练时长。仅提及模型参数量为18M，训练100个epoch。未提供计算资源细节（如GPU小时数）。因此无法评估算力消耗。

## 5. 实验数量与充分性
- **主要实验数量**：论文包含三类评估（VO探针、意外检测、多步展开），每类在域内验证集和域外TartanGround上报告指标，表1汇总了主要结果。此外还进行了潜在几何对比分析（PCA子空间重叠、分类分离度等，图3）。
- **充分性评价**：
  - **积极方面**：评估任务覆盖了表征质量（探针）、动态预测置信度（意外检测）和长期预测能力（展开），且包含域外泛化。实验相对全面。
  - **不足**：
    - **缺少消融实验**：未单独消融深度正则化项和过参数化的各自贡献（文中提到留给未来工作）。
    - **基线窄**：仅与LeWM及其DINOv2参考对比，未与其他JEPA变体（如V-JEPA、DINO-WM）或更大规模模型比较。
    - **评价指标局限性**：展开相似度可能偏向平滑潜在空间，跨模型比较需谨慎；意外检测仅考虑分类准确率和分离度，未分析具体误差分布。
    - **数据规模小**：仅约3.1万帧，来自单一机器人平台和农业场景，泛化性有限。
    - **离线评估**：所有实验均为离线指标，未涉及闭环机器人规划或控制。

## 6. 主要结论与发现
- **VO探针**：深度正则化模型MSE从0.0022降至0.0015（相对降低33%），优于DINOv2（0.0029），表明几何先验使潜在表征蕴含更丰富的运动信息。
- **意外检测**：域内分离度从0.067提升至0.120；域外TartanGround上分离度从0.035提升至0.099（提升约3倍），且准确率也从87.5%提升至89.2%。特别地，在非几何扰动（亮度跳变、颜色交换）上也有显著增益，表明深度正则化使模型形成更稳健的物理预期。
- **多步展开**：域内深度模型略低于基线（h=10目标相似度0.890 vs 0.903），但域外反超且增益随步长扩大（h=20：0.537 vs 0.458），说明深度正则化提升了跨域泛化能力和长时预测鲁棒性。
- **几何分析**：深度正则化模型的域间潜在子空间重叠更高、分类分离度更低（更具跨域一致性），表明几何先验促进了更可迁移的潜在结构。
- **总体结论**：训练时轻量级深度对齐和过参数化，使紧凑JEPA世界模型在真实户外数据上表征更优、预测更准、泛化更强，且推理零开销。

## 7. 优点
- **方法简洁有效**：仅通过一个对齐损失和训练时过参数化，无需复杂训练技巧或大模型，在紧凑架构（18M参数）上取得显著提升。
- **推理无成本**：深度分支和过参数化分支均为训练专属，推理时无需深度输入或额外参数，与基线完全一致。
- **实验设计全面**：覆盖表征质量、预测置信度、长期动态预测和域外泛化，且包含潜在几何可视化分析。
- **新颖性**：首次在真实户外农业机器人数据上展示紧凑JEPA世界模型的端到端训练；将深度作为动态几何先验与多样性正则化结合，展示了物理先验对非几何任务（光照）的间接帮助。
- **可复现性强**：基于开源LeWM框架，训练数据规模小，方法易于实现。

## 8. 不足与局限
- **未消融各组件贡献**：深度正则化和过参数化耦合，无法确定每部分独立贡献。过参数化可能抵消了深度项带来的预测器瓶颈压力，但未做控制实验。
- **评估离线**：所有指标均为离线潜在空间指标，未在真实机器人上验证闭环性能或规划效果。
- **数据规模小且单一**：仅约3.1万帧，单一平台（Aigen Element）和单一领域（农业），可能无法推广到更广泛场景。
- **基线窄**：仅对比LeWM和DINOv2探针，未与更主流的JEPA模型（如V-JEPA、DINO-WM）或更大规模（V-JEPA 2）比较，缺乏SOTA参考。
- **域外基准局限**：TartanGround是模拟环境，与真实域外仍有差距；且分离度分析中未报告所有扰动的分离度细节。
- **潜在几何分析的解释力有限**：域间分离度降低和PCA重叠升高仅体现几何一致性，但未证明与下游任务直接相关。
- **未讨论深度质量影响**：训练时深度从立体相机获得，存在噪声和截断（1米），可能限制几何先验的上限。

（完）
