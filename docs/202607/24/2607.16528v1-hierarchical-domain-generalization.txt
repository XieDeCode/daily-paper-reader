Title: Hierarchical Domain Generalization

URL Source: https://arxiv.org/pdf/2607.16528v1

Published Time: Tue, 21 Jul 2026 00:17:01 GMT

Number of Pages: 24

Markdown Content:
# Hierarchical Domain Generalization 

Chenxiao Yang 1, Zhiyuan Li 1, Shai Ben-David 2, Nathan Srebro 11Toyota Technological Institute at Chicago 

> 2

University of Waterloo 

{chenxiao,zhiyuanli,nati }@ttic.edu, shai@uwaterloo.ca 

# Abstract 

We study hierarchical domain generalization as a problem of extrapolation from finite observed regions to an entire instance space, replacing i.i.d. sampling with arbitrary domain hierarchies. We show that the central obstruction is not only the complexity of the hypothesis class, but the train/test domain partition through which evidence is revealed. In particular, no matter how small the class or how large the training size, some partition makes generalization fail for some target. These results suggest that modern generalization theory must treat domain structure as a first-class object. 

# 1 Introduction 

The concept of generalization in machine learning has been shaped by the seminal work of Vapnik and Chervonenkis on statistical generalization theory [Vapnik and Chervonenkis, 1971] and by Valiant’s introduction of the PAC model for binary classification [Valiant, 1984]. In these setups, training data is drawn i.i.d. from some unknown data distribution D, and the learner’s output is evaluated on the same D. A successful learner is required to achieve low expected error on the distribution that generated the training data (or related data distributions in more recent work on domain adaptation learning, e.g., Ben-David et al. [2006]). This statistical framework has shaped the field’s conception of “what it means to generalize” for four decades. By contrast, the large-language-model era asks for a kind of generalization that this classical frame-work does not capture: trained on a very large fixed domain of inputs (e.g., the web), the learner should reason — recover the underlying rule — and produce correct outputs on relevant inputs outside its training domain. However, a model trained on five-digit addition may fit its training data perfectly yet fail on six-digit numbers, as it often does in practice [Anil et al., 2022, Lee et al., 2024]. In that case, it has matched a pattern in the data, not the rule that adds arbitrarily long numbers. It fails to reason . What the classical theory calls generalization is, in the sense most scientists and philosophers use the word, interpolation (biased or regulated by some prior knowledge). This is increasingly insufficient to capture many learning tasks of practical interest. Reasoning demands extrapolation, or out-of-domain generalization: from one kind of input to a different kind that shares some controlled (deterministic) similarity with the training data. We formalize this form of generalization as exact identification along a domain hierarchy. Instead of measuring expected error under a data-generating distribution, we require the learner to recover the target function on the entire instance space Ω. Instead of assuming an i.i.d. observation model, we let the observable region grow through an externally specified hierarchy ¯X = ( Xn)n≥0 of finite cumulative domains, with ∅ = X0 ⊊ X1 ⊊ X2 ⊊ · · · and S 

> n≥1

Xn = Ω (so Ω is countable). At level n, the learner observes the labeled training set {(x, f (x)) : x ∈ X n} and is evaluated on all of Ω; equivalently, it must extrapolate from the observed region Xn to the unobserved complement 

Ω \ X n. The only way to achieve this is to exactly identify f .

> Preprint. arXiv:2607.16528v1 [cs.LG] 17 Jul 2026

This captures a common desideratum of modern reasoning systems: train on shorter inputs and generalize to longer ones [Anil et al., 2022, Lee et al., 2024, Zhou et al., 2024] (a.k.a. length generalization), train on simpler instances and generalize to harder ones [Bengio et al., 2009], and train on bounded-horizon tasks and generalize to tasks with longer horizons [Abbe et al., 2023, Yang et al., 2026b]. In the point-by-point case, where each increment is a singleton Xn \ X n−1 = {xn}

and hence Xn = {x1, . . . , x n}, the model recovers the presentation model of Gold’s identification-in-the-limit paradigm [Gold, 1967]. The central question is: given a hierarchy ¯X , how many levels N must a learner observe to recover the target f ∈ H on the entire domain Ω? This question admits answers of varying strength: the required level may depend on both the target and the hierarchy, it may be common to all targets once the hierarchy is fixed, or it may be required to work uniformly over both targets and hierarchies. The answer depends on how uniformly the level must be chosen. If the level may depend on both the target and the hierarchy, countability of H is exactly the right condition. If the hierarchy is fixed but the same level must work for every target, the threshold becomes finiteness of H. But if the level must be chosen uniformly over the hierarchy itself, the situation changes completely: for every nontrivial hypothesis class, such a guarantee is possible only in the finite-domain case (Theorems 1, 5 and 8). Length generalization gives a concrete fixed-hierarchy instance of this question. The hierarchy is string length: training on all strings up to length N and testing on longer strings asks whether that observed prefix determines the target rule. Since relevant classes such as finitely encoded Transformer families are infinite, we stratify them by complexity: for each bound s, we ask how many lengths are needed to identify every target of complexity at most s. Existing non-asymptotic length-generalization results estimate this sublevel domain complexity for particular representation classes and complexity scales [Chen et al., 2025, Yang et al., 2026a]; qualitative and approximate variants fit the same fixed-hierarchy picture [Huang et al., 2025, Izzo et al., 2026]. For a fixed hierarchy, such bounds have a clear interpretation: they say how far one must train along that hierarchy before the target is determined. But this still leaves open whether the bound reflects a property of the model class itself, or only of the particular hierarchy used to train and test. Our no-free-lunch result shows that the latter dependence is unavoidable. Even with the hypothesis class and complexity scale fixed, another hierarchy can make the finite-slice bounds grow arbitrarily fast (Theorem 12 and Corollary 13). Thus a length-generalization bound is a bound for the length hierarchy, not a hierarchy-free guarantee for arbitrary domain shifts. Classical no-free-lunch theorems warn that an unrestricted hypothesis class admits no uniform learning guarantee [Shalev-Shwartz and Ben-David, 2014]. Here the obstruction comes from a second object that classical theory keeps offstage: the hierarchy that determines what is observed before what. A domain-generalization guarantee is therefore a property of the pair (H, ¯X ), not of H

alone. We further extend the framework and connect it to several neighboring learning settings, including online learning and generation in the limit [Kleinberg and Mullainathan, 2024]. Online learning counts mistakes rather than identifying levels, while generation in the limit asks for a weaker output than exact recovery. Furthermore, allowing error, or replacing hierarchies by distributional train/test sampling, does not by itself remove the difficulty: a guarantee still requires structural assumptions relating the observed region to the region on which the learner is evaluated. However, once we change the data model to positive-only presentations, uniform identification can hold for infinite classes and is controlled by overlap among positive regions. 

Organization. The rest of the paper is organized as follows. Sections 2 and 3 introduce the hierarchy-based learning model and the main characterization theorem. Sections 4 and 5 develop the fixed-hierarchy quantitative viewpoint, with length generalization as the central example. Section 6 shows why these bounds cannot be made independent of the hierarchy. Section 7 discusses extensions to related learning settings. 22 Hierarchical Domain Generalization 

2.1 Setup 

Let Ω be a countably infinite instance space and let H ⊆ { 0, 1}Ω be a binary hypothesis class. 1

We work in the realizable setting with deterministic labeling: an unknown target f ∈ H generates labels f (x) ∈ { 0, 1} for x ∈ Ω. For any subset X ⊆ Ω, write f |X for the labeled training set 

{(x, f (x)) : x ∈ X } .To model out-of-domain generalization, we must specify how the observable part of Ω grows. The primitive object is not a sampling distribution, but a predetermined sequence of finite cumulative regions whose labels become available to the learner. 

Definition 1 (Domain hierarchy) . A hierarchy on Ω is a telescopic exhaustion ¯X = ( Xn)n≥0 by finite cumulative observed regions satisfying 

∅ = X0 ⊊ X1 ⊊ X2 ⊊ · · · , |X n| < ∞ for every n, [ 

> n≥1

Xn = Ω . (1) For n ≥ 1, write ∆n := Xn \ X n−1 for the newly revealed increment at level n. The definition is intentionally agnostic about how the regions are ordered. One common source is a sequence of externally specified domains, such as different populations, demographic subgroups, or intervention-based environments. Another source orders Ω by an intrinsic property of the inputs, such as length or difficulty, placing simpler inputs in early levels and harder ones later, as in length generalization and curriculum learning. When each increment is a singleton, say ∆n = {xn}, equivalently 

Xn = {x1, . . . , x n}, the hierarchy recovers Gold’s identification-in-the-limit setting [Gold, 1967]. 

Hierarchical Domain Generalization. Once a hierarchy is fixed, level n splits Ω into the observed region Xn, from which the learner receives the labeled training set f |Xn , and the unobserved complement Ω \ X n, which is out of domain relative to that level. The learner is evaluated on the entire Ω, so success requires extrapolation from the observed region to the unseen part of the hierarchy. This in-domain/out-of-domain split is determined by ¯X alone, without any distributional assumptions. We idealize each observed level as fully labeled: at level n, the learner has access to all labels on 

Xn. Thus the question is not whether the learner can fit the observed region, but whether that region already determines the target beyond it. 

Definition 2 (Learner) . A learning algorithm is a map A : S 

> X ⊆ Ω,|X | <∞

{0, 1}X → { 0, 1}Ω, which takes the labeled training set f |X as input and returns a binary predictor A(f |X ). The learner may be chosen with knowledge of the hypothesis class H.

Because the training set is indexed by a set rather than an ordered sequence, A does not see the order in which sample points are revealed — a difference from online learning, where the within-level order can itself carry information. 

Definition 3 (Domain complexity) . Given a learner A, a target f ∈ H , and a hierarchy ¯X , the 

domain complexity of A on f along ¯X is 

N ¯X 

> A

(f ) := min i ∈ N≥1 : ∀n ≥ i, A (f |Xn ) = f , (2) 

with value ∞ if no such finite i exists. 

Thus N ¯X 

> A

(f ) is the first level after which, for every n ≥ N ¯X 

> A

(f ), training on the observed region Xn

yields a hypothesis correct on all of Ω, equivalently on every later and currently unobserved part of the hierarchy. 

2.2 Levels of Learning Goals 

We distinguish the possible forms of hierarchical domain generalization by the order of the quantifiers over the learner, the target, the hierarchy, and the identifying level. The first two criteria are hierarchy-dependent: in A1 , the identifying level may depend on both the target and the hierarchy; in A2 , it may still depend on the hierarchy, but must be common to all targets in the class.    

> 1Finite domains are used only as degenerate comparison cases, with the convention that the hierarchy collapses to Xn= Ω
> for all large enough n. The binary range is for concreteness; all characterizations extend to any finite label space.

3A1: non-uniform                                                                                  

> ∃A∀f∀¯X ∃ N:N¯X
> A(f)≤N
> A2: target-uniform
> ∃A∀¯X ∃ N∀f:N¯X
> A(f)≤N
> A4: uniform
> ∃A∃N∀f∀¯X:N¯X
> A(f)≤N
> fixed hierarchy version
> ∃A∃¯X ∀ f∃N:N¯X
> A(f)≤N
> fixed hierarchy common bound
> ∃A∃¯X ∃ N∀f:N¯X
> A(f)≤N
> A3: domain-uniform
> ∃A∀f∃N∀¯X:N¯X
> A(f)≤N
> target-chosen hierarchy version
> ∃A∀f∃¯X ∃ N:N¯X
> A(f)≤N
> structural condition
> Hcountable
> structural condition
> Hfinite
> trivial case
> |Ω|<∞
> Definition 4 Definition 5
> Definition 6
> Definition 7
> Theorem 4 Theorem 7 Theorem 8
> Theorem 1 Theorem 5 Theorem 8

Figure 1: Key characterization. Exact characterization of when a hypothesis class allows hierarchical domain generalization at levels that may depend on the target and hierarchy ( A1 ), depend on the hierarchy but are uniform over targets ( A2 ), depend on the target but are uniform over hierarchies (A3 ), or are uniform over both targets and hierarchies ( A4 ). Throughout the figure, f ∈ H , ¯X ranges over hierarchies, and |H| > 2. Gray dashed arrows are formal implications, black double arrows mark substantive equivalences, and transitive arrows are omitted. 

Definition 4 (A1 : Non-uniform identifiability ). H is non-uniformly identifiable (A1 ) if there exists a learning algorithm A with the following property: for every target f ∈ H and every hierarchy ¯X ,there exists a finite N such that N ¯X 

> A

(f ) ≤ N .

A2 keeps the dependence on the hierarchy but requires one level to work for all targets. 

Definition 5 (A2 : Target-uniform identifiability ). H is target-uniformly identifiable (A2 ) if there exists a learning algorithm A with the following property: for every hierarchy ¯X , there is a finite N

such that, for every target f ∈ H , N ¯X 

> A

(f ) ≤ N .

The remaining criteria are domain-uniform. Unlike A1 and A2 , they require the level bound to be chosen without knowing the hierarchy, and hence without knowing the induced train/test split. This is the hierarchy analogue of choosing a PAC sample-size bound before the data-generating distribution is revealed. 

Definition 6 (A3 : Domain-uniform identifiability ). H is domain-uniformly identifiable (A3 ) if there exists a learning algorithm A with the following property: for every target f ∈ H , there exists a finite 

N such that, for every hierarchy ¯X , N ¯X 

> A

(f ) ≤ N .

A3 still permits target-dependent bounds. A4 removes this last dependence: it requires a single finite level that works for all targets and all hierarchies. In this sense, A4 is the fully uniform analogue of a sample-complexity guarantee. 

Definition 7 (A4 : Uniform identifiability ). H is uniformly identifiable (A4 ) if there exist a learning algorithm A and a finite N with the following property: for every target f ∈ H and every hierarchy 

¯X , N ¯X 

> A

(f ) ≤ N .

# 3 Exact Characterization of Domain Generalization 

We now characterize the four learning goals from Section 2.2. 

3.1 Non-Uniform Domain-Dependent Generalization 

Very little, it turns out, is required for the weakest criterion. If we settle for A1 , H being countable is all we need. 

Theorem 1 (A1 ⇔ Countable H). H is non-uniformly identifiable ( A1 , Definition 4) if and only if H

is countable. Proof sketch. Enumerate H and let A be the consistent learner with this fixed tie-breaking order: after observing a prefix, it outputs the first hypothesis in the enumeration that is consistent with 4the observed labels. For f = hm, each lower-index hj is eventually ruled out as ¯X reveals its disagreement with f , leaving hm as the first such hypothesis. Conversely, fix any hierarchy. If a learner identifies every target after some finite prefix, then each target is associated with at least one finite labeled prefix on which the learner outputs it. Since there are only countably many finite labeled prefixes, H must be countable. The countability characterization is still learner-dependent: it says that some chosen rule eventually outputs the target, not that the observed labels have ruled out every other hypothesis. A learner may identify the target by choosing one hypothesis consistent with the observed labels while other hypotheses still agree with those labels. The following example makes this separation explicit. 

Proposition 2. There exist a countable hypothesis class H, a hierarchy ¯X , and a successful learner 

A for H over ¯X (namely, for every f ∈ H , N ¯X 

> A

(f ) < ∞) so that for some f ∗ ∈ H for every finite level n, there exists some gn ∈ H so that gn|Xn = f ∗|Xn , and yet gn̸ = f ∗ (namely, for some x ∈ Ω,

f ∗(x)̸ = gn(x)). Proof sketch. Let Ω = N and H contain the all-zero function 0 together with the singleton indicators 

hi = 1{i}. Take the natural hierarchy Xn = {1, . . . , n } and the target f = 0 . A least-index learner that lists 0 first identifies 0 immediately, so N ¯X 

> A

(0) < ∞. But for every n and every j > n , the singleton hj agrees with 0 on Xn. Thus no finite prefix by itself rules out all alternatives to 0.For the class in Proposition 2, every finite set misses some singleton hypothesis, so the target 0 is not finitely isolated. More generally, finite collapse of the version space is equivalent to the existence of a finite set of domain points that separates the target from all alternatives; this existence does not depend on the ordering of the hierarchy. 

Proposition 3 (Finite isolation is hierarchy-invariant) . Fix f ∈ H . For a hierarchy ¯X , write 

V ¯XH,n (f ) := {g ∈ H : g|Xn = f |Xn } for the version space around f at level n. The following are equivalent: (i) for some hierarchy ¯X , there is a finite level n such that V ¯XH,n (f ) = {f };(ii) for every hierarchy ¯X , there is a finite level n such that V ¯XH,n (f ) = {f };(iii) there is a finite set S ⊆ Ω such that every g ∈ H \ { f } differs from f on some point of S.

One topological way to understand this is the following. For a finite S ⊆ Ω and h ∈ H , let 

[h]S := {g ∈ H : g|S = h|S } be the set of hypotheses that agree with h on the finite observation set S. These finite-observation cells generate a topology on H. For any finite-level hierarchy that exhausts Ω, the same topology is generated by the level version spaces V ¯XH,n (h). In this topology, saying that {f } is open is exactly saying that some finite observation set isolates f , or equivalently that the version space around f collapses at a finite level. The next theorem gives several ways of relaxing the hierarchy quantifier in A1 : one may fix a hierarchy in advance, or even choose the hierarchy separately for each target. These relaxations have the same threshold because they still ask only for target-by-target success. Thus the following statements are equivalent. 

Theorem 4 (Equivalent conditions for A1 ). The following are equivalent: (i) ∃A ∀f ∈ H ∀ ¯X ∃ N ∈ N≥1 : N ¯X 

> A

(f ) ≤ N (that is, H satisfies A1 , Definition 4); (ii) ∃A ∃ ¯X ∀ f ∈ H ∃ N ∈ N≥1 : N ¯X 

> A

(f ) ≤ N ;(iii) ∃A ∀f ∈ H ∃ ¯X ∃ N ∈ N≥1 : N ¯X 

> A

(f ) ≤ N ; (iv) H is countable. 

3.2 Target-Uniform Domain-Dependent Generalization A2 asks for a different kind of success. Once the hierarchy is fixed, the same prefix level must work for every target: the amount of evidence is independent of which f ∈ H generated the labels. 

Theorem 5 (A2 ⇔ Finite H). H is target-uniformly identifiable ( A2 , Definition 5) if and only if H is finite. 

5Proof sketch. With H finite, the least-index learner from Theorem 1 achieves a common bound on any hierarchy: max f ∈H N ¯X 

> A

(f ) < ∞. Conversely, a common bound N on a single hierarchy makes the labeled-prefix map f 7 → f |XN injective on H; otherwise two targets with the same prefix would give the learner the same input at level N . Hence |H| ≤ 2|X N |.The proof isolates the mechanism behind A2 : at a common level, two targets with the same prefix are indistinguishable to any learner. The relevant quantity is therefore the first prefix that separates the class. 

Definition 8 (Class domain complexity) . For a hypothesis class H and a hierarchy ¯X , the class domain complexity is 

N ¯X (H) := min {n ∈ N≥1 : ∀f, g ∈ H , f ̸ = g =⇒ ∃ x ∈ X n : f (x)̸ = g(x)}. (3) 

If no such finite level exists, set N ¯X (H) := ∞.

Proposition 6 (Optimality of class domain complexity) . For every hierarchy ¯X , N ¯X (H) =min A sup f ∈H N ¯X 

> A

(f ), where A ranges over learners and both sides take values in N≥1 ∪ {∞} .

Thus the optimal fixed-hierarchy A2 bound is algorithm-agnostic, and the variants below are equiv-alent: a common bound along one hierarchy already forces H to be finite, while finiteness gives a common bound along every hierarchy. 

Theorem 7 (Equivalent conditions for A2 ). The following are equivalent: (i) ∃A ∀ ¯X ∃ N ∈ N≥1 ∀f ∈ H : N ¯X 

> A

(f ) ≤ N (that is, H satisfies A2 , Definition 5); (ii) ∃A ∃ ¯X ∃ N ∈ N≥1 ∀f ∈ H : N ¯X 

> A

(f ) ≤ N ;(iii) ∃ ¯X : N ¯X (H) < ∞; (iv) ∀ ¯X : N ¯X (H) < ∞; (v) H is finite. 

3.3 Domain-Uniform Generalization Theorem 8 (A3 /A4 characterization) . Assume |H| > 2. The following are equivalent: (i) H is uniformly identifiable ( A4 , Definition 7); (ii) H is domain-uniformly identifiable ( A3 , Definition 6); (iii) Ω is finite. Proof sketch. (i) implies (ii). This is immediate. (iii) implies (i). If Ω is finite, then every hierarchy reveals all of Ω by level |Ω|. At that level the observed labels determine the target on the entire domain. A learner that outputs the unique hypothesis in H consistent with the full-domain labels identifies every target under every hierarchy by level |Ω|.

(ii) implies (iii). Suppose A3 holds through a learner A and target-dependent levels (Nf )f ∈H . If two distinct hypotheses f, g agreed on at least M = max {Nf , N g } points, choose a finite set 

S ⊆ { f = g} of size M and a hierarchy with XM = S. Since f |S = g|S , the learner receives the same input under both targets at level M , but A3 requires the same output to equal both f and g, a contradiction. Hence every pairwise agreement set is finite. Since |H| > 2, choose three distinct hypotheses h1, h 2, h 3 ∈ H . Write {hi = hj } := {x ∈ Ω : 

hi(x) = hj (x)}. At each x ∈ Ω, two of the three binary labels must agree, so 

{h1 = h2} ∪ { h1 = h3} ∪ { h2 = h3} = Ω . (4) The three sets on the left are finite by the previous paragraph. Hence Ω is finite. Under the standing assumption of this paper, Ω is countably infinite. Thus for every class with 

|H| > 2, both A3 and A4 are impossible. The only remaining cases are degenerate. Singleton classes are trivial. For a two-element class H = {h, g }, domain-uniform ( A3 ), equivalently uniform (A4 ), identification is possible exactly when h and g agree on only finitely many points: then any sufficiently large observed region contains a point where they differ, and the label at that point identifies the target. 6Why Domain Generalization Is Hard. The obstruction above is not a usual PAC-learning obstruc-tion from VC dimension, class size, or agnostic noise. It comes from the train/test partition itself. The useful way to read the obstruction is adversarial: Suppose Ω is infinite and take any three distinct hypotheses h1, h 2, h 3 ∈ H . Then some pair must agree on infinitely many points. Indeed, if both {h1 = h2} and {h1 = h3} are finite, then their union is finite, so on infinitely many points h2 and h3 both take the binary label opposite to h1. Hence 

{h2 = h3} is infinite. Thus, from any class with |H| > 2, an adversary can choose two distinct hypotheses f, g with an infinite agreement set and place arbitrarily many of those agreement points at the beginning of the hierarchy. On any such prefix S ⊆ { f = g}, the learner receives the same labeled training set under the two possible targets. Whatever it outputs on that input, at least one of f

and g is not identified. 

# 4 Stratified Domain Complexity 

The characterizations above are qualitative: finite classes admit target-uniform bounds along a fixed hierarchy, while infinite classes do not. To study infinite model families quantitatively, we stratify them by model complexity and ask how the best bound grows with the allowed complexity. 

Complexity Sublevels. A natural refinement is to keep track of model complexity. Many repre-sentation classes of interest are not just infinite sets of functions: programs, automata, grammars, and implemented neural-network families come with finite descriptions or size parameters, such as description length, 2 program length, number of states, grammar size, depth, width, or number of parameters. Such a scale c : H → N organizes the class into bounded-complexity slices. We now formalize this fixed-hierarchy quantity. Fix a hierarchy ¯X on Ω, a hypothesis class H ⊆ {0, 1}Ω, and a model-complexity function c : H → N. For s ∈ N, the sublevel class of model complexity at most s is 

Hc 

> ≤s

:= {f ∈ H : c(f ) ≤ s}. (5) Throughout this section, we consider complexity scales whose sublevels are finite and exhaustive: 

|H c

> ≤s

| < ∞ for every s ∈ N, [

> s∈N

Hc 

> ≤s

= H. (6) 

Sublevel Bounds. Abstractly, every countable class admits such a stratification by enumeration. In applications, however, the scale c is chosen because it reflects a representation of the hypotheses. The quantity of interest is the best target-uniform bound on each sublevel class. 

Definition 9 (Sublevel domain complexity) . The sublevel domain complexity of (H, c ) along ¯X is the function N ¯X 

> (H,c )

: N → N≥1 ∪ {∞} defined by 

N ¯X

> (H,c )

(s) := N ¯X (Hc

> ≤s

) = min i ∈ N≥1 : ∀h̸ = h′ ∈ H c

> ≤s

, ∃x ∈ X i s.t. h(x)̸ = h′(x) . (7) 

If no such i exists, set N ¯X

> (H,c )

(s) := ∞.

Under the finite-sublevel assumption, each sublevel class has some finite target-uniform identifying level along the fixed hierarchy. The next result shows that the smallest possible such bound is exactly the sublevel domain complexity just defined. 

Theorem 9 (Sublevel domain complexity is the optimal rate) . Fix a hierarchy ¯X , a class H ⊆ { 0, 1}Ω,and a complexity scale c : H → N satisfying Equation (6) . Then, for every s ∈ N with Hc

> ≤s

̸ = ∅,

N ¯X

> (H,c )

(s) = min  

> A

sup  

> f∈H c
> ≤s

N ¯X 

> A

(f ), (8) 

where A ranges over learners. If, in addition, |H c

> ≤s

| → ∞ as s → ∞ , then N ¯X

> (H,c )

(s) → ∞ .

Thus finite sublevels give the qualitative guarantee, while Theorem 9 identifies the best possible target-uniform bound at each complexity level. When the sublevel classes grow, these bounds must also diverge. The remaining interesting question is how fast the sublevel domain complexity function grows.  

> 2For a prefix-code description length, Kraft’s inequality implies that only finitely many descriptions have code length at most any fixed s. We use only this finite-slice consequence, not an MDL or probabilistic argument.

75 Length Generalization as a Case Study 

Length generalization is the fixed-hierarchy case in which the hierarchy is ordered by input length and the quantitative question is how sublevel domain complexity grows with model complexity. Concretely, length generalization asks whether a model trained on short inputs succeeds on longer inputs. This question is especially visible for Transformers [Vaswani et al., 2017] trained on algorith-mic tasks, where models can fit all short instances yet fail sharply on longer ones [Anil et al., 2022, Lee et al., 2024, Zhou et al., 2024]. 

Definition 10 (Length hierarchy) . Formally, fix a finite alphabet Σ and consider the hierarchy 

Ω := Σ +, ¯Σ := (Σ ≤N )N ≥0, Σ≤0 := ∅, Σ≤N := Σ ∪ Σ2 ∪ · · · ∪ ΣN (N ≥ 1) . (9) 

A learner trained on lengths at most N observes all labels on Σ≤N and is evaluated on all of Σ+.Thus length generalization is exact identification under the fixed cumulative hierarchy ¯Σ.

We write the representation class as H ⊆ { 0, 1}Σ+

.3 Here H may be a class of languages, programs, or Transformer-realizable predictors, and c may encode description length, program size, Transformer depth, number of heads, parameter precision, norm bounds, or related architectural parameters. The quantitative content of non-asymptotic length generalization lies in estimating the growth of 

N ¯Σ(H,c )(s) for concrete choices of (H, c ).

Existing Results. Huang et al. [2025] give a qualitative fixed-hierarchy, target-wise guarantee for Transformer length generalization: under an idealized regularized inference procedure, targets in their Limit-Transformer class are eventually identified along ¯Σ. This does not estimate a sublevel domain-complexity growth rate. More quantitatively, Chen et al. [2025] estimate this sublevel domain complexity for several repre-sentation classes. For DFAs, the model class is regular languages and the complexity measure is the number of states; their bound is 2q − 2 for targets recognized by DFAs with at most q states. For linear CFGs, the complexity measure is grammar description size, and the analogous sublevel bound has no computable upper bound in that size. For restricted C-RASP, 4 the relevant parameters are precision T in the one-layer Transformer setting, and precision T together with K heads in the two-layer setting; the resulting bounds are O(T 2) and O(T O(K)), respectively. Later work continues the same calculation for more Transformer-facing classes. Yang et al. [2026a] study the same sublevel domain-complexity function for C-RASP, proving uncomputability for full C-RASP and tight exponential growth for positive C-RASP. Izzo et al. [2026] move to an approximate analogue: exact identification is replaced by approximate agreement on all longer inputs, with bounds in terms of norms, locality, periodicity, precision, and margins. 

Beyond Length. The preceding results all concern the fixed length hierarchy ¯Σ. The uncom-putability statements in this literature should not be interpreted as a peculiarity of ordering strings by length. The obstruction can already come from the representation of H: if semantic equivalence of descriptions is undecidable, then the sublevel domain-complexity function has no computable upper bound in the complexity parameter s.To discuss computability, we use a represented class: a finite description p ∈ Π denotes a computable hypothesis hp : Ω → { 0, 1}, and κ : Π → N is a computable complexity scale on descriptions. For a complexity bound s and hierarchy ¯X , write 

HΠ,κ  

> ≤s

:= {hp : p ∈ Π, κ (p) ≤ s}, N ¯X

> (Π ,κ )

(s) := N ¯X (HΠ,κ  

> ≤s

). (10) The quantity on the right is still the information-theoretic domain complexity from Definition 9. A hierarchy is computable if its finite levels can be listed, and two descriptions are semantically equivalent if their denoted hypotheses agree on all of Ω.   

> 3The binary-valued language notation is only for convenience; the same setup extends directly to finite-output maps such as functions Σ∗→Σ.
> 4C-RASP programs compile to softmax Transformers [Yang and Chiang, 2024]; hence C-RASP gives a programmable lower bound on softmax-Transformer expressivity and is often used as a proxy for studying Transformer expressiveness.

8Proposition 10 (Computability obstruction) . If semantic equivalence is undecidable for descriptions in Π, then for every computable exhaustive hierarchy ¯X on Ω, there is no computable function 

B : N → N such that 

N ¯X

> (Π ,κ )

(s) ≤ B(s) for all s ∈ N, (11) For classes such as linear CFGs, this recovers the absence of a computable non-asymptotic bound along ¯Σ shown by Chen et al. [2025], and holds for any computable exhaustive hierarchy. The computability requirement on the hierarchy is essential: without it, the hierarchy itself can encode the finite witnesses that separate the relevant hypotheses. 

Proposition 11 (Necessity of computability) . Let Ω be countably infinite, and let H =

{h1, h 2, . . . } ⊆ {0, 1}Ω be countable. There exists an exhaustive hierarchy ¯X such that 

N ¯X ({h1, . . . , h m}) ≤ m for all m ≥ 1.

Thus, for a complexity scale c with finite sublevels, if the enumeration is ordered so that Hc 

> ≤s

⊆{h1, . . . , h b(s)} for some computable b, the same hierarchy gives N ¯X

> (H,c )

(s) ≤ b(s) for all s. Hence the computability assumption in Proposition 10 is essential. Together, these observations separate two sources of difficulty. The computability obstruction above shows that non-computable rates can arise from the effective description of H itself, provided the hierarchy is computable. The next section concerns the other source: even with (H, c ) fixed, the growth of the sublevel domain complexity need not be determined by this pair alone. 

# 6 No Free Lunch for Domain Generalization 

The preceding sections give meaningful quantitative notions once a hierarchy is fixed. For a target, a learner has an identification level N ¯X 

> A

(f ); for a finite class, N ¯X (H) is the optimal target-uniform bound; and for a complexity scale c, N ¯X

> (H,c )

(s) is the corresponding optimal bound on the sublevel class Hc

> ≤s

. All three are defined relative to the hierarchy ¯X .This dependence is essential. Theorem 8 already shows that, for classes with |H| > 2, domain generalization uniformly over all hierarchies is possible only in the finite-domain case. We now formalize the same dependence as a no-free-lunch result: over an infinite domain, changing the hierarchy alone can make each of the fixed-hierarchy quantities above arbitrarily large. 

Class-Level Bounds. We first ask this question before adding a complexity scale. 

Theorem 12 (No free lunch for domain generalization) . Let Ω be countably infinite. For any 

H ⊆ { 0, 1}Ω with |H| > 2 and every M ∈ N≥1, there exists a hierarchy ¯X , even one revealing a single new point at each level, such that, for every learner A, there exists a target h ∈ H with 

N ¯X 

> A

(h) > M. (12) 

When H is finite, this is equivalent to the class-domain-complexity statement that, for every M ∈ N≥1,there exists a hierarchy ¯X such that N ¯X (H) > M .

Equivalently, the theorem can be read as a train/test no-free-lunch statement. Fix any hypothesis class H with |H| > 2, no matter how small, and fix any desired training size M , no matter how large. There exists a partition Ω = Xtr ⊔ X te with |X tr | = M such that no learner succeeds uniformly for every target. This does not contradict the fixed-hierarchy characterizations: countability gives target-wise existence, and finiteness gives target-uniform existence, after ¯X is fixed. The theorem says that changing ¯X can push the required level past any prescribed M .

Complexity-Stratified Bounds. The same question can be asked after stratifying the class by complexity. Once ¯X is fixed, finite sublevel classes give well-defined optimal target-uniform bounds 

N ¯X

> (H,c )

(s). Can the growth of this function be controlled by (H, c ) alone? 

Corollary 13 (Complexity-stratified no free lunch) . Let Ω be countably infinite, let H ⊆ { 0, 1}Ω

be countably infinite, and let c : H → N have finite sublevel classes. For every nondecreasing 

B : N → N≥1 with B(s) → ∞ , there exist a hierarchy ¯X on Ω and s0 ∈ N such that, for every 

s ≥ s0,

N ¯X

> (H,c )

(s) > B (s). (13) 9The key point is quantitative: with (H, c ) fixed, changing the domain hierarchy can make the sublevel domain-complexity function grow faster than any prescribed unbounded rate B. In particular, for some hierarchy, this function has no computable upper bound. For length generalization, the hierarchy is the length hierarchy ¯Σ from Definition 10. A bound for a Transformer-realizable class under this hierarchy is therefore a statement about training on all strings up to a given length and testing on longer strings. It does not automatically imply the corresponding bound for the same Transformer architecture and the same precision scale under another hierarchy on 

Σ+, for example one ordered by reasoning-trajectory length, recursion depth, theorem difficulty, or planning horizon. The corollary says that such a replacement can make the bound arbitrarily large, even noncomputably so. 

Comparison with Classical NFL. Classical learning-theoretic no-free-lunch theorems are usually read as warnings about hypothesis classes [Shalev-Shwartz and Ben-David, 2014]. If the learner is asked to compete with all possible labelings, there is no structure to exploit and no uniform generalization theorem to be had. Our result moves the warning to a second object that is often kept offstage: the structure connecting what is in domain to what is out of domain. Fixing the hypothesis class is therefore not enough. A domain-generalization statement also needs to say what makes the observed part informative about the unobserved part. Counting hypotheses alone is therefore not enough. Therefore, domain-generalization guarantees cannot be characterized by the concept class alone. They must be properties of the pair (H, ¯X ): the hypotheses together with the hierarchy that determines how observed regions are related to unobserved ones. 

# 7 Extended Discussions 

We consider four extensions: online mistake bounds, approximate prediction, distributional support mismatch, and positive-only presentations. 

7.1 Online Learning 

Online learning keeps the same hierarchy, but replaces the identifying level by a mistake count. Before level n is revealed, the prediction rule has observed the labels on Xn−1 and must predict the labels on the new increment ∆n = Xn \ X n−1. We count one mistake at level n if any of these predictions is wrong. This is weaker than identification. It counts errors only when they are exposed on a newly revealed increment; identification asks whether the labels already observed determine the target on all of Ω.As in Definition 2, the online prediction rule may output an arbitrary binary predictor in {0, 1}Ω, not necessarily an element of H.

Definition 11 (Uniform mistake bound) . For an online prediction rule A, target f ∈ H , and hierarchy 

¯X , define the mistake count 

MA(f, ¯X ) := X

> n≥1

1A(f |Xn−1 )|Xn\X n−1̸ ≡ f |Xn\X n−1

 , (14) 

where X0 = ∅. We say H admits a uniform mistake bound if there exist an online prediction rule A

and a finite M ∈ N such that MA(f, ¯X ) ≤ M for every f ∈ H and every hierarchy ¯X .

Let Ldim( H) denote the Littlestone dimension of H, namely the supremum of the depths of complete binary trees shattered by H. The classical mistake-bound theorem of Littlestone [1988] applies to the criterion above. 

Theorem 14 (Littlestone’s mistake-bound theorem) . H admits a uniform mistake bound if and only if Ldim( H) < ∞. Moreover, the optimal mistake bound is exactly Ldim( H).

Thus Littlestone dimension controls the number of mistaken revealed increments. It does not control the level at which the target has been identified. 10 From Mistakes to Identification. The mistake-bound theorem is uniform over both targets and hierarchies, just as A4 is. The difference is what the bound promises: A4 gives a level after which the target has been identified, while a mistake bound only limits how many revealed increments can be predicted incorrectly. The next proposition places this online criterion between the finite and countable cases characterized in Section 3. 

Proposition 15 (Intermediate mistake complexity) . Assume Ω is countable. Then 

|H| < ∞ =⇒ Ldim( H) < ∞ =⇒ H is countable . (15) Thus, over a countable domain, finite Littlestone dimension sits between finiteness and countability. The same counting argument extends to any infinite domain: if Ldim( H) = d < ∞, then |H| ≤ | Ω|.Fix a well-order of Ω and run a bounded-mistake learner; each target is determined by its at-most-d

mistake locations. Since an infinite Ω has only |Ω| subsets of size at most d, the bound follows. Combining Proposition 15 with Theorems 1, 5 and 14 gives 

A2 =⇒ uniform mistake bound =⇒ A1 . (16) Thus online learning is weaker than target-uniform identification, even when the same mistake bound must work for every hierarchy. A mistake bound controls how many times disagreement can be revealed as error, but not how long the hierarchy can postpone the first point that distinguishes the remaining possible targets. 

Example 1 (One mistake does not imply target-uniform identification) . Let Ω = {x1, x 2, . . . } and consider the class of singleton indicators 

Hsing := {hi : i ≥ 1}, hi(xj ) = 1[i = j]. (17) 

Then Ldim( Hsing ) = 1 , but Hsing does not satisfy A2 .

For this class, online prediction is easy: predict zero until the unique positive point appears. At most one mistake is possible. Identification is different. Along the natural hierarchy XN = {x1, . . . , x N },all targets hi with i > N have the same all-zero labels at level N . Thus no common finite level can force the learner to know which singleton target is correct. 

7.2 Approximation 

A second comparison keeps the hierarchy but weakens exact identification to small P -error. So far, success meant exact identification: after some level, the learner must recover the target on all of Ω.Here we only ask for small error under a fixed evaluation distribution. The question is whether the hierarchy-uniform impossibility from exact identification persists under this weaker requirement. Fix a distribution P on Ω. For a target f ∈ H and predictor h ∈ { 0, 1}Ω, the P -error of h relative to 

f is 

LP,f (h) := P ({x ∈ Ω : h(x)̸ = f (x)}). (18) 

Definition 12 (ε-approximate domain complexity) . For ε > 0, learner A, hierarchy ¯X , evaluation distribution P , and target f ∈ H , define 

N ¯X ,P A,ε (f ) := min {i ∈ N≥1 : ∀n ≥ i, L P,f (A(f |Xn )) ≤ ε}, (19) 

with value ∞ if no such finite i exists. This is the first level after which every later predictor has 

P -error at most ε.

For a fixed hierarchy, approximation has a simple tail-mass guarantee: every consistent learner satisfies 

LP,f (A(f |Xn )) ≤ P (Ω \ X n). (20) Because the hierarchy is exhaustive, P (Ω \ X n) → 0. Thus fixed-hierarchy approximation does not require any combinatorial structure of H. The nontrivial question is whether one can choose a level bound before the hierarchy is known. Say that P has full support if P (x) > 0 for every 

x ∈ Ω. Then every disagreement between distinct hypotheses has positive P -mass: if u̸ = v, then 

P ({u̸ = v}) > 0.11 Proposition 16 (Approximate no free lunch over hierarchies) . Let Ω be countably infinite, let P have full support on Ω, and let H ⊆ { 0, 1}Ω have |H| > 2. Then there exists ε > 0 such that, for every 

M ∈ N≥1, there is a hierarchy ¯X for which every learner A has some target f ∈ H satisfying 

N ¯X ,P A,ε (f ) > M. (21) Thus allowing error does not produce a hierarchy-independent rate. For a fixed hierarchy and fixed evaluation distribution, consistency gives convergence in P -error. But before the hierarchy is fixed, the points on which hypotheses differ can be postponed for arbitrarily many levels, even when those points have positive P -mass. 

7.3 Out-of-Support Generalization 

A third variant returns to a distributional observation model. The learner is no longer given a hierarchy of observed regions; it receives i.i.d. labeled samples from a training distribution Q, returns a binary predictor, and is evaluated under a possibly different distribution P . The hierarchy-uniform impossibility results above do not apply literally to this model. The same issue reappears as a support question: does training ever reveal the distinctions that evaluation can test? The support of Q is the region from which labels can ever be observed. Sampling reveals points in 

supp( Q) randomly, with repetitions, but never reveals labels outside that support. We measure error by the same P -error as in Section 7.2. 

Full-Support Generalization. If Q has full support, meaning Q(x) > 0 for every x ∈ Ω, no point is permanently outside training. Every point that can affect the P -error is at least possible to observe. For finite classes, this gives a target-uniform sample size. 

Theorem 17 (Full-support generalization) . Let Ω be countably infinite, let Q have full support on Ω,fix any evaluation distribution P on Ω, and let H ⊆ { 0, 1}Ω be finite. Then there exists a learner A

such that, for every ε, δ > 0, there is a finite N such that, for every target f ∈ H and every n ≥ N ,

P(x1,...,x n)∼Qn

LP,f 

 A   xi, f (xi)ni=1 

 ≤ ε ≥ 1 − δ. (22) Full-support sampling removes the permanent hiding place for any fixed disagreement set. Since a finite class has only finitely many pairwise disagreement sets, one sample size can make all of them appear with high probability. 

Out-of-Support. If Q is not full support, some labels are never observed. The next theorem says that this gap can break the same target-uniform conclusion even when Q has infinite support: the learner may see infinitely many possible training points, but still not the part of Ω on which evaluation distinguishes two targets. 

Theorem 18 (Out-of-support generalization can fail) . Let Ω be countably infinite, let P have full support on Ω, and let H ⊆ { 0, 1}Ω with |H| ≥ 3. Then there exist a distribution Q with infinite support and an ε > 0 such that, for every learner A, every N ∈ N≥1, and every n ≥ N , there exists a target f ∈ H satisfying 

P(x1,...,x n)∼Qn

LP,f 

 A   xi, f (xi)ni=1 

 > ε  ≥ 12 . (23) Thus the relevant condition is not how many points Q can sample from. It is whether the training support includes the distinctions that P -error will test. Full support guarantees this; an infinite support that is not full need not. 

7.4 Positive-Only Presentations 

Finally, consider a nearby observation model, familiar from identification from text and generation in the limit [Gold, 1967, Kleinberg and Mullainathan, 2024], in which the learner sees only positive examples. For f ∈ H , write 

Pos( f ) := {x ∈ Ω : f (x) = 1 } (24) 12 for its positive region. A positive presentation of f is a strictly increasing sequence ∅ = S0 ⊊ S1 ⊊

S2 ⊊ · · · of finite subsets of Pos( f ) whose union is Pos( f ). We restrict to the nontrivial case in which every Pos( f ) is infinite. Write Pres +(f ) for the set of all such presentations. We ask for the same fully uniform exact-identification guarantee as before, but with positive sets replacing labeled finite domains. Here A denotes a positive-only learner whose input is a finite positive set: 

∃A ∃N < ∞ ∀ f ∈ H ∀ ¯S ∈ Pres +(f ) : ∀t ≥ N, A (St) = f. (25) The relevant condition is finite pairwise positive overlap: distinct targets cannot share arbitrarily many positive points. 

Proposition 19 (Uniform positive-only identification) . Assume Pos( f ) is infinite for every f ∈ H .Then the fully uniform positive-only criterion in Equation (25) holds if and only if 

sup  

> f,g ∈H
> f̸=g

|Pos( f ) ∩ Pos( g)| < ∞, (26) 

with the convention that the supremum over the empty set is 0. Moreover, if this supremum is d, then 

N = d + 1 suffices. 

The condition is not finiteness. For example, take Ω = N × N and, for a, b ∈ N, let 

Ga,b := {(t, at + b) : t ∈ N}, (27) and let fa,b be the indicator of Ga,b . The class {fa,b : a, b ∈ N} is countably infinite, and every positive region is infinite. Yet two distinct graphs intersect in at most one point, so two positive observations already certify the target against every competitor. In our terms, this restricts the presentation to positive examples while keeping exact identification and full uniformity. Under this restriction, the strongest uniform statement can hold even for infinite classes. This positive-only model is closely related to generation in the limit [Kleinberg and Mullainathan, 2024]. There too, the learner sees a growing finite subset of the target language, but only needs to generate one new valid positive example. Since the target is unknown, that point must be positive for every consistent hypothesis; closure dimension characterizes when this can be done uniformly [Raman et al., 2025]. The proposition above shows that the stronger goal of recovering the whole language, equivalently generating all positives, can also hold uniformly. 

# 8 Conclusion 

We characterized hierarchical domain generalization at four levels of uniformity, and proved a no-free-lunch theorem showing that hierarchy-uniform guarantees are achievable only in the trivial finite-domain case. For infinite classes, meaningful guarantees thus live at the level of fixed hierarchies and bounded-complexity slices, as in length generalization. The hierarchy is part of the learning problem. 

# References 

Emmanuel Abbe, Samy Bengio, Aryo Lotfi, and Kevin Rizk. Generalization on the unseen, logic reasoning and degree curriculum. In Proceedings of the 40th International Conference on Machine Learning , volume 202 of Proceedings of Machine Learning Research , pages 31–60. PMLR, 2023. URL https://proceedings.mlr.press/v202/abbe23a.html .Cem Anil, Yuhuai Wu, Anders Andreassen, Aitor Lewkowycz, Vedant Misra, Vinay Ramasesh, Ambrose Slone, Guy Gur-Ari, Ethan Dyer, and Behnam Neyshabur. Exploring length gen-eralization in large language models. Advances in Neural Information Processing Systems , 35: 38546–38556, 2022. URL https://proceedings.neurips.cc/paper_files/paper/2022/ hash/fb7451e43f9c1c35b774bcfad7a5714b-Abstract-Conference.html .Shai Ben-David, John Blitzer, Koby Crammer, and Fernando Pereira. Analysis of repre-sentations for domain adaptation. In Advances in Neural Information Processing Sys-tems 19 , pages 137–144. MIT Press, 2006. URL https://papers.nips.cc/paper/ 2983-analysis-of-representations-for-domain-adaptation .13 Yoshua Bengio, J ´er ˆome Louradour, Ronan Collobert, and Jason Weston. Curriculum learning. In 

Proceedings of the 26th International Conference on Machine Learning (ICML) , pages 41–48. ACM, 2009. doi: 10.1145/1553374.1553380. Thomas Chen, Tengyu Ma, and Zhiyuan Li. Non-asymptotic length generalization. In Proceedings of the 42nd International Conference on Machine Learning , volume 267 of Proceedings of Machine Learning Research , pages 8621–8682. PMLR, 2025. URL https://proceedings.mlr.press/ v267/chen25ar.html .E Mark Gold. Language identification in the limit. Information and Control , 10(5):447–474, 1967. doi: 10.1016/S0019-9958(67)91165-5. Xinting Huang, Andy Yang, Satwik Bhattamishra, Yash Sarrof, Andreas Krebs, Hattie Zhou, Preetum Nakkiran, and Michael Hahn. A formal framework for understanding length generalization in transformers. In International Conference on Learning Representations , volume 2025, pages 58095– 58179, 2025. URL https://proceedings.iclr.cc/paper_files/paper/2025/hash/ 928170bcb050fe64a63fe781b82265aa-Abstract-Conference.html . arXiv:2410.02140. Zachary Izzo, Eshaan Nichani, and Jason D. Lee. Quantitative bounds for length generalization in transformers. In International Conference on Learning Representations , 2026. URL https: //openreview.net/forum?id=TLSUIyBIfs . arXiv:2510.27015. Jon Kleinberg and Sendhil Mullainathan. Language generation in the limit. In Advances in Neu-ral Information Processing Systems , volume 37, pages 66058–66079. Curran Associates, Inc., 2024. doi: 10.52202/079017-2111. URL https://proceedings.neurips.cc/paper_files/ paper/2024/hash/7988e9b3876ad689e921ce05d711442f-Abstract-Conference.html .Nayoung Lee, Kartik Sreenivasan, Jason Lee, Kangwook Lee, and Dimitris Papailiopoulos. Teaching arithmetic to small transformers. In International Conference on Learning Representations , vol-ume 2024, pages 25001–25054, 2024. URL https://proceedings.iclr.cc/paper_files/ paper/2024/hash/6bf82fdcbd92b6a7793b3894422d2437-Abstract-Conference.html .Nick Littlestone. Learning quickly when irrelevant attributes abound: A new linear-threshold algorithm. Machine Learning , 2(4):285–318, 1988. doi: 10.1023/A:1022869011914. Vinod Raman, Jiaxun Li, and Ambuj Tewari. Generation through the lens of learning theory. In Nika Haghtalab and Ankur Moitra, editors, Proceedings of the Thirty Eighth Conference on Learning Theory , volume 291 of Proceedings of Machine Learning Research , pages 4740–4776. PMLR, 2025. URL https://proceedings.mlr.press/v291/raman25a.html .Shai Shalev-Shwartz and Shai Ben-David. Understanding machine learning: From theory to algorithms . Cambridge University Press, 2014. doi: 10.1017/CBO9781107298019. Leslie G Valiant. A theory of the learnable. Communications of the ACM , 27(11):1134–1142, 1984. doi: 10.1145/1968.1972. Vladimir N Vapnik and Alexey Ya Chervonenkis. On the uniform convergence of relative frequencies of events to their probabilities. Theory of Probability & Its Applications , 16(2):264–280, 1971. doi: 10.1137/1116025. Ashish Vaswani, Noam Shazeer, Niki Parmar, Jakob Uszkoreit, Llion Jones, Aidan N Gomez, Łukasz Kaiser, and Illia Polosukhin. Attention is all you need. In Advances in Neural Information Processing Systems , volume 30, pages 5998–6008. Curran Associates, Inc., 2017. URL https: //papers.nips.cc/paper/7181-attention-is-all-you-need .Andy Yang and David Chiang. Counting like transformers: Compiling temporal counting logic into softmax transformers. In First Conference on Language Modeling , 2024. URL https: //openreview.net/forum?id=FmhPg4UJ9K . arXiv:2404.04393. Andy Yang, Pascal Bergstr ¨aßer, Georg Zetzsche, David Chiang, and Anthony W. Lin. Length generalization bounds for transformers. arXiv preprint arXiv:2603.02238 , 2026a. doi: 10.48550/ arXiv.2603.02238. URL https://arxiv.org/abs/2603.02238 .14 Chenxiao Yang, Nathan Srebro, and Zhiyuan Li. Recursive models for long-horizon reasoning. In 

International Conference on Machine Learning , 2026b. URL https://arxiv.org/abs/2603. 02112 . arXiv:2603.02112. Hattie Zhou, Arwen Bradley, Etai Littwin, Noam Razin, Omid Saremi, Joshua Susskind, Samy Bengio, and Preetum Nakkiran. What algorithms can transformers learn? a study in length generalization. In International Conference on Learning Representations , volume 2024, pages 15898–15926, 2024. URL https://proceedings.iclr.cc/paper_files/paper/2024/ hash/45ed1a72597594c097152ef9cc187762-Abstract-Conference.html .15 A Auxiliary Definitions 

The appendix uses the following derived notation. For two hypotheses f, g ∈ { 0, 1}Ω, write 

Eq( f, g ) := {x ∈ Ω : f (x) = g(x)}, (28) also denoted {f = g} when this is typographically lighter. 

Definition 13 (Version space and pointwise class complexity) . Fix a hypothesis class H, a hierarchy 

¯X , and a target f ∈ H . The version space at level n is 

V ¯XH,n (f ) := {g ∈ H : g|Xn = f |Xn }. (29) 

The pointwise algorithm-agnostic domain complexity is 

N ¯XH (f ) := min {n ∈ N≥1 : V ¯XH,n (f ) = {f }} (30) 

= min {n ∈ N≥1 : ∀g ∈ H \ { f }, ∃x ∈ X n : g(x)̸ = f (x)}. (31) 

If no such finite level exists, set N ¯XH (f ) := ∞.

# B Proofs for Exact Characterization 

This appendix proves the characterization results from Section 3. The proofs repeatedly use the same elementary observation: if two targets induce the same labeled sample, then any deterministic learner receives the same input under the two targets and must return the same hypothesis. 

B.1 Non-Uniform Domain-Dependent Generalization 

Proof of Theorem 1 . If H = ∅, the conclusion is vacuous. Assume henceforth that H is nonempty. Assume first that H is countable. Fix an enumeration 

H = {h1, h 2, . . . }. (32) Define the learner A as follows. On any labeled finite sample, if there is at least one hypothesis in H

consistent with that sample, output the least-index consistent hypothesis; if there is none, output h1.Only the consistent case matters on samples generated by targets in H.Fix a target f = hm and a hierarchy ¯X . For every j < m , the hypotheses hj and f are distinct, so choose a point xj ∈ Ω such that hj (xj )̸ = f (xj ). The finite set {xj : j < m } is contained in some prefix XN because ¯X exhausts Ω. For every n ≥ N , each lower-index hypothesis hj with j < m 

disagrees with f on Xn, while f itself remains consistent with f |Xn . Hence the least-index consistent hypothesis is exactly f , so A(f |Xn ) = f for all n ≥ N . Thus N ¯X 

> A

(f ) ≤ N , proving A1 .Conversely, suppose H satisfies A1 , witnessed by a learner A. Fix a point-by-point hierarchy ¯X , say 

Xn = {x1, . . . , x n}. For each f ∈ H , choose a finite level Nf such that A(f |XNf ) = f , which is possible because N ¯X 

> A

(f ) < ∞. Consider the map 

f 7 −→  Nf , f |XNf

. (33) This map is injective. Indeed, if two targets f, g have the same image, then they give A the same labeled sample at the same level; hence 

f = A(f |XNf ) = A(g|XNg ) = g. (34) For each fixed n, there are only finitely many binary labelings of Xn, and there are countably many choices of n. Therefore the range of this injective map is countable, and so H is countable. 

Proof of Proposition 2 . Let Ω = N and 

H = {0} ∪ { hi : i ≥ 1}, hi(j) = 1[i = j], (35) 16 where 0 is the all-zero function. Let the hierarchy be Xn = {1, . . . , n }. Use the least-index consistent learner with 0 listed first. For the target f = 0 , every observed prefix is all zero, so 0 is consistent at every level and is listed first. Hence A(0 |Xn ) = 0 for every n ≥ 1, and N ¯X 

> A

(0) = 1 .However, for every finite n and every j > n , the singleton indicator hj agrees with 0 on Xn. Thus the version space around 0 never collapses at any finite level, even though the learner has already identified the target. 

Proof of Proposition 3 . The implication (ii) ⇒(i) is immediate. If (i) holds, then any level Xn with 

V ¯XH,n (f ) = {f } is such a finite set S. If (iii) holds, then every hierarchy eventually contains S, and at that level the version space around f is {f }.For the topological reformulation, the finite-observation cells [h]S form a basis: finite intersections of such cells are either empty or another finite-observation cell. Since every finite-level hierarchy that exhausts Ω eventually contains any fixed finite S ⊆ Ω, its level version spaces generate exactly the same topology. Finally, {f } is open in this topology if and only if some finite-observation cell [f ]S is equal to {f }, which is exactly finite isolation. 

Proof of Theorem 4 . The implication (i) ⇒(ii) follows by fixing any hierarchy, and (ii) ⇒(iii) follows by allowing the hierarchy to depend on the target. Assume (iii). Then there is a learner A such that for every f ∈ H there are a hierarchy ¯X f and a finite level Nf with 

A(f |X fNf

) = f. (36) Assign to f one such finite labeled sample f |X fNf

. This assignment is injective: if two targets are assigned the same labeled finite sample, then A receives the same input and must output both targets. Since Ω is countable, the collection of finite subsets of Ω is countable, and each finite subset has finitely many binary labelings. Hence there are only countably many finite labeled samples, so H is countable. Finally, if H is countable, then (i) holds by Theorem 1. Therefore all the listed conditions are equivalent. 

B.2 Target-Uniform Domain-Dependent Generalization 

Proof of Theorem 5 . If H = ∅, the conclusion is vacuous. Assume henceforth that H is nonempty. Assume first that H is finite. Use the least-index consistent learner from the proof of Theorem 1, for any fixed enumeration of the finite class. Fix a hierarchy ¯X . By the argument in Theorem 1, every target f ∈ H has a finite identifying level N ¯X 

> A

(f ). Since H is finite, 

N := max   

> f∈H

N ¯X 

> A

(f ) (37) is finite and works for all targets along this hierarchy. The same learner is used for every hierarchy, so A2 holds. Conversely, suppose A2 holds. Then there are a learner A and, for any fixed hierarchy ¯X , a finite level N such that N ¯X 

> A

(f ) ≤ N for every f ∈ H . If two distinct targets f, g ∈ H agreed on XN ,then the learner would receive the same input at level N under both targets. Since N ¯X 

> A

(f ) ≤ N and 

N ¯X 

> A

(g) ≤ N , it would have to output both f and g on that same input, a contradiction. Thus the restriction map 

f 7 −→ f |XN (38) is injective on H. Because XN is finite, it has only finitely many binary labelings. Hence H is finite. 

Proof of Proposition 6 . Fix a hierarchy ¯X . If H = ∅, the identity is vacuous. Assume henceforth that H is nonempty. 17 First suppose a learner A identifies every target in H by some finite level M , meaning 

sup  

> f∈H

N ¯X 

> A

(f ) ≤ M. (39) If two distinct hypotheses f, g ∈ H agreed on XM , then A would receive the same labeled input at level M under both targets. Since both targets must be identified by level M , the same output would have to equal both f and g, impossible. Therefore XM distinguishes every pair in H, so 

N ¯X (H) ≤ M. (40) Since this holds for every learner A with a finite uniform identifying level, it gives 

N ¯X (H) ≤ min  

> A

sup  

> f∈H

N ¯X 

> A

(f ). (41) Conversely, suppose N ¯X (H) = N < ∞. Define a learner that, on a labeled finite sample, outputs the unique hypothesis in H consistent with that sample if such a unique hypothesis exists, and outputs an arbitrary fixed element of H otherwise. For every target f ∈ H and every n ≥ N , the prefix Xn

contains XN , and XN distinguishes every pair in H. Hence f is the unique hypothesis in H consistent with f |Xn , so the learner outputs f . Therefore 

min  

> A

sup  

> f∈H

N ¯X 

> A

(f ) ≤ N ¯X (H). (42) If N ¯X (H) = ∞, the first paragraph shows that no learner can have a finite uniform identifying level. Thus both sides are ∞. This proves the identity in all cases. 

Proof of Theorem 7 . The implication (i) ⇒(ii) is immediate by fixing one hierarchy. If (ii) holds with learner A, hierarchy ¯X , and bound N , then no two distinct targets can agree on XN : otherwise 

A would receive the same labeled input under both targets at level N , but would have to output two different functions. Hence XN distinguishes every pair in H, so N ¯X (H) < ∞, proving (iii). If (iii) holds, then for some hierarchy ¯X and some finite N , the prefix XN distinguishes every pair in 

H. The restriction map f 7 → f |XN is injective, so H injects into the finite set of binary labelings of 

XN . Hence H is finite, proving (v). If H is finite, then for every hierarchy ¯X and every distinct pair f, g ∈ H , there is some point xf,g 

with f (xf,g )̸ = g(xf,g ). Since ¯X exhausts Ω, each such point appears in some finite prefix. Taking the maximum over the finitely many pairs gives a finite level whose prefix distinguishes every pair in 

H. Thus (iv) holds. If H has at most one element, the distinguishing condition is vacuous and any level, say 1, works. Finally, (iv) ⇒(iii) is immediate, and (v) ⇒(i) also follows from Theorem 5. Therefore all five conditions are equivalent. 

B.3 Domain-Uniform Generalization 

Proof of Theorem 8 . The implication (i) ⇒(ii) is immediate, since a single bound that works for all targets and all hierarchies is in particular allowed to depend on the target. Assume next that Ω is finite. Under the finite-domain convention, every hierarchy has revealed all of 

Ω by level |Ω|. Consider the learner that outputs the unique hypothesis in H consistent with the full observed labels once the full domain has been revealed, and is arbitrary before that. For any target 

f ∈ H , the full-domain labeled sample determines f uniquely inside H. Hence this learner identifies every target along every hierarchy by level |Ω|, proving A4 , and therefore A3 .It remains to prove that A3 implies Ω is finite, under the assumption |H| > 2. Suppose A3 is witnessed by a learner A. For every f ∈ H , choose a finite level bound Nf such that 

N ¯X 

> A

(f ) ≤ Nf (43) for every hierarchy ¯X .18 We first show that every pair of distinct hypotheses has a finite agreement set. Let f, g ∈ H be distinct and set M = max {Nf , N g }. If Eq( f, g ) contained at least M points, choose a hierarchy whose 

M -th prefix consists of M such agreement points. At level M , the labeled samples generated by f

and by g are identical. But M ≥ Nf , N g , so domain-uniform identifiability requires A to output f

on this input under target f and g on the same input under target g, impossible. Therefore Eq( f, g )

has fewer than M points, and is finite. Now choose three distinct hypotheses h1, h 2, h 3 ∈ H . Since the labels are binary, at each point 

x ∈ Ω at least two of the three values h1(x), h 2(x), h 3(x) are equal. Hence 

Ω = Eq( h1, h 2) ∪ Eq( h1, h 3) ∪ Eq( h2, h 3). (44) Each set on the right is finite by the previous paragraph, so Ω is finite. This proves (ii) ⇒(iii), and completes the equivalence. 

# C Proofs for Stratified Domain Complexity 

Proof of Theorem 9 . Fix a hierarchy ¯X , a class H ⊆ { 0, 1}Ω, and a complexity scale c : H → N.Fix s ∈ N. By Equation (6), Hc 

> ≤s

is finite. For every distinct pair f, g ∈ H c

> ≤s

, choose a point xf,g 

with f (xf,g )̸ = g(xf,g ). Since ¯X exhausts Ω, each such point appears in some finite prefix. Taking the maximum over the finitely many pairs gives a finite prefix that distinguishes every pair in Hc

> ≤s

. If 

|H c

> ≤s

| ≤ 1, any level, say 1, works. Thus 

N ¯X

> (H,c )

(s) < ∞. (45) For the optimality identity, fix s with Hc

> ≤s

̸ = ∅. By definition, 

N ¯X

> (H,c )

(s) = N ¯X (Hc

> ≤s

). (46) Applying Proposition 6 to the class Hc 

> ≤s

gives 

N ¯X

> (H,c )

(s) = min  

> A

sup  

> f∈H c
> ≤s

N ¯X 

> A

(f ), (47) with both sides taking values in N≥1 ∪ {∞} .Finally suppose |H c

> ≤s

| → ∞ . The sublevel classes are nested in s, so the numbers N ¯X

> (H,c )

(s) are nondecreasing. If they did not tend to infinity, then there would be a finite M and arbitrarily large s

with N ¯X

> (H,c )

(s) ≤ M . For each such s, the prefix XM would distinguish every pair in Hc

> ≤s

, so the restriction map into {0, 1}XM would be injective on Hc

> ≤s

. This gives 

|H c

> ≤s

| ≤ 2|X M | (48) for arbitrarily large s, contradicting |H c

> ≤s

| → ∞ . Therefore N ¯X

> (H,c )

(s) → ∞ .

Proof of Proposition 10 . Suppose, toward a contradiction, that for some computable exhaustive hierarchy ¯X there is a computable function B satisfying the bound in the statement. Given two descriptions p, q ∈ Π, compute 

s := max {κ(p), κ (q)}. (49) Compute B(s) and enumerate XB(s). Since evaluation is decidable from descriptions, we can check whether hp and hq disagree on some point of XB(s). If they do, then they are not semantically equivalent. If they do not, then hp and hq agree on XB(s). But hp, h q ∈ H Π,κ  

> ≤s

, and B(s) is at least the sublevel domain complexity of this slice. Hence any two distinct hypotheses in HΠ,κ  

> ≤s

would have been separated inside XB(s). Therefore hp = hq on all of Ω, and the procedure decides semantic equivalence from descriptions. 19 Proof of Proposition 11 . Fix an enumeration z1, z 2, . . . of Ω. We construct the hierarchy recursively. Set X0 = ∅. Given Xm−1, for each i < m choose a point wi,m ∈ Ω such that hi(wi,m )̸ = hm(wi,m ).Let um be the first point in the enumeration of Ω that is not contained in 

Xm−1 ∪ { wi,m : i < m }. (50) Define 

Xm := Xm−1 ∪ { wi,m : i < m } ∪ { um}. (51) Each Xm is finite and Xm−1 ⊊ Xm. The added points um ensure that every point of Ω eventually appears, so ¯X is exhaustive. Finally, for any pair i < j ≤ m, the point wi,j was added by level j, and hence belongs to Xm; it separates hi from hj . Thus Xm distinguishes every pair in {h1, . . . , h m}, so 

N ¯X ({h1, . . . , h m}) ≤ m.

# D Proofs for the No-Free-Lunch Results 

D.1 Class-Level No Free Lunch 

Proof of Theorem 12 . Choose three distinct hypotheses h1, h 2, h 3 ∈ H . Since labels are binary, the three pairwise agreement sets 

Eq( h1, h 2), Eq( h1, h 3), Eq( h2, h 3) (52) cover Ω: at every point, at least two of the three binary labels agree. Since Ω is infinite, at least one of these three agreement sets is infinite. Let g0, g 1 ∈ H be a distinct pair with Eq( g0, g 1) infinite. Fix M ∈ N≥1. Choose distinct points 

z1, . . . , z M ∈ Eq( g0, g 1), (53) and extend them to an enumeration z1, z 2, . . . of Ω. Define the point-by-point hierarchy 

Xn := {z1, . . . , z n}. (54) Then g0 and g1 agree on XM .Now let A be any learner. At level M , the labeled sample generated by target g0 is the same as the labeled sample generated by target g1. Therefore A has the same output on both samples. It cannot be equal to both distinct functions g0 and g1. Hence at least one of the two targets, call it h, is not identified by level M , so 

N ¯X 

> A

(h) > M. (55) The hierarchy was constructed before choosing A, so this proves the first claim. When H is finite, the equivalent class-domain-complexity statement follows from Proposition 6. Indeed, 

N ¯X (H) > M (56) if and only if no learner can identify every target in H by level M , which is equivalent to saying that every learner has some target h ∈ H with N ¯X 

> A

(h) > M .

D.2 Complexity-Stratified No Free Lunch 

Proof of Corollary 13 . Fix an enumeration 

Ω = {y1, y 2, . . . }. (57) We will construct a point-by-point hierarchy by constructing an enumeration z1, z 2, . . . of Ω. The construction ensures that, for infinitely many increasing complexity thresholds, a pair of hypotheses inside the corresponding sublevel class agrees on a very long initial prefix. 

Step 1: a reusable extension claim. We first state the combinatorial step used at every stage. Let 

P ⊆ Ω be finite, let S ∈ N, and let R ⊆ Ω be infinite. Since Hc 

> ≤S

is finite and H is infinite, the set 

H \ H c 

> ≤S

is infinite. Only finitely many binary label patterns can occur on P , so infinitely many hypotheses in H \ H c 

> ≤S

share the same pattern on P . Choose three such hypotheses. On every point 20 of R, at least two of their three binary labels agree; since the three pairwise agreement sets cover R,one pair agrees on infinitely many points of R. Thus there exist distinct f, g ∈ H \ H c 

> ≤S

such that 

f |P = g|P and Eq( f, g ) ∩ R is infinite . (58) 

Step 2: build a hierarchy with longer and longer concealed pairs. Choose any three hypotheses in H. Their three pairwise agreement sets cover the infinite set Ω, so one pair (f0, g 0) has infinite agreement set. Let 

A0 := Eq( f0, g 0), S0 := max {c(f0), c (g0)}, P0 := ∅. (59) Inductively, suppose we have a finite prefix Pk of the ordering, a pair (fk, g k), an infinite agreement set Ak := Eq( fk, g k), and a threshold Sk := max {c(fk), c (gk)}, with Pk ⊆ Ak. The invariant means that the current pair agrees on every point revealed so far. Apply the combinatorial step with the finite set Pk ∪ { yk+1 }, the threshold Sk, and the infinite set Ak.We obtain a pair (fk+1 , g k+1 ) outside Hc 

> ≤Sk

such that it agrees on Pk ∪ { yk+1 } and has infinitely many agreement points inside Ak. Set 

Ak+1 := Eq( fk+1 , g k+1 ), Sk+1 := max {c(fk+1 ), c (gk+1 )}. (60) Because both fk+1 and gk+1 lie outside Hc 

> ≤Sk

, we have Sk+1 > S k. Also Ak ∩ Ak+1 is infinite, and 

Pk ∪ { yk+1 } ⊆ Ak+1 .Extend the current ordering by adding unused points from Ak ∩ Ak+1 until at least the first B(Sk+1 )

positions have been filled. Then append yk+1 if it has not already appeared. Let Pk+1 be the resulting finite prefix. This preserves the induction invariant Pk+1 ⊆ Ak+1 : the old prefix and yk+1 lie in 

Ak+1 , and the added points lie in Ak ∩ Ak+1 . It also ensures that the first B(Sk+1 ) points of the ordering lie in Ak.Since yk+1 is inserted by the end of stage k + 1 if it has not appeared earlier, every point of Ω

eventually appears. The limiting ordering z1, z 2, . . . is therefore an enumeration of Ω, and it defines a point-by-point hierarchy ¯X by 

Xn := {z1, . . . , z n}. (61) 

Step 3: verify the lower bound on the sublevel domain complexity. It remains to verify the bound. The thresholds Sk are strictly increasing, hence Sk → ∞ . Let s ≥ S0. Choose k such that 

Sk ≤ s ≤ Sk+1 . (62) The pair (fk, g k) lies in Hc 

> ≤s

because both complexities are at most Sk ≤ s. By construction, fk

and gk agree on the first B(Sk+1 ) points of the hierarchy. Since B is nondecreasing and s ≤ Sk+1 ,they agree on the first B(s) points. Therefore XB(s) does not distinguish all pairs in Hc

> ≤s

, and by the definition of sublevel domain complexity, 

N ¯X

> (H,c )

(s) > B (s). (63) Thus the corollary holds with s0 = S0.

# E Proofs for Online Learning 

Proof of Theorem 14 . We first prove the upper bound when d := Ldim( H) < ∞, using the conven-tion Ldim( ∅) = −1. The learner maintains the version space Vn ⊆ H of hypotheses consistent with all labels revealed before level n. Thus V1 = H, and after level n is revealed the version space is restricted to hypotheses consistent with the new labels. For a point x and a label b ∈ { 0, 1}, write 

Vn(x, b ) := {h ∈ Vn : h(x) = b}. (64) Before seeing the labels on Xn \ X n−1, the learner predicts at each x a label b maximizing 

Ldim( Vn(x, b )) , breaking ties arbitrarily. After all labels at level n are revealed, it updates the version space to the subset of Vn consistent with the whole increment. 21 We claim that every mistaken level strictly decreases the Littlestone dimension of the version space. Suppose level n is mistaken. Then for some x ∈ X n \ X n−1, the learner predicted 1 − b while the true label is b = f (x). Since the learner chose a label of maximum Littlestone dimension, 

Ldim( Vn(x, 1 − b)) ≥ Ldim( Vn(x, b )) . (65) Both Vn(x, 0) and Vn(x, 1) are subclasses of Vn, so their Littlestone dimensions are at most 

Ldim( Vn). If Ldim( Vn(x, b )) = Ldim( Vn), then also Ldim( Vn(x, 1 − b)) = Ldim( Vn), and placing x at the root above two shattered trees of depth Ldim( Vn) would shatter a tree of depth 

Ldim( Vn) + 1 , a contradiction. Hence 

Ldim( Vn(x, b )) < Ldim( Vn). (66) The updated version space after the whole increment is contained in Vn(x, b ), so its Littlestone dimension is also strictly smaller than Ldim( Vn). Since the dimension starts at d and never goes below zero, at most d levels can be mistaken. For the lower bound, let d be any integer such that H shatters a complete binary tree of depth d. We show that every deterministic learner can be forced to make d mistakes; if Ldim( H) = ∞, this holds for every finite d.The adversary follows a shattered tree. At the root, it presents the root instance as the first singleton increment. If the learner predicts 0, the adversary reveals label 1; if the learner predicts 1, it reveals label 0. The adversary then moves to the child corresponding to the revealed label and repeats this procedure for d levels. By shattering, the resulting root-to-leaf sequence of labels is realized by some target f ∈ H .The points along the realized path are distinct. If the same point appeared twice on one root-to-leaf path, then at the later occurrence the two outgoing branches would require both labels while the earlier occurrence has already fixed one label, contradicting shattering. Thus the path points can be used as distinct singleton increments of a hierarchy, which is then completed arbitrarily to exhaust Ω.By construction, the learner makes one mistake at each of the first d levels. Therefore no learner can have a uniform mistake bound smaller than d. Taking the supremum over all shattered depths gives the lower bound Ldim( H).

Proof of Proposition 15 . If H shatters a complete binary tree of depth d, then the 2d root-to-leaf label sequences must be realized by 2d distinct hypotheses: two paths that first diverge at a node assign different labels to the same instance at that node. Hence, when H is finite, 

2d ≤ |H| (67) for every shattered depth d, so Ldim( H) ≤ log 2 |H| < ∞.Now suppose Ldim( H) = d < ∞. By Theorem 14, there is a deterministic online prediction rule that makes at most d mistakes on every target and hierarchy. Fix a singleton hierarchy enumerating 

Ω, say Xn = {x1, . . . , x n}.For each target f ∈ H , let Ef ⊆ N be the finite set of times at which this learner makes a mistake when run against f on the fixed hierarchy. Then |Ef | ≤ d. The set Ef determines f : given Ef ,replay the learner from the beginning; at time n, compute its prediction from the labels reconstructed so far, and set the true label to be the prediction if n / ∈ Ef and the opposite label if n ∈ Ef . This reconstructs f (xn) for every n. Since the hierarchy enumerates Ω, it reconstructs f on all of Ω.Thus the map f 7 → Ef is injective from H into the collection of finite subsets of N of size at most d,which is countable. Therefore H is countable. 

Verification of Example 1. The class shatters a one-point tree: at any point xj , the hypothesis hj

realizes label 1, while any hi with i̸ = j realizes label 0. Hence Ldim( Hsing ) ≥ 1.It cannot shatter a depth-two tree. If the root is labeled by some point xj and the root label is 1, then the only consistent target is hj . All later labels are then fixed, so the next node cannot realize both outgoing labels. Thus no depth-two tree is shattered, and Ldim( Hsing ) = 1 .Equivalently, the all-zero online predictor makes at most one mistake on this class, namely when the unique positive point of the target singleton is revealed. 22 Finally, consider the singleton hierarchy Xn = {x1, . . . , x n}. Suppose A2 held along this hierarchy with some learner and common level N . For every i > N , the target hi gives the same all-zero labeled sample on XN . At level N , the learner would therefore receive the same input for all targets 

hi with i > N , but would have to output each of these distinct targets. This is impossible. Hence no target-uniform identification bound exists, so A2 fails. 

# F Proofs for Approximation 

Proof of Proposition 16 . Choose three distinct hypotheses in H. Since the labels are binary, the three pairwise agreement sets cover Ω; because Ω is infinite, some pair f, g agrees on infinitely many points. Since P has full support and f̸ = g,

∆ := P ({f̸ = g}) > 0. (68) Set ε := ∆ /3. Given M , choose a finite set S ⊆ { f = g} of size M , list these points first, and complete the list to a hierarchy with XM = S. Since f |S = g|S , the learner returns the same predictor on the two training sets; call it ˆh. On every point where f and g disagree, ˆh is wrong for at least one of the two targets. Hence 

∆ ≤ LP,f (ˆh) + LP,g (ˆh). (69) Therefore at least one of the two targets, say u ∈ { f, g }, has LP,u (A(u|XM )) ≥ ∆/2 > ε , and so 

N ¯X ,P A,ε (u) > M .

# G Proofs for Out-of-Support Generalization 

Proof of Theorem 17 . Enumerate the finite class as H = {h1, . . . , h M } and use the least-index consistent learner: given a finite labeled sample, output the first hypothesis in the enumeration consistent with all observed labels, if one exists, and output h1 otherwise. The learner ignores the order and multiplicity of repeated sample points. If M = 1 , the claim is immediate. Assume M ≥ 2. For every pair of distinct hypotheses hj , h k ∈ H ,define 

Dj,k := {x ∈ Ω : hj (x)̸ = hk(x)}. (70) Each Dj,k is nonempty and has positive Q-mass because Q has full support. Let 

α := min   

> j̸=k

Q(Dj,k ) > 0. (71) If the sample contains at least one point from every pairwise disagreement set, then the observed labels distinguish the target from every other hypothesis in H. The least-index consistent learner therefore outputs the target, and the P -error is zero. By a union bound, the probability of missing some pairwise disagreement set is at most 

M (M − 1)(1 − α)n, (72) which tends to zero as n → ∞ . Hence for every δ > 0 there is a finite N such that, for all n ≥ N ,the sample hits every pairwise disagreement set with probability at least 1 − δ. On this event, the learner outputs the target for every f ∈ H , and therefore has P -error at most ε for every target. This gives the target-uniform bound. 

Proof of Theorem 18 . Choose three distinct hypotheses in H. Their three pairwise agreement sets cover the infinite domain Ω, so some pair u, v ∈ H has an infinite agreement set Eq( u, v ). Choose any distribution Q with infinite support contained in Eq( u, v ); for example, enumerate countably many distinct points in Eq( u, v ) and assign them probabilities proportional to 2−k.Since u̸ = v, the disagreement set {u̸ = v} is nonempty. Because P has full support, 

∆ := P ({u̸ = v}) > 0. (73) Choose ε < ∆/2.Fix a learner A and a sample size n. Samples from Q always lie in Eq( u, v ), so the labeled sample given to A is identical under target u and target v. For any fixed sample sequence, let ˆh be this 23 common learner output. At each point where u and v disagree, ˆh is wrong for at least one of the two targets. Therefore 

LP,u (ˆh) + LP,v (ˆh) ≥ ∆. (74) Thus, for every sample sequence, at least one of the two targets has error larger than ε. Averaging over Qn, at least one of the two targets has failure probability at least 1/2. Since A and n were arbitrary, this holds for every learner and every sample size; in particular, it holds for every N and every n ≥ N .

# H Proofs for Positive-Only Presentations 

Proof of Proposition 19 . First suppose 

d := sup  

> f,g ∈H
> f̸=g

|Pos( f ) ∩ Pos( g)| < ∞. (75) Define a learner as follows. Given a finite positive set S, if there is a unique h ∈ H such that 

S ⊆ Pos( h), output that h; otherwise output an arbitrary binary predictor. Fix a target f ∈ H and a positive presentation ¯S ∈ Pres +(f ). Since the presentation is increasing and every step adds a positive point, |St| ≥ t. Hence |St| ≥ d + 1 for every t ≥ d + 1 . No g̸ = f can contain all points in St, because then |Pos( f ) ∩ Pos( g)| ≥ | St| ≥ d + 1 , contradicting the definition of d. Thus f is the unique hypothesis whose positive region contains St, and the learner outputs f for all t ≥ d + 1 .Conversely, suppose 

sup  

> f,g ∈H
> f̸=g

|Pos( f ) ∩ Pos( g)| = ∞. (76) Let A be any learner and let N < ∞. Choose f̸ = g with |Pos( f ) ∩ Pos( g)| ≥ N , and choose distinct points x1, . . . , x N ∈ Pos( f ) ∩ Pos( g). Since both positive regions are infinite, we can extend the common initial sequence 

St = {x1, . . . , x t}, 1 ≤ t ≤ N, (77) to positive presentations of both f and g. At time N , the learner receives the same input under the two targets. It therefore gives the same output in both worlds, and cannot be correct for both f and g.No learner can satisfy Equation (25) with this N , and since N was arbitrary, no uniform finite bound exists. 

An infinite example. The finite-overlap condition can hold for infinite classes. Let Ω = N × N

and, for a, b ∈ N, define 

Ga,b := {(t, at + b) : t ∈ N}, (78) and let fa,b be the indicator of Ga,b . Each Pos( fa,b ) = Ga,b is infinite, and the class {fa,b : a, b ∈ N}

is countably infinite. Two distinct such sets intersect in at most one point, since two distinct affine functions over N agree at at most one input. Hence the pairwise positive overlap is at most one, and two positive observations uniformly identify the target. 

# I Limitations and Scope 

Our results are information-theoretic: they characterize when identification is possible, not how to compute the identifying level for a given (H, c, ¯X ). Three structural restrictions are worth noting. First, we work in the realizable, deterministic-labeling setting; agnostic and noisy presentations are deferred to future work. Second, we focus on exact identification; approximate analogues with ε-error are discussed in Section 7.2. Third, the framework is set-indexed rather than sequence-indexed, hiding within-level order; online-learning variants are discussed in Section 7.1. The work is theoretical and does not raise direct societal concerns. 24