---
title: "TESSERA v2: Scaling Pixel-wise Earth Foundation Models"
title_zh: TESSERA v2：扩展像素级地球基础模型
authors: "Zhengpeng Feng, Sadiq Jaffer, Ira Shokar, Jovana Knezevic, Mark Elvers, Clement Atzberger, Robin Young, Aneesh Naik, Niall Robinson, Andrew Blake, David Coomes, Anil Madhavapeddy, Srinivasan Keshav"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03949v1"
tags: ["query:rs-fm"]
score: 9.0
evidence: 像素级地球观测基础模型的最大规模缩放研究
tldr: "像素级地球观测基础模型的缩放规律尚不明确。本研究进行了395次可控实验，发现预训练损失无法预测下游性能，需根据下游任务选择模型。提出编码器与数据同步增长、投影器固定的缩放规则，训练了0.5B和1B模型并蒸馏为21M参数的紧凑学生模型TESSERA v2-1B-M，性能超越所有现有模型。Matryoshka表示以1/8存储保留92%性能，代码将开源。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03949v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1449, \"height\": 831, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03949v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 1041, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03949v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1432, \"height\": 804, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03949v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1428, \"height\": 861, \"label\": \"Figure\"}]"
motivation: 探索像素级地球观测基础模型的缩放规律和预算分配策略，解决预训练损失与下游性能弱关联的问题。
method: 基于Barlow Twins框架进行395次缩放实验，提出编码器与数据共缩放、投影器固定的规则，并通过知识蒸馏训练紧凑学生模型。
result: "TESSERA v2-1B-M以21M参数超越所有开源和闭源模型，Matryoshka表示在1/8存储下保留92%全维度性能。"
conclusion: 给出了像素级EO基础模型的实用缩放配方：训练大编码器、根据下游性能选择、蒸馏为灵活学生模型。
---

## 摘要
像素级地球观测基础模型现在通过生成空间嵌入实现了最先进的性能。然而，这些模型如何扩展以及如何最佳地分配预训练预算仍鲜为人知。我们展示了迄今为止最大规模的地球观测可控扩展研究：在固定像素级Barlow Twins框架内，使用1,024个GH200超级芯片进行395次训练运行，每次评估15个下游任务。我们发现，预训练损失几乎无法预测下游性能（|Pearson r| < 0.2），因此根据损失选择模型会浪费大量计算资源。我们还发现，随着训练预算增加，编码器和数据应同步增长，而投影器保持固定，这为分配计算提供了简单规则。利用这一规则，我们训练了一系列像素级模型（0.5B和1B，另有一个2B模型正在训练），并将其蒸馏为紧凑的学生模型，用于嵌入即数据部署。参数为2100万的蒸馏TESSERA v2-1B-M模型在总体上优于所有测试的开源和专有模型，其中一些模型规模大数个数量级。这些学生模型产生嵌套表示，服务成本低廉：16维前缀在1/8存储下保留了完整128维性能的92%。训练完成后，我们计划发布覆盖2017年至2025年的全球嵌入v2版本。综合这些结果，我们为扩展像素级地球观测基础模型提供了具体且基于经验的准则：训练大型编码器，根据下游性能选择模型，并将其蒸馏为灵活的学生模型。所有代码将在https://github.com/ucam-eo/tessera发布。

## Abstract
Pixel-wise Earth-observation (EO) foundation models are now achieving state-of-the-art performance via generated spatial embeddings. However, how these models scale and how best to spend a pretraining budget remain poorly understood. We present the largest controlled scaling study for EO to date: 395 training runs on 1,024 GH200 superchips within a fixed pixel-wise Barlow Twins family, each evaluated on 15 downstream tasks. We find that pretraining loss barely predicts downstream performance (|Pearson r| < 0.2), so selecting models by loss wastes a large share of the compute. We also find that, as the training budget grows, the encoder and the data should grow together while the projector stays fixed, which gives a simple rule for allocating compute. Using this rule, we train a family of pixel-wise models (0.5B and 1B, with a 2B model in training) and distill them into compact students for embeddings-as-data deployment. The 21-million-parameter distilled TESSERA v2-1B-M in aggregate outperforms all open and proprietary models tested, some of which are orders of magnitude larger. These students produce Matryoshka representations that are inexpensive to serve: a 16-dimensional prefix keeps 92% of the full 128-dimensional performance at 1/8 of the storage. Upon completion of training we plan to release v2 global embeddings covering 2017-2025. Together, these results give a concrete, empirically grounded recipe for scaling pixel-wise EO foundation models: train large encoders, select by downstream performance, and distil into flexible student models. All code will be released at https://github.com/ucam-eo/tessera.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **研究动机**：像素级地球观测（EO）基础模型在生成空间嵌入方面已取得先进性能，但其缩放规律（scaling laws）和预训练预算的分配策略尚不明确。同时，现有嵌入产品（如 TESSERA v1、Alpha Earth）要么是固定维度嵌入（缺乏适应性），要么不是完全开放的分析就绪产品，用户无法根据部署成本灵活调整模型大小和嵌入维度。
*   **整体含义**：旨在解决 EO 嵌入产品的“可扩展性”与“部署经济性”矛盾——即如何高效地训练一个大型模型并使其能够以低成本、自适应方式部署给下游用户。论文提出了一个从缩放研究到蒸馏部署的完整生产配方。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **核心思想**：预训练损失不是下游性能的良好代理，因此应采用下游任务驱动的缩放研究，得出计算预算的分配规则（编码器和数据一起缩放，投影器固定），然后训练一个大型教师模型，再通过蒸馏将其压缩为可部署的紧凑学生模型，并利用 Matryoshka 表示提供多维度选择。
*   **关键技术细节**：
    *   **缩放研究**：固定 Barlow Twins 框架（架构通过7轴消融预先选优），在 395 次训练中系统地改变编码器大小（N_enc，16种宽度，7–278 M）、投影器大小（N_proj，4种宽度）和训练数据量（D，0.03–9984 M d-pixels）。定义计算量 C = 12·D·(N_enc/L_ref + N_proj)，L_ref=240。在每个 iso-FLOP 桶内，通过 log N 的二次拟合找到计算最优大小。
    *   **教师模型**：1B 参数（实际为 1B？文中提到 teacher 是 1B，但 Sweep 中 encoder 最大 278M，teacher 是 extrapolate 的），双分支 Transformer（Sentinel-1 和 Sentinel-2 分别处理），使用日间位置编码、学习注意力池化、两层融合 Transformer 输出 768 维嵌入。训练时采用随机采样长度 L~Uniform{8,16} 和 mix-up 一致性正则化。推理时采用自适应桶采样（bucket ladder {16,32,48,…}）以容纳所有有效观测。
    *   **蒸馏与学生模型**：蒸馏到 4 个学生（1M/7M/21M/44M 参数），使用 Matryoshka 蒸馏目标 L_DIST = Σ_{k∈K} (1 - cos(h_k(s_{1:k}), t))，其中每个前缀 k ∈ {16,32,64,128} 通过线性头映射到教师嵌入空间。这样，每个学生可以输出任意前缀长度（16/32/64/128）的嵌入，无需重新训练。核心发现：只有蒸馏（而非自监督）才能为 Matryoshka 前缀提供有意义的坐标排序。

### 3. 实验设计：数据集、基准、对比方法

*   **数据集与场景**：
    *   训练数据：全球 Sentinel-1/2 像素级时间序列（d-pixels），teacher 约 4.2B d-pixels，蒸馏约 200M d-pixels。
    *   评估套件：共 29 个任务（15 个 Alpha Earth 套件任务 + 14 个额外 held-out 数据集），涵盖分类、分割、回归、变化检测、分布预测等。
    *   验证地域：主要集中在 well-studied 区域。
*   **基准方法**：对比模型包括 TESSERA v1、Alpha Earth、PRESTO、OlmoEarth、MOSAIKS，以及多种其他 RSFMs（如 Scale-MAE、CROMA、SkySense、OmniSat、ViT 等）。
*   **对比协议**：每个任务使用固定轻量级头部（像素级用两层 MLP，面片级用 <2M 参数的简单 CNN）。公平性由相同的评估框架和固定的头部确保。

### 4. 资源与算力

*   **计算资源**：
    *   缩放研究：使用 1,024 个 NVIDIA GH200 超级芯片（每个含 Grace CPU + H100 GPU，96GB HBM3）。
    *   教师训练：512 个 GPU，全局 batch size 131,072，bf16 精度， FSDP，单 epoch。
    *   蒸馏训练：64 个 H100 GPU。
*   **时间**：文中未明确给出具体训练时长（如多少天），但指出 1B 教师推理一次全球数据约需 50 H100-years，学生为 0.04–2 H100-years。

### 5. 实验数量与充分性

*   **实验数量**：
    *   缩放研究：395 次受控训练运行，每次在 15 个下游任务上评估。
    *   架构消融：沿 7 个结构轴逐一选择。
    *   稳定性检验：bootstrap 和 leave-one-bucket-out 验证指数稳定性。
    *   最终评估：在 29 个任务的完整套件上比较 7 个以上的基线模型。
*   **充分性与公平性**：
    *   充分性：系统性地覆盖了不同的计算预算范围，从极小（0.03M d-pixels）到接近生产规模（9,984M）。下游评估任务多样，且包含 held-out 测试集。
    *   公平性：固定架构、固定轻量头部、使用同一评估管线（Alpha Earth 套件）。对比模型均为同一产品范式（嵌入即数据）或知名基线，且已在各自最优设置下使用。

### 6. 论文的主要结论与发现

1.  **预训练损失无力预测下游性能**：Pearson r = -0.18，Spearman ρ = -0.16，损失驱动的模型选择会浪费约 254% 的计算。
2.  **计算最优分配规则**：N_enc ∝ C^0.36，D ∝ C^0.63，N_proj ∝ C^0.00（投影器不增长）。0.36+0.63≈1 验证了计算与数据/模型的线性关系（类似 Hoffmann 法则）。
3.  **蒸馏学生模型领先**：TESSERA v2-1B-M（21M 参数）在 29 个任务上取得最佳复合得分 0.611，优于 TESSERA v1（0.576）、Alpha Earth（0.574）、OlmoEarth-L（0.562）。在 Alpha Earth 套件上，v2-1B-L 得分为 0.584，也超越 Alpha Earth（0.560）和 v1（0.541）。
4.  **Matryoshka 前缀高效**：d=16 保存了 d=128 约 92% 的复合得分，存储仅 1/8。
5.  **蒸馏优于自监督嵌套**：自监督无法为 Matryoshka 前缀提供有意义排序，而蒸馏提供了必要的监督信号。

### 7. 优点

*   **系统性与下游驱动**：首次在 EO 领域进行大规模、受控、下游驱动的缩放研究，而非依赖预训练损失。这揭示了一个重要的反直觉发现——损失不可信。
*   **完整的生产链条**：从缩放研究推导出分配规则 → 训练大型教师 → 蒸馏为可部署学生 → 提供自适应维度（Matryoshka）。设计目标是产品级的“分析就绪”嵌入，而非仅仅一个模型。
*   **全面的评估**：29 个任务（涵盖公开和 held-out），公平比较多种基线（包括开源和专有）。结果具有强说服力。
*   **可复现性与经济性**：承诺开源所有代码、学生检查点和评估工具。蒸馏后的学生具有极高的部署效率（存储、推理均可调）。
*   **方法论创新**：发现蒸馏可解决自监督 Matryoshka 的坐标排序问题，为后续 EO 嵌套嵌入研究提供了新思路。

### 8. 不足与局限

*   **缩放规律的通用性有限**：该规律仅适用于像素级 Barlow Twins 框架下的 Sentinel-1/2 编码器，以及 15 个特定下游任务。对于其他架构（如 ViT patch-based）、其他模态（如高光谱、雷达干涉）或其他目标（如变化检测特定），规律可能需要重新验证。
*   **教师模型昂贵**：训练 1B 教师需要大量计算（单 epoch 使用 512 GPU），且推理一次全球数据需约 50 H100-years。虽然预处理算力可作为产品预算摊销，但对资源有限的机构仍是一个障碍。
*   **评估地域偏差**：基准大多源于“well-studied regions”（如欧美、成熟农作物区），在未充分覆盖的气候带、极端季节或贫穷数据地区的泛化能力未验证。
*   **时态覆盖**：尽管使用了年度序列，但嵌入是静态的（一年一版）。对于需要日内变化、快速扰动检测的应用可能不够。
*   **损失-下游关系**：虽发现弱相关，但未深入分析损失与特定任务类型的关系，也未提供为何损失失效的理论解释（仅给出定性猜测：云和轨道采样主导方差，冗余减少目标学到的恒等性与下游物理过程正交）。

（完）
