---
title: "DA-MergeLoRA: Hypernetwork-Based LoRA Merging for Few-Shot Test-Time Domain Adaptation"
title_zh: DA-MergeLoRA：基于超网络的LoRA合并用于少样本测试时域适应
authors: "Siobhan Reid, Zhixiang Chi, Li Gu, Omid Reza Heidari, Ziqiang Wang, Yang Wang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17467v1"
tags: ["query:cross-domain"]
score: 9.0
evidence: 小样本测试时域自适应方法
tldr: 少样本测试时域适应（FSTT-DA）旨在用少量无标签目标样本适应新域，但现有方法未能有效利用源域知识。本文提出DA-MergeLoRA，为每个源域在CLIP视觉编码器上独立微调LoRA模块，并通过元学习训练的超网络动态生成合并权重，将多个LoRA模块融合为统一表示。在多个域适应数据集上达到最先进性能，验证了该框架的有效性。该方法兼顾源域知识保留与目标域适应，为少样本域适应提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17467v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1225, \"height\": 471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17467v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 888, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17467v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1142, \"height\": 533, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17467v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1273, \"height\": 490, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17467v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1269, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17467v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1271, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17467v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1264, \"height\": 196, \"label\": \"Table\"}]"
motivation: 现有FSTT-DA方法未能有效利用源域特定知识，依赖浅层批归一化或黑盒提示，适应性能受限。
method: 对每个源域在CLIP视觉编码器上独立微调LoRA模块，再用元学习训练的超网络生成合并权重，融合为适应目标域的表示。
result: 在多个域适应基准数据集上达到最先进性能，显著优于现有方法。
conclusion: 通过LoRA模块动态合并，高效融合源域知识，为少样本测试时域适应提供新范式。
---

## 摘要
少样本测试时域适应（FSTT-DA）旨在仅使用少量未标记的目标样本来使模型适应新领域。该设置比典型的域适应设置更现实，因为典型设置假定在源训练期间可以访问目标数据。然而，先前的FSTT-DA方法未能有效利用源领域特定知识，它们依赖于浅层的批归一化更新、将模型视为黑盒的基于提示的方法，或者无法捕捉跨域关系的集成策略。为了解决这些限制，我们引入了一种新的FSTT-DA框架，该框架将LoRA微调与模型合并相结合。在我们的方法中，针对每个源领域，在CLIP的视觉编码器上单独微调LoRA模块。由于LoRA仅修改模型的一小部分参数，它在内部学习领域特定特征的同时保留了基础模型的泛化知识。为了将学到的知识适应到特定目标领域，我们提出了一种通过元学习训练的超网络，该网络生成每列的合并因子以组合LoRA模块。给定一小批目标图像，超网络产生合并权重，将源LoRA模块融合为单一的适应表示。我们的结果在各种域适应数据集上展示了最先进的性能。我们的代码公开在https://github.com/nahbois4321/DA-MergeLoRA。

## Abstract
Few-shot Test-Time Domain Adaptation (FSTT-DA) seeks to adapt models to novel domains using only a handful of unlabeled target samples. This setting is more realistic than typical domain adaptation setups, which assume access to target data during source training. However, prior FSTT-DA approaches fail to effectively leverage source domain-specific knowledge, relying on shallow batch normalization updates, prompt-based methods that treat the model as a black box, or ensembling strategies that do not capture cross-domain relationships. To address these limitations, we introduce a new FSTT-DA framework that integrates LoRA fine-tuning with model merging. In our approach, separate LoRA modules are fine-tuned on CLIP's vision encoder for each source domain. Since LoRA modifies only a small fraction of the model's parameters, it retains the base model's generalized knowledge while internally learning domain-specific features. To adapt the learned knowledge to a specific target domain, we propose a hypernetwork trained via meta-learning that generates per-column merging factors to combine LoRA modules. Given a small batch of target images, the hypernetwork produces merging weights that fuse source LoRA modules into a single adapted representation. Our results demonstrate state-of-the-art performance across various domain adaptation datasets. Our code is publicly available at https://github.com/nahbois4321/DA-MergeLoRA.

---

## 论文详细总结（自动生成）

# 论文《DA-MergeLoRA: Hypernetwork-Based LoRA Merging for Few-Shot Test-Time Domain Adaptation》中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：少样本测试时域适应（FSTT-DA）旨在仅使用少量无标签目标样本使模型适应新域，比传统域适应设置更现实（传统方法假设源训练时可访问目标数据）。
- **背景**：现有FSTT-DA方法（如MABN依赖浅层BN更新、VDPG/L2C使用黑盒提示、Meta-DMoE集成策略）未能有效利用源域特定知识，存在泛化性差、跨域知识共享不足、计算开销大等局限。
- **动机**：提出一种既能保留CLIP基础模型泛化能力，又能将多源域知识高效融合到目标域的新框架。

## 2. 论文提出的方法论

### 核心思想
- 为每个源域单独训练一个LoRA模块（附着在CLIP视觉编码器上），冻结基座参数以保留泛化知识。
- 在测试时，利用一个元学习训练的超网络，根据少量无标签目标图像，生成**每列（per-column）合并权重**，将多个源LoRA模块的ΔW矩阵按列加权求和，得到一个专门适应目标域的单一LoRA模块。

### 关键技术细节
- **LoRA模块训练**：每个源域独立训练LoRA（秩r=16, α=32），仅更新A、B矩阵，基座CLIP冻结。使用交叉熵损失，文本提示嵌入（如“a photo of a <classname>”）预计算。
- **超网络架构**：
  - **输入**：目标图像经CLIP编码后取平均嵌入，再经MLP投影到128维；所有源域LoRA的ΔW列经投影到128维，并附加可学习的层索引token、子层类型token（attention/projection）、域token。
  - **交叉注意力**：将目标嵌入作为Query，处理后的LoRA列作为Key，计算每个列上各源域的注意力权重（softmax+温度τ），作为合并权重。
  - **合并**：对各列按权重加权求和得到ΔW_merged，加到CLIP基座权重上。
- **元学习训练**：
  1. 每次迭代随机选取一个源域作为“伪目标域”，将其LoRA列置零。
  2. 从该域采样support集（无标签图像）和query集（有标签）。
  3. 超网络用support嵌入和其余源域LoRA列生成合并权重，应用到CLIP。
  4. 合并模型在query集上计算交叉熵损失，反向传播更新超网络参数（基座和源LoRA全部冻结）。
- **推理**：对真实目标域，用无标签batch和所有源LoRA模块，超网络一步生成合并权重，得到适应模型，用于整个目标域。

### 公式/算法流程（文字说明）
- 算法1给出了伪代码：每个meta-epoch：选伪目标域→采样support/query→计算support嵌入→设置伪目标LoRA为零→超网络生成逐列权重→加权合并ΔW→应用合并模型→在query上计算CE损失→更新超网络。

## 3. 实验设计

- **数据集**：
  - **DomainNet**：6个域（Clipart, Infograph, Painting, Quickdraw, Real, Sketch），345类，约600k图像。采用留一域测试协议。
  - **WILDS**：iWildCam（物种分类）、Camelyon17（淋巴结组织病理）、FMoW（卫星图像土地利用分类，带年份域偏移）、PovertyMap（经济福祉回归）。
- **基准与对比方法**：
  - CNN基类：ERM、Mixup、CORAL、MTL、SegNet、ARM、Meta-DMoE、MABN。
  - ViT基类：Zero-shot CLIP、ERM (ViT)、MIRO、VDPG、L2C，以及DoPrompt。
- **评价指标**：官方数据集指标（DomainNet：各域准确率及平均；iWildCam：Acc和Macro F1；Camelyon17：Acc；FMoW：Acc和WC Acc；PovertyMap：Pearson r和WC Pearson r）。
- **训练配置**：CLIP ViT-B/16，LoRA rank=16，α=32，学习率3e-4~5e-4（LoRA训练）和1e-5~5e-5（超网络训练），batch size=16，超网络训练1~5个epoch（PovertyMap用额外MLP回归头，50 epochs）。iWildCam源域聚类为10组。

## 4. 资源与算力

- **文中未明确说明**：未提及使用的GPU型号、数量、训练时长等具体硬件资源信息。仅在附录A.3提及“详细超参数配置和训练过程”，但文本中未包含该附录内容。因此无法给出具体算力统计。

## 5. 实验数量与充分性

- **主实验**：2个表（DomainNet留一域结果、WILDS四个数据集结果）。
- **消融实验**：
  - 表2a：对比多种LoRA合并策略（LoRA-universal、average、entropy-average、TIES、AdaMerge）在WILDS三个分类数据集上。
  - 表2b：移除超网络组件（层token、域token、目标图像输入替换为噪声或可学习向量）的影响。
- **额外分析（附录A.4）**：架构比较（MLP vs cross-attention）、合并粒度（per-model/per-layer/per-column）、支持集大小和源域数量敏感性、最难案例分析、t-SNE可视化、合并权重与域相似度相关性、推理计算效率比较（速度、内存）。
- **充分性评价**：
  - 实验覆盖多类域偏移场景（风格、年份、地点、成像条件），对比方法涵盖CNN和ViT基线，消融设计较全面。
  - 使用官方评价协议和多次运行（3次随机种子取均值标准差），结果客观。
  - 但部分数据集（iWildCam、QuickDraw）上性能略低于某些基线，消融实验结果有波动，整体公平。

## 6. 论文的主要结论与发现

- DA-MergeLoRA在DomainNet上平均准确率比SOTA FSTT-DA方法L2C提高1.24%（达到62.44%），在WILDS上Camelyon17提高2.70%（96.90%）、FMoW提高2.80%（57.60%）、FMoW最差域准确率提高11.36%（52.26%），PovertyMap回归指标也有提升。
- 合并权重热力图显示：训练后超网络倾向于给与目标域视觉相似或难度较低的源域更大权重，说明学到有意义的域选择策略。
- 与简单参数平均、熵加权、TIES、AdaMerge等相比，超网络方法在复杂域偏移（iWildCam、FMoW）上优势显著，在简单域（Camelyon17）上相近。
- 移除目标图像输入或层/域token会降低性能，表明条件合并和位置/源域标签的必要性。

## 7. 优点

- **方法创新**：将FSTT-DA转化为参数空间合并问题，利用LoRA轻量保留基础模型泛化能力，通过超网络动态按列合并，灵活性高。
- **元学习训练**：模拟目标域适应过程，使超网络学会从少量无标签数据推断最优合并策略。
- **可解释性**：合并权重热图可视化揭示了超网络对源域的选择偏好，有助于理解模型行为。
- **性能SOTA**：在多个基准上超越现有方法，尤其在困难域（FMoW最差准确率大幅提升）。
- **效率**：推理时仅需一次超网络前向和一次合并，比集成多个专家模型更高效（文中提及比L2C快约50%、内存减少50%）。

## 8. 不足与局限

- **资源信息缺失**：未报告训练/推理所需的GPU型号、时长、显存等，不利于复现和实际部署评估。
- **部分域表现不足**：DomainNet的Quickdraw域和iWildCam的F1分数低于某些基线，表明对极端风格或类不平衡的域适应仍有挑战。
- **对新类别支持有限**：实验假设标签空间共享，未处理开放集或新类别情况。
- **源LoRA训练依赖**：需要为每个源域单独训练LoRA，虽然轻量，但在源域数量很多时训练开销仍可能增加。
- **元学习伪目标选择**：训练时随机选取伪目标域，可能忽略某些域组合下的最优策略；文中未进行交叉验证。
- **文本模态未充分利用**：仅用简单提示模板，未结合CLIP文本编码器进行更深入的视觉-语言对齐来提升适应。
- **应用限制**：需要预训练CLIP和多源域数据，不适用于单源域或无法获得源域数据的场景。

（完）
