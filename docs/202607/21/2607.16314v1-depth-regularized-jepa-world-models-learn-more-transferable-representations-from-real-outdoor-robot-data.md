---
title: Depth-Regularized JEPA World Models Learn More Transferable Representations from Real Outdoor Robot Data
title_zh: 深度正则化的JEPA世界模型从真实户外机器人数据中学习更可迁移的表征
authors: Usman M. Khan
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16314v1"
tags: ["query:world-models"]
score: 9.0
evidence: 深度正则化的JEPA世界模型用于真实户外机器人数据
tldr: "从真实户外机器人视频学习世界模型面临视觉复杂性和不可预测环境。本文提出在JEPA架构训练中引入深度作为几何先验，结合各向同性潜在正则化器SIGReg最大化任务无关的潜在多样性，并采用训练时过参数化以增加复杂度而不增加推理时间。在农业机器人视频上训练的18M参数模型，在冻结表征的视觉里程计探测中误差降低33%，惊奇分数分离在域内和域外均显著提升，多步rollout保真度在域偏移下随步长增长。结果表明轻量级训练时几何先验使紧凑JEPA世界模型在真实户外数据上更鲁棒和可迁移，无推理开销。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1622, \"height\": 1150, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1707, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 1449, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 717, \"label\": \"Table\"}]"
motivation: 现实户外环境视觉复杂，现有JEPA世界模型难以从真实机器人视频数据中学习鲁棒潜在动态。
method: 在JEPA训练中引入深度几何先验和SIGReg各向同性正则化，并采用训练时过参数化提升容量。
result: "视觉里程计误差降低33%，惊奇检测分离度在域内外均提升，多步rollout保真度随步长改善。"
conclusion: 轻量训练时深度先验增强紧凑JEPA世界模型在真实户外数据上的泛化与迁移性，无推理开销。
---

## 摘要
世界模型，尤其是基于JEPA架构的模型，已被证明能够学习各种环境的鲁棒动力学。然而，从视觉复杂的真实世界数据中学习仍然是一个挑战，尤其是在不可预测的户外环境中。我们引入深度作为训练过程中的几何先验，直接从机器人视频数据中学习更鲁棒的潜在动力学并处理视觉复杂性。这将深度监督与各向同性诱导的潜在正则化器（SIGReg）相结合，最大化与任务无关的潜在多样性，同时约束多样性的组织方式，综合目标旨在获得与场景几何一致的最高熵表征。为满足这种更高的复杂性而不增加推理时间，我们还添加了仅训练时的过参数化。在真实农业机器人的视频上训练一个1800万参数的模型，我们通过冻结表征的视觉里程计探测、基于预测器的意外检测和多步潜在展开保真度进行评估。与基线LeWM相比，我们的方法将视觉里程计探测误差降低了33%，显著提高了领域内和领域外TartanGround基准上的意外分数分离度，并在领域偏移下改善了多步展开保真度，其增益随展开步长增加而增大。值得注意的是，我们还在与3D几何不直接相关的物理理解（如光照和阴影）上观察到意外分数分离度的改善。这些结果表明，轻量级的训练时几何先验使紧凑的JEPA世界模型在真实户外数据上更具实用性和可迁移性，且具有强大的底层表征，且不增加推理开销。我们的工作表明，深度作为物理基础的先验可以增强世界模型在各种任务上的泛化能力。

## Abstract
World models, especially based on JEPA architectures, have been shown to learn robust dynamics of various environments. However, learning from visually complex real-world data remains a challenge, especially in unpredictable outdoor environments. We introduce depth as a geometric prior during training in learning more robust latent dynamics directly from robot video data and handling visual complexity. This combines depth supervision with an isotropy-inducing latent regularizer (SIGReg), maximizing task-agnostic latent diversity while constraining how that diversity is organized, with the combined objective targeting the highest-entropy representation consistent with scene geometry. To satisfy this greater complexity without increasing inference time, we also add training-only overparameterization. Training an 18M-parameter model on video from a real agricultural robot, we evaluate with frozen-representation visual odometry probes, predictor-based surprise detection, and multi-step latent rollout fidelity. Compared to the baseline LeWM, our method lowers visual odometry probe error by 33%, substantially increases surprise-score separation both in-domain and on the out-of-domain TartanGround benchmark, and improves multi-step rollout fidelity under domain shift, with gains that grow with rollout horizon. Notably, we also see improvements in surprise-score separation on physics understanding that is not directly tied to 3D geometry, such as lighting and shadows. These results show that a lightweight training-time geometric prior makes a compact JEPA world model more useful and more transferable on real outdoor data with strong underlying representations, without adding inference overhead. Our work suggests that depth as a physically grounded prior can enhance world model generalization on a variety of tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有 JEPA 世界模型在模拟环境或简单室内场景中表现良好，但在真实户外机器人数据（如农业机器人）上面临视觉复杂性高（变化光照、阴影、运动模糊、植被杂波等）的挑战。直接像素预测容易建模无关的噪声变化，而 JEPA 的潜在预测虽避免像素重建，但易发生表征坍缩。已有方法如 V-JEPA 规模大、训练复杂，而 LeWorldModel（LeWM）通过 SIGReg 正则化实现紧凑训练，但仅在模拟基准上验证过，尚未在真实户外数据中测试。
- **核心问题**：如何从真实户外机器人视频中训练紧凑的 JEPA 世界模型，使其学到鲁棒、可迁移的潜在表征，同时保持低推理开销？
- **整体含义**：本文证明，通过在训练时引入深度几何先验（深度对齐损失），并结合各向同性正则化（SIGReg）和训练时过参数化，能够显著提升紧凑 JEPA 模型的表征质量、意外检测能力和跨域泛化性能，且推理时不需深度输入。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在 LeWM 基础上增加一个仅训练时使用的深度正则化损失，将 RGB 嵌入与来自配准深度相机的嵌入对齐（stop-gradient 处理深度分支），从而为 JEPA 的潜在空间注入几何结构。同时，利用 SIGReg（各向同性高斯正则化）最大化任务无关的潜在多样性，两者结合追求与场景几何一致的最高熵分布。
- **关键技术细节**：
  - **基础模型**：采用 LeWM 架构，ViT-Tiny 编码器，嵌入维度 192，输入 224×224 RGB 图像，历史长度 3，预测未来 1 步，6 层 predictor。
  - **损失函数**：\( \mathcal{L} = \mathcal{L}_{\text{pred}} + 0.09 \cdot \mathcal{L}_{\text{sigreg}} + 0.1 \cdot \mathcal{L}_{\text{depth}} \)，其中 \(\mathcal{L}_{\text{depth}} = 1 - \cos(f_\theta(x_{\text{RGB}}), \text{sg}[f_\theta(x_{\text{depth}})])\)。
  - **训练时过参数化**：对 predictor 的 MLP 上/下投影添加 rank-16 的低秩 RepLinear 分支，训练后通过代数合并还原为原始权重，不增加推理成本。
  - **深度数据**：来自立体相机，截断至 1 米，训练时仅用于对齐，推理时完全不用。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - 训练集：Aigen Element 农业机器人收集的约 31K 帧视频，85 个 episode，包含 RGB 和配对深度。
  - 验证集：3,587 帧，9 个 episode。
  - 域外基准：TartanGround 数据集（地面机器人），取 15,608 帧，6 个 episode，涵盖多种环境，存在 sim-to-real 域差异。
- **基准与评价指标**：
  - **视觉里程计（VO）线性探测**：从冻结的嵌入回归自运动，评估表征质量（MSE）。
  - **意外检测**：对短片段施加破坏物理规律的人工扰动（瞬移、时间反转、亮度突变、颜色互换），计算 predictor 的预测误差作为意外分数，报告准确率和分离度（异常帧与正常帧的意外分数差）。
  - **多步展开保真度**：给定真实动作序列，自回归地展开潜在预测，计算目标相似度（最终帧）和轨迹相似度（所有中间帧）的余弦相似度，步长 h=10 和 h=20。
- **对比方法**：
  - 主对比：Vanilla LeWM（无深度正则化） vs. 深度正则化 LeWM（本文）。
  - 参考基线：冻结的 DINOv2 编码器（非 LeWM 家族），仅用于 VO 探测和展开保真度对比。

### 4. 资源与算力

- **论文未明确说明** GPU 型号、数量、训练时长等算力信息。仅提及模型为 18M 参数，训练 100 个 epoch，但硬件详情未给出。因此需要在总结中指出未明确说明。

### 5. 实验数量与充分性

- **实验数量**：论文主要在三个任务（VO 探测、意外检测、多步展开）上对比了基线模型和深度正则化模型，并在域内（Aigen-val）和域外（TartanGround）两个数据集上评估。详细结果见 Table 1。此外，额外分析了光照等非几何扰动的分离度，以及跨域潜在几何的对齐情况（PCA 分析、子空间重叠等）。
- **充分性评估**：
  - **优势**：实验覆盖了表征质量、预测器行为、多步动力学三个关键维度，并且包含了域内和域外两种场景，结果一致且增益显著。针对意外检测细分了多种扰动类型，并提供了分离度这一更精细的指标。跨域几何分析直接验证了表征对齐。
  - **不足**：仅与单一基线 LeWM 和 DINOv2 对比，未与其他大规模世界模型（如 V-JEPA、Dreamer）或近期方法比较，基线范围较窄。未对训练时过参数化的单独贡献进行消融实验（文中已提及留作未来工作）。数据集规模相对较小（~31K 帧），且仅涉及农业机器人单一平台，泛化性有待更大规模验证。未进行闭环机器人实际控制实验。

### 6. 论文的主要结论与发现

- 深度正则化显著提升表征质量：VO 探测误差降低 33%（MSE 从 0.0022 降至 0.0015）。
- 意外检测分离度在域内提升 79%（0.067→0.120），在域外提升 182%（0.035→0.099），且对非几何扰动（如亮度跳变）也有明显改善。
- 多步展开保真度在域外大幅提升，且增益随展开步长增加而增大（h=20 时目标相似度从 0.458 升至 0.537），说明模型的动力学更鲁棒且更可迁移。
- 跨域潜在几何分析显示深度正则化模型在两个域的子空间重叠更高、分离程度稍小，表明学习到了更通用的结构。
- 结论：轻量级训练时深度先验（无需推理成本）能使紧凑 JEPA 世界模型在真实户外数据上获得更鲁棒、更可迁移的表征，尤其适合部署在计算资源受限的平台。

### 7. 优点：方法或实验设计上的亮点

- 方法简洁有效：仅添加一个训练时的余弦对齐损失和一个训练时过参数化分支，推理时完全无额外开销。
- 训练时过参数化采用结构重参数化（RepLinear），可无损合并，设计巧妙。
- 实验设计全面：不仅评估标准表征质量，还设计了物理意义明确的意外检测和多步展开测试，并区分域内外，结果可信度高。
- 跨域潜在几何可视化（PCA、子空间重叠）提供了额外的解释性，验证了表征对齐的机制。
- 工作填补了紧凑 JEPA 模型在真实户外机器人数据上训练的空白，具有实际应用价值。

### 8. 不足与局限

- **实验覆盖不足**：未与其他大规模世界模型（如 V-JEPA、Dreamer 等）对比，仅与 LeWM 基线比较；未分析不同深度权重或过参数化秩的影响。
- **数据规模小且单一**：仅使用约 31K 帧来自一个农业机器人平台的数据，是否能在更大数据量或多种机器人上复现增益未验证。
- **无闭环验证**：评估仅在离线潜在空间进行，没有实际机器人闭环操作结果。
- **计划任务未展示**：论文提到初步采样规划实验不成功，未给出有效的下游规划结果。
- **计算资源未报告**：未说明训练所需的 GPU 型号、数量和时间，不利于复现和成本评估。
- **消融实验缺失**：未分离深度损失和过参数化的各自贡献，仅提出留作未来工作。

（完）
